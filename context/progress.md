# GSP Management Information System — Progress Tracker

> **Living document. Single source of truth for what is actually built.**
> Update this file at the end of every work session and every time a Feature Loop step is completed.
> The plan of _what to build_ lives in [build-plan.md](build-plan.md); this file records _what is done_.

**Last updated:** 2026-07-26 · **Current phase:** Phase 3 — Finance & Admin · **Current feature:** 3.5 Profile Management is done (●) — Phase 3 is complete, 5/5. **All 21 features across all 4 phases are done — the build plan is complete.** Post-completion follow-up: 1.1's open decision #7 (email-sending service) is now resolved — `forgot-password` sends a real email and a new `/reset-password` page + endpoint complete the flow. The manual-testing bug-fix batch, 1.6's one-troop-per-leader revision, 1.3's Troop-Leader-scoping revision (list/read/create/update/renew scoped server-side to the leader's own troop, plus its Troop column → header badge / Troop filter follow-up), and the cross-cutting header-cleanup revision are all committed and pushed to `origin/master`. A small 1.5 fix (My Troop Roster capped/scrollable so an unbounded troop can't stretch the dashboard grid) and a `context/test-guide.md` expansion (one-troop-per-leader guard steps + fuller Membership Registry troop-scoping steps) are committed as of this session — see Session Log 2026-07-26. The two bug fixes described lower in this same header (1.3's archive/restore pre-archive-status loss, and the cross-cutting edit-form-reverts-mid-edit fix across 8 modals) are now committed as `1f48df3`. Three more bug fixes, all user-reported, are now committed as `f6f4f6b` and pushed to `origin/master`: (1) the Save-button-not-disabled-until-dirty fix across 8 modals, (2) Badges'/Finance's member-option pickers now include `expiring` members alongside `active` (previously silently excluded, even though an expiring member is still current — renewal due within 30 days, not yet lapsed), (3) announcement-post notifications now carry the real title/content pair instead of the title repeated as both fields. See Session Log 2026-07-26 for the full breakdown of all three. Three Finance/Reports revisions (Payments filter bar, Excel-content fix + PDF styling + Excel export disabled, 731-day report date-range cap) plus a Settings revision (Backups tab hidden) are recorded in Session Log 2026-07-26 below. Latest session: Settings' Email Notifications toggle also hidden, and 1.1's forgot-password now actually delivers — real SMTP (Gmail) wired via `apps/api/src/shared/utils/email.ts`, with a Mailtrap HTTP API option also available, plus a branded HTML template (`email-templates.ts`). Same session, a security gap was raised and fixed: self-signup for Executive Council/Troop Leader was fully open (immediate active session, no verification) — now lands `isActive: false` pending Administrator approval via the existing 3.4 Users & Access panel, live-verified end-to-end; committed as `a796cca` and pushed to `origin/master`. Immediately after, the user asked how `ADMIN_SIGNUP_KEY` works when deployed — answered, and while doing so surfaced 3 related deployment-hardening gaps (no rate limiting on `/auth/*`, no audit entry on admin self-signup, the key is a single non-rotating shared secret); logged as Open Decision #8, deliberately deferred, not yet acted on. Everything else from that prior batch (Settings toggles hidden, forgot-password real email delivery) was already committed as `5ad8f8d`. Latest session: user/test-guide docs synced to the approval-gate revision (`302f038`), and a user-reported bug fixed — deleting the last user on a non-first Users & Access page landed on an empty "no users" state instead of returning to the previous page; see Session Log 2026-07-26.

---

## Status Legend

| Symbol | Status            | Meaning                                                                           |
| ------ | ----------------- | --------------------------------------------------------------------------------- |
| ☐      | Not started       | No work begun                                                                     |
| ◔      | UI + Mock         | Full page built with mock data (Loop step 1)                                      |
| ◑      | Visually verified | Human sign-off on look/responsive/states (Loop step 2) — **gate passed**          |
| ◕      | Read wired        | Real data renders on screen (Loop steps 3–4)                                      |
| ●      | Done              | Writes wired, validated, RBAC, tested — meets Definition of Done (Loop steps 5–6) |
| ⚠      | Blocked           | Cannot proceed — blocker noted in the entry                                       |

Feature Loop steps referenced above are defined in [build-plan.md](build-plan.md) §1.

---

## Summary Dashboard

| Phase                     | Features | ☐      | ◔     | ◑     | ◕     | ●     | ⚠     |
| ------------------------- | -------- | ------ | ----- | ----- | ----- | ----- | ----- |
| Phase 0 — Foundation      | 5        | 0      | 0     | 0     | 0     | 5      | 0     |
| Phase 1 — Core Operations | 6        | 0      | 0     | 0     | 0     | 6      | 0     |
| Phase 2 — Activities      | 5        | 0      | 0     | 0     | 0     | 5      | 0     |
| Phase 3 — Finance & Admin | 5        | 0      | 0     | 0     | 0     | 5      | 0     |
| **Total**                 | **21**   | **0**  | **0** | **0** | **0** | **21** | **0** |

**Overall completion: 21 / 21 features done (100%) — every phase (0–3) is fully complete end-to-end. The build plan has no remaining features.**

> **Phase 0 exit passed 2026-07-22.** 0.2 and 0.3 cleared Feature Loop gate 2 with human sign-off at 900 / 768 / 480px. The remaining Definition of Done items (real data reads, writes, RBAC) do not apply to scaffolding — they are satisfied per feature by whichever Phase 1+ screen consumes these components. The database is live on **Neon**: the `init` migration is applied and seeded, and `GET /api/v1/health` reports `database: up`. Nothing is outstanding for Phase 0.

---

## How to Update This File

When you finish work on a feature:

1. Update its **Status** symbol and **Loop step**.
2. Fill in the detail fields that now apply (screens, components, endpoints, files).
3. Note anything unfinished under **Remaining** and any blocker under **Blockers**.
4. Add a line to the [Session Log](#session-log) at the bottom.
5. Recalculate the Summary Dashboard counts.
6. If new UI components were built, also update [ui-registry.md](ui-registry.md).

---

# Phase 0 — Foundation & Scaffolding

### 0.1 Repo & Tooling — ●

- **Loop step:** Done (no visual surface)
- **Scope:** Next.js + backend workspaces, TypeScript strict, ESLint/Prettier, Tailwind configured with GSP tokens, **test runner (Vitest/Jest + supertest)**.
- **Files:** `package.json` (npm workspaces `apps/web`, `apps/api`) · `.prettierrc.json` · `.prettierignore` · `.editorconfig` · `.gitignore` · `apps/web/{package.json,tsconfig.json,next.config.mjs,tailwind.config.ts,postcss.config.mjs,.eslintrc.json,vitest.config.ts,vitest.setup.ts}` · `apps/web/src/shared/design/tokens.ts` · `apps/web/src/shared/utils/cn.ts` · `apps/api/{package.json,tsconfig.json,tsconfig.build.json,.eslintrc.json,vitest.config.ts}`
- **Verified:** `npm run typecheck`, `npm run lint`, `npm run test`, `npm run build` all pass in both workspaces.
- **Remaining:** None
- **Notes:** Test runner is **Vitest** in both workspaces (+ `supertest` for API routes, jsdom + Testing Library for the web). Tailwind **v3.4** with a TypeScript config, so `tokens.ts` is the single palette source imported by `tailwind.config.ts`. Path alias `@/*` is used in the web app only — the API uses relative imports, because `tsc` does not rewrite path aliases in emitted JS.

### 0.2 Design System Port — ●

- **Loop step:** 2 passed — **visual sign-off 2026-07-22**. Steps 3–6 are N/A for a component library; each consuming feature carries them.
- **Scope:** Tokens → `tailwind.config` (**with §9 contrast fixes — no `#aaa` text, no white-on-gold**); base components (Button, Card, Badge, Modal, Toast, Alert, form inputs) from [ui-registry.md](ui-registry.md) Foundations/Buttons/Feedback; **plus §9 components absent from the prototype** — `EmptyState`, skeletons, `ErrorState`, `Pagination`, `ConfirmDialog`, `FormField`.
- **Verification:** ✅ Component gallery at **`/gallery`** renders every base component in all variants **and states** (loading/empty/error/disabled). Confirmed responsive at 900 / 768 / 480px.
- **Components built (20):** `Button` · `Card`/`CardHeader` · `Badge` · `Alert` · `Modal` · `ConfirmDialog` · `ToastProvider`/`useToast` · `FormField` · `Input`/`PasswordInput` · `Select` · `Textarea` · `ToggleSwitch` · `SearchInput` · `EmptyState` · `ErrorState` · `Skeleton`/`TableSkeleton`/`CardSkeleton`/`ChartSkeleton` · `Pagination` · `Table` primitives (`TableWrapper`, `Table`, `TableHead`, `TableBody`, `TableRow`, `TableHeaderCell`, `TableCell`, `TableAvatar`)
- **Files:** `apps/web/src/shared/components/ui/*` · `apps/web/src/shared/components/icons.ts` · `apps/web/src/shared/hooks/{use-focus-trap,use-body-scroll-lock,use-media-query}.ts` · `apps/web/src/app/globals.css` · `apps/web/src/app/(app)/gallery/*`
- **Remaining:** None. Components not in 0.2 scope stay `planned` in [ui-registry.md](ui-registry.md) §9 — `SortableTableHeader`, `Combobox` and `DatePicker` are all needed by 1.3.
- **Notes:** Contrast fixes applied — `.btn-gold` now uses `#8a7500` (4.54:1 with white) instead of `--gold` (2.30:1), and every muted text token is `--gray` `#6c757d` (4.76:1); `#aaa` is not ported anywhere. `Table` was built alongside the §9 list because `TableSkeleton`, `EmptyState` and `Pagination` cannot be verified without it. `StatCard`, charts and the calendar stay `prototype` — they belong to feature 1.5.

### 0.3 App Shell — ●

- **Loop step:** 2 passed — **visual sign-off 2026-07-22**. Role-filtered nav and the authenticated user block are feature 1.2, not unfinished 0.3 work.
- **Scope:** Sidebar, topbar, page container, responsive off-canvas sidebar.
- **Verification:** ✅ Navigable shell with placeholder pages, confirmed at 900px, 768px & 480px. Working mobile menu toggle + backdrop verified — the prototype's is broken ([ui-rules.md](ui-rules.md) §5).
- **Components built:** `AppShell` · `Sidebar` · `Topbar` · `PageHeader` · `PlaceholderPage` · `NAV_SECTIONS`
- **Files:** `apps/web/src/shared/components/layout/*` · `apps/web/src/app/layout.tsx` · `apps/web/src/app/page.tsx` · `apps/web/src/app/(app)/layout.tsx` · 12 placeholder routes under `apps/web/src/app/(app)/`
- **Routes:** `/dashboard` `/organizations` `/members` `/approvals` `/events` `/attendance` `/badges` `/finance` `/reports` `/analytics` `/profile` `/settings` `/gallery`
- **Remaining:** None for 0.3. The placeholder pages are disposable — each is replaced wholesale by its feature's real screen, never built on.
- **Notes:** All four prototype mobile-nav defects fixed — the toggle is visible below `md`, an interactive backdrop closes the panel, body scroll locks while open, and focus is trapped and restored. Escape closes it; navigating closes it; crossing the breakpoint closes it. Landmarks are real `nav`/`header`/`main` plus a skip link.
- **Revision — remove the double/triple page header + topbar search (2026-07-25):** User flagged that every page stacked 2–3 headings repeating the same title: the topbar's `<h1>` (`Topbar`, driven by `resolvePageTitle`), each page's `<PageHeader>` `<h2>` (title + description, always matching the topbar text), and often the main feature view's own `<CardHeader>` `<h3>` too. Confirmed via a full route audit — no documented rule required literal text duplication, only heading-*level* nesting ([ui-rules.md](ui-rules.md) §9. User chose to keep the topbar `<h1>` as each page's one header. Changes: **`Topbar`** — removed the disabled "Search the portal" input (`search-input` import + JSX), never wired to anything since 0.2/0.3. **`PageHeader`** — removed from all 15 real feature `page.tsx` files (dashboard, organizations, members, approvals, events, attendance, activity-reports, badges, announcements, finance, reports, analytics, profile, settings, notifications); each now returns its feature view directly with no wrapping fragment. Left untouched on `/gallery` (QA-only tooling, not a real app page) and unused in the already-dead `PlaceholderPage` (no live routes reference it). **`CardHeader`** (`card.tsx`) — `title` is now optional; omitting it renders no heading line, only `subtitle`/`actions`. Used to drop the 3 literal exact-duplicate CardHeader titles found by direct inspection (the initial audit only caught 2 of these — always verify a subagent's audit against the actual files before trusting it as complete): `MemberDirectory` ("Membership Registry" → for Troop Leader, title is now just their own-troop `Badge`; for Admin/EC, no title), `ActivityReportsView`'s admin/EC branch ("Activity Reports" → no title; Troop Leader's "My Activity Reports" is kept, since it isn't an exact match), `AnnouncementsView` ("Announcements" → no title). Explicitly checked and left alone: `NotificationsView`'s "Notifications" title — `/notifications` isn't in `nav-items.ts` at all, so its topbar actually shows the generic "GSP Portal" fallback, not "Notifications"; removing it would have left that page with no header at all, which would've been a regression, not a fix. Multi-card/tabbed pages (Finance, Analytics, Badges, Settings, Organizations' 5 tabs, Dashboard's role-specific cards) were left untouched — their `CardHeader` titles are legitimately distinct section labels (e.g. "Payments", "Councils"), not duplicates. **Known, deliberately-scoped gap**: didn't do a full heading-level renumbering pass (bumping top-level `CardHeader`s from the default `h3` to `h2` now that `PageHeader`'s `h2` is gone) — most single-card pages now skip from the topbar's `h1` straight to an `h3`, which is a mild, pre-existing-pattern a11y nit, not what was asked (visual de-duplication) or introduced by this change (organizations' tabs, previously forced to `h2`, actually got *more* correct — no more colliding with `PageHeader`'s own `h2`). Noted in [ui-rules.md](ui-rules.md) §9 as a follow-up. 215 tests still pass unchanged (this is presentational only, no service/schema changes), typecheck/lint clean both workspaces. Live-verified against the real dev servers + seeded Neon DB across both roles: Members (Admin — no card title, just filters; Troop Leader — own-troop badge only), Organizations (tabs' `h2` labels legitimately distinct, no collision), Dashboard (Troop Leader — role cards' own labels distinct), Activity Reports (Admin — no title; Troop Leader — "My Activity Reports" kept), Announcements (no title), and a member detail page (`/members/[id]`, unaffected — those routes never rendered `PageHeader` to begin with). Confirmed no topbar search bar anywhere. Responsive re-confirmed at 480/900/1280px on Members. One transient `__webpack_modules__[moduleId] is not a function` dev-server error surfaced on the first post-edit page load — a stale Next.js HMR cache artifact from the large number of file edits in one dev-server session, not a real code issue; resolved itself on the next navigation, confirmed by clean `tsc`/`eslint` runs throughout.

### 0.4 Backend Spine — ●

- **Loop step:** Done (no visual surface)
- **Scope:** `app.ts`/`server.ts`, singleton `asyncHandler`, centralized `errorHandler`, Prisma client, health route, `/api/v1` versioning, **`.env` + typed env-schema validation** (`DATABASE_URL`, `JWT_SECRET`, `JWT_REFRESH_SECRET`, `COOKIE_SECRET`).
- **Endpoints:** `GET /api/v1/health` — **live**. Returns 200 `{success,data}` when the DB answers, 503 with `status: "degraded"` when it does not.
- **Files:** `apps/api/src/{app.ts,server.ts}` · `apps/api/src/config/{env.ts,prisma.ts}` · `apps/api/src/shared/handlers/{async-handler.ts,error-handler.ts}` · `apps/api/src/shared/utils/{api-error.ts,api-response.ts}` · `apps/api/src/shared/constants/roles.ts` · `apps/api/src/routes/v1.ts` · `apps/api/src/modules/health/*` · `apps/api/.env.example` · `apps/api/tests/*`
- **Verified:** 8 passing tests (health service unit tests, health route via supertest, 404 envelope, `ApiError`/`ZodError`/unexpected-error formatting). Booted and hit live: health → 503 degraded with no DB, unknown route → 404 envelope. Env validation fails fast with a named list of missing keys.
- **Remaining:** None
- **Notes:** Response envelope is `{success,data,meta?}` / `{success,error:{code,message,details?}}` — every module uses it. Filenames are kebab-case (`async-handler.ts`) per [code-standards.md](code-standards.md) §4, not the camelCase shown in [architecture.md](architecture.md) §4.1. `.env` is loaded relative to the workspace root, so cwd does not matter.

### 0.5 Database Schema + Seed — ●

- **Loop step:** Done (no visual surface)
- **Scope:** Prisma schema for all domains in [database-design.md](database-design.md); migrations; seed with realistic demo data (councils, troops, members, events, badges) **plus the 3 roles and their permissions**.
- **Models done (33):** User · Role · Permission · UserRole · RolePermission · RefreshToken · AuditLog · Council · Troop · ScoutLevel · BadgeCategory · ActivityCategory · MemberStatus · Member · MemberProfile · Membership · Event · EventRegistration · AttendanceRecord · AttendanceSummary · ActivityReport · Badge · BadgeRequirement · MemberBadge · AchievementRecord · FeeType · Payment · Expense · FinancialPeriod · FinancialSummary · Report · ReportTemplate · AnalyticsSnapshot · Notification · AnnouncementPost · SystemSetting
- **Files:** `apps/api/prisma/schema.prisma` · `apps/api/prisma/seed.ts` · `apps/api/prisma.config.ts` · `apps/api/prisma/migrations/20260722142556_init/`
- **Verified:** ✅ Migration `20260722142556_init` created and applied to **Neon PostgreSQL** — 37 tables live. Seed ran clean and was re-run to confirm idempotency (identical counts): 4 users · 3 roles · 26 permissions · 57 role-permission links · 1 council · 3 troops · 10 members (7 scouts + 3 adult leaders) · 10 memberships · 6 badges · 7 member badges · 4 events · 24 registrations · 12 attendance records · 5 payments · 4 settings. The one-role-per-user invariant holds. `GET /api/v1/health` now returns **200 `status: ok`, `database: up`**.
- **Remaining:** None.
- **Notes:** Seed data becomes the "real" data that replaces feature mocks at Loop step 4. Role model is relational (`user_roles` + `role_permissions`) — no role column on `users`; seeds `admin`, `executive_council`, `troop_leader` with 26 permissions wired through `role_permissions`. Seed is idempotent (upsert-keyed) and covers 4 users, 1 council, 3 troops, 10 members (6 scouts + 3 adult leaders + mixed statuses), 6 badges, 4 events with attendance, payments, expenses and settings. Demo password comes from `SEED_PASSWORD`. Beyond the design doc, the schema adds: `MemberType` (scout vs adult leader, feature 1.3), member approval columns (`reviewedById`/`reviewedAt`/`rejectionReason`, feature 1.4), `ActivityReport` (feature 2.3), and `ScoutLevel` on `Member` — all four are required by features in the build plan that the original table list did not cover.
- **Prisma 7 (upgraded 2026-07-22, exact-pinned `7.9.0`):** the connection URL is **not** in `schema.prisma`. Migrate reads it from `prisma.config.ts`; the runtime client is constructed with the **node-postgres driver adapter** (`@prisma/adapter-pg`) in `src/config/prisma.ts`. Anything that needs a client must import that singleton — `new PrismaClient()` with no adapter throws on v7. The `package.json#prisma` seed key was removed; the seed command now lives in `prisma.config.ts` under `migrations.seed`. Verified after the upgrade: schema valid, client generates, `migrate status` clean, `migrate diff` reports **no drift** against the v6-built database, seed re-runs green, health returns `database: up`.

---

## Visual sign-off record — 2026-07-22

**0.2 and 0.3 passed Feature Loop gate 2.** Human-verified: the gallery and the shell render correctly and are responsive at **900 / 768 / 480px**.

This sign-off approved five deliberate deviations from the prototype. They are now the house standard — apply them to every Phase 1+ screen without re-asking:

1. **Mobile-first breakpoints.** The prototype is desktop-first with `max-width` queries; the port inverts to Tailwind's `min-width` default with a custom `lg2: 900px` stop. Same three breakpoints, opposite direction. Recorded in [ui-rules.md](ui-rules.md) §5 and [ui-registry.md](ui-registry.md). **Never write `max-*` variants.**
2. **Gold buttons are `#8a7500`, not `--gold`.** White on `--gold` measures 2.30:1. Decorative gold is unchanged for borders, fills and chart segments.
3. **Emoji → `react-icons`.** Icons are re-exported under semantic names from `@/shared/components/icons` so the set stays swappable in one place. Never import from `react-icons/*` in a feature.
4. **Tables carry a `<caption>`** (visually hidden by default) and real `<th scope="col">`.
5. **Buttons, not divs.** Every interactive element is a real control; the prototype's 89 inline `onclick` handlers on `<div>`s are not reproduced.

Not covered by this sign-off, and still to be confirmed per feature as screens are built: keyboard-only walkthroughs of real forms, and a screen-reader pass. The primitives support both (focus trap, `aria-live`, labelled controls) but no audit has been run against a real feature screen.

---

# Phase 1 — Core Operations

### 1.1 Authentication — ●

- **Loop step:** Done (steps 1–6 complete). Visual sign-off (step 2) passed 2026-07-23; Contract (step 3) same day; Wire Read (step 4, login+refresh) same day; Wire Write (step 5, signup+logout+forgot-password) same day; service-layer tests + manual failure-path walk (step 6) same day. **Follow-up (2026-07-24):** the "Known gap" below is now resolved — `forgot-password` sends a real email and a new `/reset-password` page + endpoint complete the loop. Ran through the full Feature Loop again for just that slice: mock UI → human visual sign-off → contract → wire read/write → test & done.
- **Screens:** `/login` — auth card with Log In / Sign Up tabs (`role="tablist"`), 3-way role selector (Administrator / Executive Council / Troop Leader) shared by both tabs, password strength meter on signup, forgot-password modal. `/reset-password` (2026-07-24) — same gradient chrome, 3 states (set-new-password form / invalid-or-expired link / success), reached via the link the forgot-password email now sends.
- **Components:** `AuthCard`, `LoginForm`, `SignupForm`, `RoleSelector`, `PasswordStrengthMeter`, `ForgotPasswordModal`, `ResetPasswordCard` (2026-07-24) — all reuse existing `Alert`/`Button`/`FormField`/`Input`/`PasswordInput`/`Select`/`Modal`/`Toast` from the kit, no base primitives duplicated
- **Endpoints:** **All 6 live** — `POST /api/v1/auth/login|refresh|signup|logout|forgot-password|reset-password`, all public by design (auth issues sessions, so none of its own routes can require one — `reset-password`'s token *is* the credential). Real DB, real JWTs, refresh rotation, transactional user+role creation, best-effort revoke on logout, non-enumerating forgot-password response that now actually emails a single-use, 1-hour link; resetting revokes every refresh token on the account.
- **Files:** `apps/web/src/app/(auth)/{login,reset-password}/page.tsx` · `apps/web/src/features/auth/types.ts` · `apps/web/src/features/auth/constants.ts` · `apps/web/src/features/auth/services/auth.service.ts` · `apps/web/src/features/auth/components/{auth-card,login-form,signup-form,role-selector,password-strength-meter,forgot-password-modal,reset-password-card}.tsx` · `apps/web/src/shared/mocks/auth.mock.ts` · `apps/web/src/app/api/auth/{login,refresh,signup,logout,forgot-password,reset-password}/route.ts` · `apps/web/src/middleware.ts` · `apps/web/src/config/env.ts` · `apps/web/src/shared/utils/jwt.ts` · `apps/web/.env.example` · `apps/api/src/modules/auth/{auth.types,auth.schema,auth.repository,auth.service,auth.middleware,auth.controller,auth.routes,index}.ts` · `apps/api/src/shared/utils/{jwt,password,email}.ts` · `apps/api/src/config/env.ts` · `apps/api/.env.example` · `apps/api/src/routes/v1.ts` · `apps/api/tests/auth.test.ts` · `apps/api/prisma/schema.prisma` (`PasswordResetToken`) · `apps/api/prisma/migrations/20260724134505_add_password_reset_tokens/`
- **Verified so far:** `npm run typecheck`/`npm run lint`/`npm test` all clean in both workspaces (198 API tests passing, 19 of them `auth.test.ts` — including 6 new forgotPassword/resetPassword cases covering email sent-on-success, no email for unknown/deactivated accounts (no enumeration), delivery failure still returns the generic message, valid-token reset revokes every session, invalid/expired/reused token → 400). Playwright-driven browser tests against the real dev servers + seeded Neon DB: admin login → real `fullName` in the success toast → redirect to `/dashboard`; bad password → real "Invalid email or password." alert; signup (executive council role) → real "Account created for …" toast → auto-login redirect to `/dashboard`; re-submitting the same signup email → real "An account with this email already exists." `Alert`; forgot-password → real generic "If that email is registered…" toast; `document.cookie` confirmed empty after both login and signup (all tokens genuinely httpOnly); signed-in visitor hitting `/login` bounces to `/dashboard`; signed-out visitor hitting `/members` bounces to `/login`. Backend also curl-verified directly: refresh rotates the token and the old one is rejected on reuse; logout revokes the presented refresh token and a subsequent `/refresh` with it is rejected; admin signup rejects a wrong `adminSecretKey` with 403.
- **Reset-password follow-up, verified live end-to-end (2026-07-24):** submitted the real forgot-password modal for a seeded Troop Leader account, read the real link out of the API's dev-mode email log (SMTP unset in this environment, so `emailService` logs the composed email via nodemailer's `jsonTransport` instead of sending — verified this is intentional dev behavior, not a bug), opened that exact link in the browser, set a new password, saw the success screen, and signed in with the new password. Confirmed the old password no longer works and the same token cannot be reused a second time (single-use, 400 on replay). An unknown token (garbage or expired) surfaces the API's own "This reset link is invalid or has expired." message as an inline `Alert` on the form rather than a full-screen swap. Reverted the test account's password back to the shared seed default (`GspDemo!2026`) afterward via the same real flow, leaving no lingering state. `WEB_APP_URL`/`EMAIL_FROM`/`SMTP_*` added to `env.ts` (all but `EMAIL_FROM` optional, so dev/test never need real mail credentials) and `.env.example`. `/reset-password` added to the web `middleware.ts` public-path allowlist (same treatment as `/login`).
- **Deliberate deviations from the prototype — flag at visual verify:**
  1. Forgot-password is a `Modal` form, not the prototype's `prompt()`/`alert()` pair (those block the thread and bypass the app's focus-trap/`aria-live` machinery).
  2. Signup's password-mismatch and "8 char minimum" errors render as field-level `FormField` errors, not a single top banner — the top `Alert` is reserved for login's non-field-specific "invalid credentials" case.
  3. Troop leader signup's second dropdown is relabeled "Primary Scout Level" and populated from the real seeded `scout_levels` names (Twinkler/Star Scout/Junior Girl Scout/Senior Girl Scout/Cadet Girl Scout) — the prototype's "Troop Type" list had a stray "Tagalog" entry and a duplicated "Cadet".
  4. Demo-fill credentials are the **real seeded accounts** (`admin@gsp-catanduanes.ph`, `council@gsp-catanduanes.ph`, `leader.virac@gsp-catanduanes.ph`, password `GspDemo!2026`) so the button keeps working once step 4 wires the real endpoint.
  5. Header trefoil uses the real logo (`context/img/logo.jpg`, now `apps/web/public/logo.jpg`) via `next/image`, not the prototype's `<img src="img/img1.jpg">` placeholder.
- **Round 1 review fixes (2026-07-22):** header `<h1>` was rendering dark, not white — the global `h1,h2,h3,h4 { color: ink }` base rule in `globals.css` wins over an inherited color from a colored wrapper, so any heading on a gradient surface needs its color set explicitly (fixed locally in `auth-card.tsx`; not a globals.css change). Swapped the placeholder monogram for the real logo. Added the required-field validation that was missing on Sign Up's First/Last Name and Email — `noValidate` was suppressing native browser validation and nothing had replaced it for those three fields (only password/confirm had custom checks), so submitting with them empty showed no feedback at all; this is still client-only, the real Zod schema lands at step 3.
- **Round 2 review fix (2026-07-23):** same gap on Log In — email/password had no required-field errors either, so an empty submit fell straight through to the generic "Invalid email or password" alert instead of pointing at the empty fields. `handleSubmit` now checks both are non-empty and shows a field-level "required" error before it ever compares against the demo account.
- **Tooling change (2026-07-23):** installed the **Playwright MCP server** (`.mcp.json` at repo root, pre-approved via `enableAllProjectMcpServers: true` in the gitignored `.claude/settings.local.json`) so the agent can drive a real browser instead of curl-only smoke checks. Active as of this session.
- **Visual sign-off (2026-07-23):** Human confirmed `/login` — UI, responsiveness (900/768/480px), and validation states all good. **Feature Loop gate 2 passed.**
- **Contract (2026-07-23):** Backend `auth.schema.ts` (Zod: `loginSchema`, `signupSchema` as a `role`-discriminated union for the three account types, `forgotPasswordSchema`, `refreshSchema`, `logoutSchema`) + `auth.types.ts` (`AuthUser`, `AuthTokens`, request/response bodies). Frontend `features/auth/types.ts` mirrors it: `AuthUser`, `LoginRequest`, `SignupRequest`, `ForgotPasswordRequest`, and the browser-facing `LoginResponse`/`SignupResponse` (`{ user }` only, no `tokens`).
- **Wire Read (2026-07-23):** Backend — `auth.repository.ts` (Prisma: find user + role join, refresh-token CRUD), `auth.service.ts` (bcrypt verify, JWT issue via `shared/utils/jwt.ts`, one-role-per-user enforced with `ApiError.internal` as a defensive check, refresh-token rotation via a `sha256` hash of the token stored in `refresh_tokens`), `auth.middleware.ts` (`requireAuth`/`requireRole` — built but not yet applied anywhere; nothing needs protecting until 1.2+), thin `auth.controller.ts`, `auth.routes.ts` mounting `login`/`refresh` only. Mounted at `/api/v1/auth` in `routes/v1.ts`.
  Frontend — `features/auth/services/auth.service.ts` (calls the BFF, never the Express API directly), two BFF Route Handlers (`app/api/auth/login`, `app/api/auth/refresh`) that call the real API server-to-server and translate `tokens` into httpOnly cookies (`access_token`/`refresh_token`, `sameSite: lax`, `maxAge` sized from each JWT's own `exp` claim — no separate expiry-parsing logic needed on the web side). `middleware.ts` gates the whole app on cookie *presence* (not signature/expiry — real enforcement is `requireAuth` API-side once something needs protecting); redirects signed-out visitors to `/login` and signed-in visitors away from it. `LoginForm` now calls the real service instead of comparing against `DEMO_ACCOUNTS`; `fillDemo()` is unchanged (still just fills the inputs with real seeded credentials, which now go through a real request). Removed `DEMO_USERS` from `auth.mock.ts` — added at the Contract step, superseded before it was ever consumed, so it would have shipped as dead code.
  Added `jsonwebtoken` to `apps/api` (pre-existing `bcryptjs` covers hashing). No new frontend dependency — env config and JWT-payload decoding are both small enough to hand-write rather than pull in `zod`/`jose` for the web workspace.
- **Wire Write (2026-07-23):** Backend — `auth.repository.ts` gained `findRoleByName` and `createUserWithRole` (a single nested Prisma write: `users` + its one `user_roles` row, atomic by construction — never one without the other). `auth.service.ts` gained `signup` (duplicate-email → 409, admin role gated by a new `ADMIN_SIGNUP_KEY` env secret compared server-side → 403 on mismatch, then transactional create + immediate token issue so signup auto-logs in exactly like login), `logout` (hashes the presented refresh token, revokes it if an active row is found, but **always** returns a generic success message — a client can always clear its cookies even if the token was already gone), `forgotPassword` (returns one fixed message regardless of whether the email exists — no user-enumeration surface, no DB lookup needed since the response never varies). `auth.controller.ts`/`auth.routes.ts` now expose all three; all five auth routes stay public by design.
  Frontend — three new BFF routes (`app/api/auth/{signup,logout,forgot-password}`) following the established pattern: `signup` sets cookies from the returned tokens exactly like `login`; `logout` reads its own refresh cookie, best-effort-forwards it to the API (swallowing network errors), and unconditionally clears both cookies; `forgot-password` is a plain proxy. `features/auth/services/auth.service.ts` gained matching `signup`/`logout`/`forgotPassword` functions. `SignupForm` gained controlled state + required-field validation for every role-specific field (council name/region/code, troop number/scout level/home council, employee ID/admin secret key — previously these were uncontrolled placeholder-only inputs left over from the mock step) and now calls the real service, showing a toast + redirecting to `/dashboard` on success or a top `Alert` on failure (duplicate email, wrong admin key), mirroring `LoginForm`. `ForgotPasswordModal` now calls the real service instead of a `setTimeout`; because the backend response is deliberately identical whether or not the email exists, a thrown error there only ever means a genuine network/server fault, shown as a generic error toast.
  **Scope boundary — documented, not a gap:** `signup`'s role-specific affiliation fields (troop number, home council, council name/region/code, employee ID) are validated by `signupSchema` but have no relational home yet — linking a troop leader to a real `Troop` row or an executive to a real `Council` row is an admin action that belongs to feature 1.6 (Organization Management), not something self-signup can assert. Only the account itself (`users` + one `user_roles` row) is persisted; see the comment on `authService.signup`.
  The Sidebar's "Sign out" button (`shared/components/layout/sidebar.tsx`) stays disabled — showing the real signed-in user and wiring that click is 1.2's "user footer block, logout" scope, not 1.1's. The frontend `logout()` service function is ready for 1.2 to call.
- **Test & Done (2026-07-23):** Added `apps/api/tests/auth.test.ts` — 11 tests covering `login` (success, unknown email, deactivated account, wrong password), `signup` (success with exactly one role assigned, duplicate email, admin with wrong/correct secret key), `logout` (revokes an active token, still succeeds on an unknown/already-revoked one), and `forgotPassword`. Repository calls are mocked via `vi.spyOn`; password hashing and JWT signing run for real (pure functions, no I/O) so the tests exercise real bcrypt/JWT behavior. `ADMIN_SIGNUP_KEY` added to `tests/setup.ts`. RBAC middleware (`requireAuth`/`requireRole`) remains built but genuinely inapplicable here — none of auth's own 5 routes can require a session (they're what creates one); it stays ready for 1.2, the first feature with a route to protect.
- **Not built (deliberately out of scope):** a client-side silent-refresh interceptor that calls `/api/auth/refresh` automatically on a 401 — nothing authenticated exists yet to trigger it; the endpoint is ready and manually verified, the interceptor is deferred to whichever feature first needs authenticated fetches.
- **Notes:** BFF issues httpOnly cookies; API stays stateless ([project-overview.md](project-overview.md) → Authentication). Role state is shared between the Login and Sign Up tabs (switching tabs keeps the selected role), matching the prototype's single `currentRole` variable.
- **Revision — real reset-password email delivery + branded template (2026-07-26):** user reported never receiving reset-password/announcement emails; investigation confirmed this was intentional dev behavior, not a bug — `SMTP_HOST` was unset in `apps/api/.env`, so `email.ts` was falling back to nodemailer's `jsonTransport` (logs instead of sends), exactly as documented at 1.1's 2026-07-24 follow-up; separately confirmed announcements were never emails at all, only in-app `Notification` rows (`shared/utils/notify.ts`), so nothing to fix there. Walked the user through two provider attempts before landing on a working one: Gmail app passwords blocked twice ("setting not available for your account" — 2FA/Workspace-policy issue on the first account), then Mailtrap's HTTP API worked but its demo domain (`hello@demomailtrap.co`) only delivers to the exact email the Mailtrap account itself was signed up with — a real constraint, not a bug, confirmed live via a genuine `403 MailtrapError` when tested against a different registered user's email. Settled on real Gmail SMTP with a second personal account (ordinary app password, no domain restriction) after also diagnosing and fixing a `self-signed certificate in certificate chain` TLS error — traced to Avast's **Mail Shield** (a separate component from Web Shield's HTTPS scanning) intercepting the SMTP connection; disabling it in Avast's Core Shields settings resolved it, confirmed local-only (won't recur on any real deploy target, which has no antivirus in the network path). Added `mailtrap` (`^4.8.0`) to `apps/api`'s dependencies and a new optional `MAILTRAP_API_TOKEN` env var (`env.ts`, `.env.example`) — `email.ts`'s `send()` now checks three tiers in order: Mailtrap's HTTP API if `MAILTRAP_API_TOKEN` is set, else real SMTP if `SMTP_HOST` is set, else the original `jsonTransport` dev fallback; `parseFromAddress()` splits `EMAIL_FROM`'s `Name <email>` shape for Mailtrap's separate `name`/`email` fields. Never printed the pasted Mailtrap token or Gmail app password back into any file except the gitignored `.env` (confirmed via `git check-ignore`) or into any assistant response. Separately, gave the reset-password email real branding: added `apps/api/src/shared/utils/email-templates.ts` (`renderEmailLayout`/`renderEmailButton`/`renderEmailParagraph`/`renderEmailFallbackLink`) — a table-based, fully inline-styled HTML shell (green gradient header banner, gold accent divider, gold CTA button, muted footer) matching the app's palette (colors copied from `tokens.ts` rather than imported, since this bundles into the API not the web app); table+inline-style layout specifically for Outlook desktop compatibility, which ignores most modern CSS. `sendPasswordResetEmail` now composes through this layout instead of raw inline `<p>` tags. Verified live: triggered real `POST /forgot-password` against a seeded/real account after each provider swap, confirmed delivery (or the expected provider-specific failure) by reading the actual server-console error and, once Gmail SMTP was working, by checking the real inbox for the styled email. `npm run typecheck`/`lint`/`test` clean in both workspaces throughout (223 API tests unaffected — `auth.test.ts` mocks `emailService`, so the template rewrite needed no test changes). No feature's Loop step changes — this is a bug-fix/revision to an already-done (●) feature.

- **Revision (`revise-feature`) — self-signup security gate for Executive Council/Troop Leader (2026-07-26):** User asked whether anyone could self-signup and get access/modify data. Audit of the shipped `authService.signup` found yes: Executive Council and Troop Leader signup was fully open — no verification of the council/troop name typed into the form, `isActive` defaulting `true`, tokens issued immediately, so anyone could grant themselves live approval/finance/reporting access in one request. Admin signup was already gated by the shared `ADMIN_SIGNUP_KEY` secret. User chose the fix from a set of options: keep self-signup, but land new Executive Council/Troop Leader accounts `isActive: false` (no tokens issued) until an Administrator approves them — reusing 3.4's existing Users & Access activate/deactivate action rather than building a new approval screen, since that panel already has the exact Active/Inactive badge + Activate/Deactivate button needed. Admin signup keeps its original behavior unchanged (secret key remains its gate). Backend: `authService.signup` (`apps/api/src/modules/auth/auth.service.ts`) now branches on `requiresApproval = role is executive_council or troop_leader`, passing `isActive: !requiresApproval` to a widened `authRepository.createUserWithRole`; pending signups get no token pair and write a new `user.signup_pending` audit log entry (actor = the new user's own id, matching the convention every other user-lifecycle event already follows). `auth.types.ts`'s `SignupResponseBody` became a discriminated union (`{status:'active',user,tokens}` / `{status:'pending',message}`) instead of always including tokens. Login was deliberately left untouched — a pending account still gets the same generic "Invalid email or password." as a wrong password or a deactivated account (checked only after password verification), preserving the existing no-enumeration convention rather than adding a new information-disclosure path; the pending message is instead shown once, at signup time, when the caller has already proven they created the account. Frontend: mirrored the union in `features/auth/types.ts`, updated the signup BFF route (`app/api/auth/signup/route.ts`) to only mint cookies on `status: 'active'`, and `SignupForm` now shows the pending message as an info toast and returns to the Log In tab instead of redirecting to `/dashboard`. Updated `auth.test.ts`: the old "creates a user with exactly one role and returns tokens" test (troop_leader) no longer matched the new contract, so it was replaced with a pending-specific assertion (`isActive:false` passed to the repository, `status:'pending'` returned, no `tokens` key, audit log written); added a matching Executive Council pending test and a login-rejects-pending-account test; the admin-signup test was updated for the new `{status:'active',...}` shape (behavior unchanged, all existing admin assertions still hold). `npm run typecheck`/`lint`/`test` clean in both workspaces (225 API + 5 web tests, up from 221 API). **Live-verified end-to-end** against the real dev servers + seeded Neon DB via Playwright: signed up a real new Executive Council account → toast read "Account created. An administrator must approve your account before you can sign in." and landed back on Log In (no cookies set, confirmed by no dashboard redirect); logging in with the exact correct password immediately after → generic "Invalid email or password."; signed in as the real seeded Admin, found the new account in Settings → Users & Access as **Executive Council / Inactive**, clicked Activate → confirm dialog → "User activated." toast, status flipped to Active; Audit Log tab (filtered to the account's name) showed a "Signup Pending" entry (the humanizer already renders `user.signup_pending` correctly with zero UI changes needed); signed out and logged back in with the same credentials → succeeded, real session, sidebar showed the real name/role. Deleted the test account afterward via the same real Delete action so seed data is unchanged.
- **Not built (deliberately out of scope, noted during this revision):** no separate "pending signups" queue/filter distinct from admin-deactivated accounts — both surface identically as `Inactive` in the existing Users & Access table. Acceptable at current user-count; revisit (e.g. a dedicated status enum) if the portal-users list grows enough that distinguishing "new, never-approved" from "was active, later deactivated" becomes operationally important.

### 1.2 App Shell + Role-Based Navigation — ●

- **Loop step:** Done (steps 1–6 complete). Visual sign-off (step 2) passed 2026-07-23 against mock session users for all 3 roles; Contract/Wire Read/Wire Write/Test & Done same day.
- **Screens:** Authenticated sidebar — role-filtered nav, real user footer block (avatar initials + name + role label), working Sign out. No new page; wires the existing Phase 0 shell.
- **Components:** `Sidebar`/`AppShell` extended with a required `user: AuthUser` prop and `onSignOut`/`isSigningOut`; `nav-items.ts` gained `getNavSectionsForRole(role)`; reused `TableAvatar` (restyled via `className`) for the footer avatar — no new base components.
- **Endpoints:** **`GET /api/v1/auth/me`** — new, protected by `requireAuth` (the first route in the codebase to use it). Returns the real signed-in user by decoding the access token's `sub` and looking up the current DB row (so a deactivated account is rejected even mid-session, not just at login).
- **Files:** `apps/web/src/shared/constants/roles.ts` (new — hand-synced mirror of `apps/api/src/shared/constants/roles.ts`'s permissions, plus `ROLE_LABELS`, `ROUTE_PERMISSIONS`, `getRequiredPermissionForPath`) · `apps/web/src/shared/components/layout/{nav-items,sidebar,app-shell}.tsx` · `apps/web/src/app/(app)/layout.tsx` · `apps/web/src/middleware.ts` · `apps/web/src/features/auth/services/session.service.ts` (new) · `apps/api/src/modules/auth/{auth.repository,auth.service,auth.controller,auth.routes,auth.types,index}.ts` · `apps/api/tests/auth.test.ts`
- **Verified:** `npm run typecheck`/`lint`/`test` clean in both workspaces (API: 22 tests, 3 new for `getCurrentUser` — happy path, deleted-user, deactivated-account). Playwright against the real dev servers + seeded Neon DB, for **all three real seeded accounts** (not mocks): each shows its own real name/role in the footer, sees exactly its permitted nav items (verified Administrator sees everything; Executive Council loses Settings; Troop Leader loses Pending Approvals/Financial Tracking/Analytics/Settings, and the Administration section correctly collapses to just Report Generation instead of vanishing). Confirmed Sign out clears cookies and redirects to `/login` (a follow-up `/dashboard` request bounces back to `/login`). Confirmed the route guard: Troop Leader hitting `/finance`, `/approvals`, or `/analytics` directly bounces to `/dashboard`; Executive Council hitting `/settings` bounces likewise; Administrator reaches all of the above with no false block. Responsive re-confirmed at 900/768/480px with the real session (off-canvas panel, backdrop, footer all render correctly below `md`).
- **Done gate:** ✅ Each role sees only its permitted menu (verified live, not just in mock). ✅ Unauthorized routes blocked (edge middleware decodes the access token's `role` claim — unverified, UX-layer only — and redirects; real enforcement is each feature's own `requireRole`, applied per route as that feature's module is built).
- **Remaining:** None for 1.2 itself. The middleware's role decode is intentionally unverified (no `Buffer`/`jsonwebtoken` in the edge runtime) — it fails open on a malformed token, which is fine because it is UX-only; a genuinely invalid/expired token still gets rejected the moment it hits any `requireAuth` route, and 1.3+ each add their own `requireRole` gate at the API layer.
- **Notes:** Diagnosed a user-reported "Fill Demo always logs in as Administrator" — not a real bug. The browser's saved-password autofill (from earlier admin logins) was silently overwriting the login form's email/password fields with the admin credential on page load/after `fillDemo()`, independent of the app's own state; confirmed by watching `fillDemo()` correctly update the field to each role's real demo account when tested directly. No code change needed; worth remembering if it resurfaces during future visual/manual testing sessions — clear saved passwords or use a fresh browser profile when testing multi-account flows. `ROUTE_PERMISSIONS` (middleware) intentionally duplicates each `NavItem.permission` in `nav-items.ts` rather than importing it, so the edge bundle doesn't pull in `react-icons`; the two are hand-synced, same convention as `AuthRoleId` mirrored between workspaces.

### 1.3 Membership Management — ●

- **Loop step:** Done (steps 1–6 complete). Visual sign-off (step 2) passed 2026-07-23; Contract, Wire Read, Wire Write, Test & Done all same day.
- **Screens:** `/members` — Membership Registry directory (debounced search, status + type filters, paginated table, register/edit/archive/restore actions) · `/members/[id]` — member profile (personal info, emergency contact, membership history table, edit/renew/archive/restore)
- **Components:** `MemberDirectory`, `MemberProfileView`, `MemberFormModal` (one modal, both registration types via a `memberType` field), `RenewMembershipModal`, `MemberStatusBadge` — all compose existing kit primitives (`Card`, `Table`, `Modal`, `ConfirmDialog`, `FormField`, `Select`, `Pagination`, `EmptyState`/`ErrorState`/`TableSkeleton`); no new base components
- **Endpoints:** **All 7 live** — `GET/POST /api/v1/members`, `GET/PUT /api/v1/members/:id`, `PATCH /api/v1/members/:id/archive|restore`, `POST /api/v1/members/:id/renew`, plus two minimal read-only lookups pulled forward from 1.6: `GET /api/v1/organizations/troops|scout-levels`. `members:read`/`members:write` gate all three roles; `members:archive` (archive/restore only) gates admin + executive council. Real Prisma reads/writes against Neon — search (name/email/troop code), status/type filters, and pagination all run server-side.
- **Files:** `apps/web/src/app/(app)/members/page.tsx` · `apps/web/src/app/(app)/members/[id]/page.tsx` · `apps/web/src/features/members/{types,constants}.ts` · `apps/web/src/features/members/components/{member-directory,member-profile-view,member-form-modal,renew-membership-modal,member-status-badge}.tsx` · `apps/web/src/features/members/services/members.service.ts` · `apps/web/src/shared/utils/api-proxy.ts` (new — shared BFF-to-API forwarder, cookie → `Authorization: Bearer`) · `apps/web/src/app/api/members/route.ts` · `apps/web/src/app/api/members/[id]/route.ts` · `apps/web/src/app/api/members/[id]/{archive,restore,renew}/route.ts` · `apps/web/src/app/api/organizations/{troops,scout-levels}/route.ts` · `apps/api/src/modules/members/{members.schema,members.types,members.repository,members.service,members.controller,members.routes,index}.ts` · `apps/api/src/modules/organizations/{organizations.types,organizations.repository,organizations.service,organizations.controller,organizations.routes,index}.ts` · `apps/api/src/routes/v1.ts` · `apps/api/tests/members.test.ts`
- **Verified:** `npm run typecheck`/`npm run lint`/`npm test` clean in both workspaces (31 API tests total, 9 new for `membersService`: list/getById/create/update/archive/restore/renew, happy + not-found + bad-troop paths, repository mocked via `vi.spyOn`). Playwright against the real dev servers + seeded Neon DB as Administrator: 10 seeded members render (types, troop codes, scout levels, status badges, pagination); registered a new scout (Josie Marasigan) — appeared in the directory as `Pending Review`, count went 10→11; opened her real profile, archived (status → Archived, Restore appeared), restored (status → Active), then renewed (new `memberships` row written, visible in history, status → Active); edit modal round-trips the full record (not just summary fields) via a real `GET /members/:id` before opening. Empty state (search for gibberish), error state (retry action), and loading skeleton all verified live. Responsive re-confirmed at 900/768/480px with real data. Backend curl-verified directly: unauthenticated `GET /members` → 401; Troop Leader `PATCH /members/:id/archive` → 403 `FORBIDDEN` (real RBAC enforcement, not just hidden UI); Admin `POST /members` with only `memberType` → 422 with per-field `VALIDATION_ERROR` details.
- **Done gate:** ✅ Both registration types work (Scout Level field conditionally required/shown). ✅ Renewal updates status visibly and appends history rather than mutating. ✅ Full CRUD + search/filter/archive working end-to-end against real data. ✅ Validation both sides (Zod backend incl. `memberType`-discriminated union; client-side required-field checks before submit).
- **Deliberate deviations from the prototype — flagged at visual verify:**
  1. One combined register/edit modal with a `memberType` selector instead of the prototype's scout-only "Register Scout" form — Adult Leader registration didn't exist in `Gsp.html` at all.
  2. Added status **and** type filters beyond the prototype's single status dropdown — needed once Scout and Adult Leader share one table.
  3. Native `<input type="date">` for birthdate/renewal (matches the prototype's own approach) instead of building the still-`planned` `DatePicker`.
  4. Native `Select` for the Troop field instead of a searchable `Combobox` — only 3 troops are seeded until 1.6 exists to grow the list.
  5. No column sorting (`SortableTableHeader` stays `planned`) — not required by this feature's Done gate.
- **Scope boundary — documented, not a gap:** `GET/organizations/troops|scout-levels` are minimal read-only lookups pulled forward from 1.6 (Organization Management) because the registration form needs a troop/scout-level picker before 1.6's own CRUD screens exist. Full council/troop CRUD, council management, and category config are still entirely 1.6's to build.
- **Notes:** Mutations refetch rather than patch local state (settled decision #3) — every create/update/archive/restore/renew re-runs the list or single-member fetch after a successful write. Editing from the directory fetches the full record (`GET /members/:id`) before opening the modal, since the row itself only carries the lighter `MemberSummary` shape (no birthdate/gender/emergency-contact/notes) — using the row data directly would have silently blanked those fields on save. New `shared/utils/api-proxy.ts` is the first generic BFF-proxy helper in the codebase (auth's routes translate tokens into cookies and can't reuse it) — every future feature's BFF routes should use it rather than hand-rolling the cookie-read/forward pattern again.
- **Bug fix — archive/restore lost the pre-archive status (2026-07-26):** User reported that archiving a member with any status other than `active`, then restoring, always landed back on `active` instead of the original status. Root cause: `membersService.restore` unconditionally set the status to `active` — there was nowhere the prior status was ever recorded. Fix: added `pre_archive_status_id` (nullable, self-referencing `MemberStatus`) to `Member` via a new migration (`20260726013133_add_member_pre_archive_status`, applied live to Neon), stashed on `archive()` and consumed + cleared on `restore()` (falls back to `active` only when nothing was stashed, e.g. members archived before this fix existed); `archive()` is now also a no-op if the member is already archived, so re-archiving can't clobber the stashed value. `MEMBER_STATUS_LABELS`-aware toast/dialog copy in `member-directory.tsx`/`member-profile-view.tsx` no longer hardcodes "restored to active". Files: `apps/api/prisma/schema.prisma`, `apps/api/prisma/migrations/20260726013133_add_member_pre_archive_status/`, `apps/api/src/modules/members/{members.repository,members.service}.ts`, `apps/api/tests/members.test.ts`, `apps/web/src/features/members/{types.ts,components/member-directory.tsx,components/member-profile-view.tsx}`. Verified: full test suite (218 tests) + typecheck clean both workspaces.
- **Bug fix — edit-form values reverting mid-edit (2026-07-26):** Same session, see 2.1's write-up for the root cause (a `useEffect` reset keyed on a parent-recreated object) — `member-form-modal.tsx` had the identical pattern and got the identical fix (reset only on the closed→open transition via a `wasOpen` ref), since Members' own edit form is one of the 8 affected here.
- **Revision — Troop Leader scoped to their own troop (2026-07-25):** User asked whether a Troop Leader should be able to edit another troop's member. Investigation found the answer was already "yes" by accident: `requireRole('admin','executive_council','troop_leader')` guarded every read/write route identically with zero ownership check in the service/repository layer, and `GET /members` had no troop filter at all — any Troop Leader could list, view, register into, edit, and renew *any* member org-wide, contradicting `project-overview.md`'s "manage their own troop" intent (the same "never trust client-supplied scope" principle behind settled decision #4). User chose full server-side scoping: list, read (`GET /:id`), create, update, and renew. Followed the existing `scopeToOwnTroop`/`requireMemberInScope` convention 2.4 (Badges) already established rather than inventing a new pattern — `membersRepository.findTroopIdsLedBy(userId)` mirrors `badgesRepository.findTroopIdsLedBy`, and a new `requireTroopIdInScope` throws 403 `FORBIDDEN` for admin/executive-council-never, troop_leader-only. `buildWhere` was restructured from a flat object to an `AND` array so the scope filter and client-supplied filters (including an explicit `troopId` query param) compose correctly instead of one overwriting the other — a Troop Leader filtering by a troop they don't lead now gets zero rows, not a leak. `getById`'s `user` param is optional by design: the controller's `GET /:id` route always passes it (scoped), but archive/restore/renew/approve/reject's internal trailing re-fetch (via `membersService.getById(id)`, no second arg) stays unscoped since it runs after an already-authorized mutation and archive/restore/approve/reject are role-gated away from Troop Leader entirely at the route level regardless. Web side: added `leaderId` to the frontend `TroopOption` type (the API already returned it; only the type was narrower) and a `restrictToTroopId` prop on `MemberFormModal` — for a Troop Leader it filters the Troop `<Select>` to their own troop, disables it, and pre-fills it on create, so the UI doesn't offer a choice that would only come back as a 403. `MemberDirectory` and `MemberProfileView` both compute this from `troopOptions.find(t => t.leaderId === currentUserId)`; `currentUserId` is threaded down from `getSession()` in both `page.tsx` files, only when the role is `troop_leader` (never a client-asserted role). No new base components; the directory's Edit button needed no extra hiding logic since a Troop Leader now simply never receives another troop's rows to begin with. 12 new tests added to `members.test.ts` (list/getById/create/update/renew × own-troop-allowed + other-troop-403, plus admin-unscoped and empty-troop-list cases) — 215 total tests (203 + 12) pass, typecheck/lint clean both workspaces. Live-verified against the real dev servers + seeded Neon DB: signed in as Liza Bagadiong (Troop Leader, Troop 12 — Virac) and confirmed the directory showed exactly her 6 real troop members and no others; confirmed the Register/Edit modal's Troop field was locked and pre-filled to `CAT-VIR-012`; a real edit to her own member (Josie Marasigan) round-tripped through the actual UI and was reverted; direct `fetch()` calls from her authenticated session against Grace Tapel's real record (a different troop) returned real `403 FORBIDDEN` on `GET`, `PUT`, and `POST /renew` — Grace's record was never reached by the repository write, since the scope check throws before it. Signed in as Administrator separately and confirmed all 13 members across every troop still show, unaffected.
- **Same-day follow-up — Troop column swapped for a header, Troop filter added for Admin/EC (2026-07-25):** With the directory now troop-scoped for Troop Leader, the per-row Troop column repeated the same value on every line. User asked to remove it for Troop Leader (replacing it with a page-level troop indicator) while keeping it — plus adding a Troop filter — for Admin/Executive Council. Confirmed isolated: only `page.tsx` imports `MemberDirectory`; `listMembers()` (the service function) is also called by `attendance-view.tsx`, but the new `troopId` param is optional and additive, so that call is unaffected. `CardHeader`'s `title` is now a `Badge` (`tone="blue"`, e.g. "CAT-VIR-012 — Troop 12 — Virac") appended next to "Membership Registry" when `isTroopLeader && ownTroop`, computed from the same `ownTroop` lookup the prior revision already built for the form-lock. For Admin/EC, a fourth `<Select>` ("Filter by troop") was added to the filter bar using the same `troopOptions` fetch already in place, wired through a new `troopFilter` state → `listMembers({ troopId })` → the web service's `ListMembersParams`/`buildQuery` (both gained an optional `troopId`, additive) → the already-scoped backend (`listMembersQuerySchema`/`buildWhere` already supported and AND-combined a client `troopId` with server-side scope from the prior revision — verified no backend change was needed, not assumed). The Troop `<TableHeaderCell>`/`<TableCell>` pair and `TableSkeleton`'s `columns` count are both conditional on `!isTroopLeader` so the column count stays consistent. No new base components. 215 tests still pass (no test changes needed — this is presentational/query-param plumbing, not new service logic), typecheck/lint clean both workspaces. Live-verified against the real dev servers + seeded Neon DB: as Liza Bagadiong, the Troop column is gone (6 columns) and the header now reads "Membership Registry `CAT-VIR-012 — Troop 12 — Virac`", confirmed at 480/900/1280px (table's own horizontal scroll wrapper handles overflow, filter bar wraps the same as the existing filters); as Administrator, the Troop column still shows, the new "Filter by Troop" dropdown lists all 5 real seeded troops, and selecting `CAT-BAT-004` live-filtered the real data to exactly its 4 real members (subtitle "4 members", pagination "1–4 of 4") with no other troop's rows leaking in.

### 1.4 Membership Approval (Executive Council) — ●

- **Loop step:** Done (steps 1–6 complete). Visual sign-off (step 2) passed 2026-07-23; Contract, Wire Read, Wire Write, Test & Done all same day.
- **Screens:** `/approvals` — Pending Registrations queue (name, type, troop, submitted date, Review action), paginated · review modal (read-only profile detail + Approve/Reject with an always-visible reason field)
- **Components:** `ApprovalQueue`, `ApprovalReviewModal` — compose existing kit primitives (`Card`, `Table`, `Modal`, `FormField`, `Textarea`, `Pagination`, `EmptyState`/`ErrorState`/`TableSkeleton`); no new base components. Reuses `Member`/`MemberSummary` types and `getMember` from the 1.3 members feature rather than duplicating them
- **Endpoints:** **All 3 live** — `GET /api/v1/members/pending` (paginated, status forced to `pending` server-side), `PATCH /api/v1/members/:id/approve`, `PATCH /api/v1/members/:id/reject` (body: `{ reason }`, Zod-validated ≥5 chars). All three gated by `members:approve` (admin + executive council only — same role set as `members:archive`, kept as a separate `canApprove` guard since the two permissions could diverge later). Approve/reject stamp `reviewedById`/`reviewedAt` on the member and write the first-ever `audit_logs` rows (`member.approve` / `member.reject`, the latter with `{ reason }` in `details`).
- **Files:** `apps/web/src/app/(app)/approvals/page.tsx` · `apps/web/src/features/approvals/components/{approval-queue,approval-review-modal}.tsx` · `apps/web/src/features/members/services/members.service.ts` (added `listPendingMembers`/`approveMember`/`rejectMember`) · `apps/web/src/app/api/members/pending/route.ts` · `apps/web/src/app/api/members/[id]/{approve,reject}/route.ts` · `apps/web/src/shared/components/icons.ts` (added `RejectIcon`) · `apps/api/src/modules/members/{members.schema,members.repository,members.service,members.controller,members.routes}.ts` · `apps/api/src/shared/utils/audit-log.ts` (new) · `apps/api/tests/members.test.ts`
- **Verified:** `npm run typecheck`/`npm run lint`/`npm test` clean in both workspaces (37 API tests total, 6 new for `listPending`/`approve`/`reject` covering happy + not-pending + unknown-id paths, `writeAuditLog` mocked via `vi.mock`). Playwright against the real dev servers + seeded Neon DB: registered a new scout (Grace Villanueva) as Troop Leader → appeared live in the Executive Council's queue; opened her real fetched detail (not the row's lighter summary) and approved → toast, queue emptied, directory shows her as Active. Filtered the Membership Registry to `Rejected` and confirmed a curl-rejected member (Marites Tuazon) shows the red badge there and the real rejection reason in 1.3's already-built "Rejected: …" banner on her profile. Backend curl-verified directly: unauthenticated `GET /members/pending` → 401; Troop Leader → 403 on list, approve, and reject; reject with a 2-char reason → 422 `VALIDATION_ERROR`; approving an already-active member → 400 `BAD_REQUEST`; audit log rows confirmed written in Neon with the reviewer's real user id.
- **Done gate:** ✅ A member registered in 1.3 appears in the queue (both a pre-seeded pending member and a freshly registered one). ✅ Approved/rejected via the real UI. ✅ Status change visible in the 1.3 directory and profile (including the rejection-reason banner 1.3 already built). ✅ RBAC enforced at the API, not just hidden nav.
- **Deliberate deviations from the prototype/build-plan — flagged at visual verify:**
  1. No search/type filter on the queue, unlike 1.3's directory — build-plan only calls for a queue table + review modal, and the queue is expected to stay small; `Pagination` still covers growth.
  2. Reject's reason is one always-visible `Textarea` in the same modal rather than a second confirm dialog, so the reviewer can write it while still looking at the record.
  3. Added `whitespace-nowrap` to the troop-code badge — at 900px the `<code>` pill was wrapping across 3 lines; 1.3's directory has the same latent issue, left as-is there since it wasn't part of this feature's scope.
- **Remaining:** None.
- **Notes:** First feature to write to `audit_logs` (cross-cutting checklist item now ◑ → in progress, not yet ●, since only approve/reject write it — other critical actions across the app don't yet). `GET /members/pending` is mounted before `GET /members/:id` in `members.routes.ts` — otherwise Express would match `/pending` as `:id="pending"` and Prisma would reject the non-UUID lookup. `membersService.approve`/`reject` both guard against acting on a non-`pending` member (400), so the queue can't double-process a row from a stale client. Mutations refetch the queue rather than patching local state (settled decision #3), same convention as 1.3.

### 1.5 Dashboard (role-aware) — ●

- **Loop step:** Done (steps 1–6 complete). Visual sign-off (step 2) passed 2026-07-23; Contract, Wire Read, Test & Done all same day. Wire Write (step 5) is **N/A by design** — see Notes.
- **Screens:** `/dashboard` — one page, three role variants picked server-side from the session (never a client param, settled decision #4): **Admin** (org-wide stat cards, Membership Growth bar chart, Members by Status donut, Recent Activity feed, Troops Overview), **Executive Council** (council-scoped stat cards, Troops Overview, Membership by Scout Level progress bars), **Troop Leader** (troop-scoped stat cards, My Troop roster, Membership by Scout Level progress bars)
- **Components:** New shared primitives — `StatCard`/`StatCardSkeleton`, `ProgressBar`, `ActivityItem`/`ActivityDot` — plus feature components `MembershipGrowthChart` and `MemberStatusDonut` (first two `react-chartjs-2` charts in the app), `RecentActivityList`, `TroopOverviewList`, `ScoutLevelProgress`, `MyTroopRoster`, and the three role containers (`AdminDashboard`, `CouncilDashboard`, `TroopLeaderDashboard`) composed by `DashboardView`. Reuses `MemberStatusBadge`/`TableAvatar` from 1.3 rather than duplicating them
- **Endpoints:** **`GET /api/v1/dashboard`** — live, `requireAuth` + `requireRole(admin, executive_council, troop_leader)` (every seeded role, matching the nav item's lack of a `permission` gate). No request body/query — dispatches purely on `req.user.role`/`req.user.id` from the verified access token
- **Files:** `apps/web/src/app/(app)/dashboard/page.tsx` · `apps/web/src/features/dashboard/{types,constants}.ts` · `apps/web/src/features/dashboard/components/{dashboard-view,admin-dashboard,council-dashboard,troop-leader-dashboard,membership-growth-chart,member-status-donut,recent-activity-list,troop-overview-list,scout-level-progress,my-troop-roster}.tsx` · `apps/web/src/features/dashboard/services/dashboard.service.ts` · `apps/web/src/app/api/dashboard/route.ts` · `apps/web/src/shared/components/ui/{stat-card,progress-bar,activity-item}.tsx` · `apps/web/src/shared/components/ui/skeleton.tsx` (added `StatCardSkeleton`) · `apps/web/src/shared/utils/format-relative-time.ts` · `apps/api/src/modules/dashboard/{dashboard.types,dashboard.repository,dashboard.service,dashboard.controller,dashboard.routes,index}.ts` · `apps/api/src/routes/v1.ts` · `apps/api/tests/dashboard.test.ts`
- **Verified:** `npm run typecheck`/`npm run lint`/`npm test` clean in both workspaces (45 API tests total, 4 new for `dashboardService.getDashboard` covering admin aggregation — stats, 6-month growth bucketing, status-breakdown ordering/zero-filtering, registration+audit-log activity merge/sort — council's shared council-wide read plus scout-level percentages, and troop leader's own-roster stats **and** the "no troop assigned yet" empty payload for a self-signed-up leader 1.6 hasn't linked to a troop). Playwright against the real dev servers + seeded Neon DB for **all three real accounts**: Admin saw live org totals (12 members, 0 pending — grown from the original 10-member seed by earlier sessions' test registrations), a real 6-month growth chart, a real status donut, real merged recent-activity text ("Grace Villanueva's registration was approved", "New scout registered — Troop 12 — Virac"), and a real Troops Overview showing **CAT-SAN-007 correctly badged "No Leader"** (matches the seed's `leaderEmail: null`) while the other two show their real leaders. Executive Council saw the same real troops/members plus a real scout-level composition (Junior 44%, etc.). Troop Leader (`leader.virac@…`, Liza Bagadiong) saw her own real 6-member roster — including herself as an Adult Leader row — with real per-member status badges and scout levels. A sign-out/navigation race incidentally exercised the real error path too (`GET /api/dashboard` → 401 after cookies cleared → `ErrorState` rendered with working Retry). Backend curl-verified: unauthenticated `GET /dashboard` → 401.
- **Done gate:** ✅ Numbers/charts reflect real member data from 1.3 (org-wide for Admin, council-wide for Executive Council, troop-scoped for Troop Leader). ✅ Each role's variant renders its own layout, not just filtered content. ✅ Role read from session/JWT only — `dashboardController.get` never reads a query/body param, confirmed by `GET /dashboard` taking no arguments at all.
- **Deliberate deviations from the prototype/build-plan — flagged at visual verify:**
  1. Dropped the prototype's Badges/Attendance/Events stat cards and lists — those modules (2.1/2.2/2.4) don't exist yet, so those numbers would have been fake. Every number shown is honestly derivable from Members (1.3) + Approvals (1.4), so nothing needs replacing later, only wiring — which is exactly what happened.
  2. Donut segments are **member status** (Active/Pending/Archived/Rejected/…) rather than the prototype's scout-level split — ties directly to 1.3/1.4 instead of an arbitrary category, and doubles as a live view of the approval pipeline.
  3. Bar chart bars are a solid fill instead of the prototype's per-bar CSS gradient — minor ChartJS-port simplification.
  4. Admin's "Upcoming Events" panel is replaced with "Troops Overview" (shared with Executive Council) — Events (2.1) isn't built.
  5. Dropped the prototype's "Annual Target" progress widget — no feature owns configuring that goal number.
  6. Troop overview's Active/Inactive badge became **Led/No Leader**, derived from the real `Troop.leaderId` (the schema has no separate troop lifecycle status) — caught during Wire Read when CAT-SAN-007's real seed data (`leaderEmail: null`) didn't match the mock's invented "all active" troops.
- **Scope boundary — documented, not a gap:** `User` has no `councilId`, and there's exactly one council in the schema/seed today, so Executive Council's dashboard reads the same council-wide data Admin's does (minus the "Active Councils" stat, which is meaningless at council scope). Revisit once 1.6 supports multiple councils with a real per-user council assignment. Separately, a `troop_leader` account created via self-signup (1.1) has no relational link to a `Troop` row until an admin assigns one (1.6) — `getTroopLeaderDashboard` handles this for real via `findTroopByLeaderId` returning `null`, returning the same "No Troop Assigned" empty payload the mock step already drew, not a crash.
- **Wire Write:** **N/A by design** — build-plan.md §1.5 scopes this feature as a single `GET` endpoint; there is nothing to create/update/delete. The Definition of Done's write-verification item does not apply, same precedent as Phase 0's scaffolding features.
- **Notes:** Mock-step scaffolding (a "Preview state" Ready/Empty/Loading/Error switcher used to visually verify all four states without a backend) was deleted at Wire Read once `DashboardView` drove `state` from a real `fetch` — same convention `MemberDirectory` set. Contract step also removed `icon`/`tone` from the wire shape: an `IconType` is a React component and cannot cross an HTTP response, so `DashboardStatDto` only carries `{id, label, value}` and the frontend maps `id` → icon/tone per role in `features/dashboard/constants.ts` (`ADMIN_STAT_PRESENTATION` etc.) — the API dictates data, not styling. `ActivityEntryDto.occurredAt` is a raw ISO timestamp for the same reason; `shared/utils/format-relative-time.ts` (new, `Intl.RelativeTimeFormat`-based) formats it client-side rather than baking "2 hours ago" into a response that might be cached. Recent Activity is the first read in the app to merge two sources (`members.createdAt` for registrations + `audit_logs` for approve/reject, written since 1.4) into one timeline — registration itself still doesn't write an audit row, a preexisting gap noted in 1.4, not introduced here. **2026-07-26 fix:** `MyTroopRoster` now caps at `max-h-[360px] overflow-y-auto` (same precedent as the notification panel) — a large troop's roster was stretching the card past its "Membership by Scout Level" sibling in the Troop Leader dashboard's two-column grid.

### 1.6 Organization Management — ●

- **Loop step:** Done (steps 1–6 complete). Visual sign-off (step 2) passed 2026-07-23; Contract, Wire Read, Wire Write, Test & Done all same day.
- **Screens:** `/organizations` — one page, five tabs (Councils, Troops, Scout Levels, Badge Categories, Activity Categories) built on a new `Tabs` primitive. Each tab is a table + Add/Edit/Delete, visible to all roles for browsing but only Admin sees the write controls.
- **Components:** New shared `Tabs`/`TabPanel` (registry §9, was `planned`) — generalizes the `role="tablist"` pattern `auth-card.tsx` hardcoded for its two tabs. Feature components: `OrganizationManagement` (tab shell + per-resource fetch), `CouncilsPanel`/`CouncilFormModal`, `TroopsPanel`/`TroopFormModal` (troop leader assignment from real `troop_leader`-role users), and one generic `CategoryPanel`/`CategoryFormModal` reused for Scout Levels/Badge Categories/Activity Categories (only difference: `orderNumber` and the usage-count label) rather than three near-duplicate components.
- **Endpoints:** **20 live**, all under `/api/v1/organizations`: `GET/POST/PUT/DELETE` for `councils`, `troops`, `scout-levels`, `badge-categories`, `activity-categories`, plus `GET /troop-leaders` (users holding the `troop_leader` role, for the leader-assignment picker). Reads gated `requireRole('admin','executive_council','troop_leader')`; writes gated `requireRole('admin')` only. `GET /troops` and `GET /scout-levels` are the same endpoints 1.3's registration form already used — widened with additive fields (`leaderId`/`leaderName`/`memberCount` etc.) rather than duplicated, since 1.3's mapping only reads the fields it needs.
- **Files:** `apps/web/src/shared/components/ui/tabs.tsx` (new primitive) · `apps/web/src/app/(app)/organizations/page.tsx` · `apps/web/src/features/organizations/{types,constants}.ts` · `apps/web/src/features/organizations/components/{organization-management,councils-panel,council-form-modal,troops-panel,troop-form-modal,category-panel,category-form-modal}.tsx` · `apps/web/src/features/organizations/services/organizations.service.ts` · `apps/web/src/app/api/organizations/{councils,troops,troop-leaders,scout-levels,badge-categories,activity-categories}/**` · `apps/web/src/shared/components/icons.ts` (added `ScoutLevelIcon`) · `apps/api/src/modules/organizations/{organizations.types,organizations.schema,organizations.repository,organizations.service,organizations.controller,organizations.routes,index}.ts` · `apps/api/tests/organizations.test.ts` · `apps/api/src/shared/constants/roles.ts` · `apps/web/src/shared/constants/roles.ts` (both corrected, see RBAC note below)
- **Verified:** `npm run typecheck`/`npm run lint`/`npm test` clean in both workspaces (62 API tests total, 21 new for `organizationsService` — duplicate-name conflicts, unknown-council/unknown-leader rejections, and the delete-conflict guard for all 5 resource types). Playwright against the real dev servers + seeded Neon DB as Administrator: all 5 tabs render real seeded data (1 council/3 troops/5 scout levels/5 badge categories/4 activity categories, matching `seed.ts` exactly); full create→edit→delete cycle exercised live for a real council and a real troop (with a real troop-leader assignment, "Grace Tapel" shown leading two troops simultaneously — the schema doesn't constrain one-leader-per-troop) and a real activity category, all three genuinely round-tripped through Neon and cleaned up after. Confirmed Executive Council sees all 5 tabs' data but no Add/Edit/Delete buttons and no Actions column (read-only, matching project-overview.md's role matrix). Responsive re-confirmed at 900/768/480px — the 5-tab strip scrolls horizontally instead of wrapping. Backend curl-verified directly: unauthenticated write → 401; Executive Council `POST /councils` → 403 (real RBAC, not just hidden UI) while `GET /councils` → 200 (read still allowed); deleting a council with troops/members → 409 `CONFLICT`; duplicate council name → 409; empty name → 422 `VALIDATION_ERROR` with field details. **Superseded 2026-07-25** — the original verification pass showed "Grace Tapel leading two troops simultaneously" as an observed, unconstrained behavior; see the one-troop-per-leader revision below, which closed that off.
- **Done gate:** ✅ Admin can CRUD the full org hierarchy (councils, troops, scout levels, badge categories, activity categories). ✅ Troop leader assignment reflected immediately — assigning "Grace Tapel" to a new troop showed her real name in the Leader column with no page reload, and 1.5's dashboard "Led/No Leader" badge (built against `Troop.leaderId`) picks it up automatically since it reads the same column.
- **RBAC correction (2026-07-23):** `apps/api/src/shared/constants/roles.ts` and `apps/web/src/shared/constants/roles.ts` both listed `organizations:write` under Executive Council's permissions — this predates 1.6 and contradicted project-overview.md, which scopes "Organization Management" (manage troops/councils/scout levels/badge categories/activity categories) to the Administrator section only; Executive Council's own section only has "Organizational KPIs"/"Organization performance" (monitoring, not managing). Confirmed with the user and removed `ORGANIZATIONS_WRITE`/`'organizations:write'` from Executive Council's list in both files. The live Neon `role_permissions` table had a stale grant row from before this fix (seed.ts only upserts, never removes stale permission links) — removed via a one-off script since `requireRole()`'s hardcoded per-route role lists are the actual enforcement mechanism in this codebase, not that table, so the stale row was cosmetic but worth cleaning up for data-hygiene. `organizations:read` stays on all three roles — browsing the org hierarchy is genuinely useful for Troop Leader/Executive Council (1.3's registration form depends on it).
- **Deliberate deviations from the prototype/build-plan — flagged at visual verify:**
  1. `Gsp.html` never had an Organization Management screen at all — this is new layout, not a port. Followed 1.3/1.4's `Card`/`Table`/`Modal`/`ConfirmDialog` conventions rather than inventing a new visual language.
  2. Delete is guarded client- and server-side by real usage counts (troop/member count for councils, member count for troops, member/badge/event count for categories) — the schema's FKs are all `onDelete: SetNull`, so an unguarded delete would silently orphan real records rather than fail loudly; the guard turns that into an explicit 409 with an actionable message instead.
  3. One generic `CategoryPanel`/`CategoryFormModal` handles Scout Levels, Badge Categories, and Activity Categories rather than three near-identical components — build-plan.md's "category/level config forms" (plural) reads as if each needed its own, but the only real differences are `orderNumber` and the usage-count label.
- **Revision — badge-category icons (2026-07-25):** The Badge Categories tab gained an admin-selectable icon (new `IconPicker`, 12 curated keys), shown as a swatch beside the name in the table and inherited by every badge on 2.4's catalog grid. Driven by 2.4 — full rationale, schema and verification are recorded under [2.4](#24-badges--achievements--) since that's where the visible payoff is. Deviation 3's generic-panel decision held up: the icon is an opt-in `showIcon` prop alongside the existing `showOrder`, so Scout Levels and Activity Categories needed no changes and were re-verified unchanged. Two extra fields on one shared component is still well short of justifying a third near-identical panel.
- **Revision — one troop per leader (2026-07-25):** User asked whether a leader could be assigned to multiple troops; investigation found the schema, service and UI all allowed it, and that the Dashboard's `findTroopByLeaderId` (`findFirst`, no `orderBy`) would silently show an arbitrary one of a multi-troop leader's troops while Badges/Activity Reports correctly aggregated across all of them — an inconsistency, not a feature. User chose to enforce one-troop-per-leader rather than make Dashboard multi-troop-aware. **Blast radius: shared** (`Troop.leaderId` is read by dashboard/badges/reports/settings/members modules per grep — all reviewed; none needed changes, since `findFirst`/`findMany`/`count` on the FK all remain correct with 0-or-1 results). Before migrating, found the live Neon DB already had the exact conflict — Grace Tapel led both `CAT-BAT-004` (real, 4 members, from the original seed) and `CAT-VIG-021` (0 members, created 2026-07-25 — leftover test data). User confirmed clearing her from `CAT-VIG-021`. Changes: `Troop.leaderId` is now `@unique` (nullable-unique, Postgres allows multiple NULLs) via migration `20260725093000_troop_leader_id_unique`, applied to Neon; new `organizationsRepository.findTroopByLeaderId` + a `requireLeaderNotAlreadyAssigned` service guard on both `createTroop` and `updateTroop` (excluding the troop being edited) returning 409 `CONFLICT`; `TroopFormModal`'s leader `<Select>` now excludes leaders already assigned to a *different* troop (a troop's own current leader stays selectable when editing), with an updated hint. 3 new tests in `organizations.test.ts` (create-conflict, update keeping own leader, update-conflict) — 203 API + 5 web tests pass, typecheck/lint clean both workspaces. Playwright + direct fetch against the real dev servers + Neon: edited `CAT-VIG-021` and confirmed the dropdown showed only the one genuinely unassigned leader (Grace and Liza both excluded); a direct `PUT` forcing Grace onto `CAT-VIG-021` returned real `409 CONFLICT`; a `PUT` re-saving Grace as `CAT-BAT-004`'s own leader still returned `200` (self-reassignment isn't blocked).
- **Remaining:** None.
- **Notes:** Mutations refetch rather than patch local state (settled decision #3) — every create/update/archive delete re-runs the affected resource's list fetch; troop create/update/delete also refetches councils (troop/member counts live on the council row). `useResourceList<T>` (a small local hook in `organization-management.tsx`) fetches once per resource on mount and exposes a `refetch`, avoiding five near-identical `useState`+`useEffect` blocks. `ScoutLevel`/`BadgeCategory`/`ActivityCategory` have no `createdAt` column in the schema — caught during Contract when the DTO types were drafted with one anyway; removed from both the backend types and the frontend `CategoryItem` type before it shipped as a lie the API could never actually return.
- **Bug fix — edit-form values reverting mid-edit (2026-07-26):** `troop-form-modal.tsx` and `category-form-modal.tsx` (and `council-form-modal.tsx`, 1.6's third form modal) had the same reset-`useEffect`-keyed-on-a-fresh-object bug found and fixed in 2.1's `EventFormModal`; same fix applied (reset only on the closed→open transition). See [2.1](#21-event-management--) for the full root-cause writeup.

---

# Phase 2 — Activities & Engagement

### 2.1 Event Management — ●

- **Loop step:** Done (steps 1–6 complete). Visual sign-off (step 2) passed 2026-07-23; Contract, Wire Read, Wire Write, Test & Done all same day.
- **Screens:** `/events` — stat cards (Upcoming/Completed/Total Registrations), List/Calendar view toggle (`Tabs`), search + status + category filters, paginated event cards, create/edit modal, delete confirm · `/events/[id]` — full event detail, registration count, edit/delete
- **Components:** `EventsView`, `EventCard`, `EventCalendar` (first real Calendar — registry §4's entry was a CSS-only placeholder the prototype never wired to markup), `EventFormModal`, `EventDetailView`, `EventStatusBadge` — all compose existing kit primitives (`Card`, `Tabs`, `Modal`, `ConfirmDialog`, `FormField`, `Select`, `Pagination`, `EmptyState`/`ErrorState`/`Skeleton`, `StatCard`); no new base primitives
- **Endpoints:** **All 5 live** — `GET/POST /api/v1/events`, `GET/PUT/DELETE /api/v1/events/:id`. Reads gated `requireRole('admin','executive_council','troop_leader')`; writes gated `requireRole('admin')` only (RBAC fix below). Reuses 1.6's `GET /organizations/activity-categories` and `GET /organizations/troop-leaders` for the category/organizer pickers rather than duplicating those lookups.
- **Files:** `apps/web/src/app/(app)/events/page.tsx` · `apps/web/src/app/(app)/events/[id]/page.tsx` · `apps/web/src/features/events/{types,constants,utils}.ts` · `apps/web/src/features/events/components/{events-view,event-card,event-calendar,event-form-modal,event-detail-view,event-status-badge}.tsx` · `apps/web/src/features/events/services/events.service.ts` · `apps/web/src/app/api/events/route.ts` · `apps/web/src/app/api/events/[id]/route.ts` · `apps/web/src/shared/components/icons.ts` (added `ClockIcon`/`LocationIcon`/`ListViewIcon`) · `apps/api/src/modules/events/{events.types,events.schema,events.repository,events.service,events.controller,events.routes,index}.ts` · `apps/api/tests/events.test.ts` · `apps/api/src/routes/v1.ts` · `apps/api/src/shared/constants/roles.ts` · `apps/web/src/shared/constants/roles.ts` (both corrected, see RBAC note below)
- **Verified:** `npm run typecheck`/`npm run lint`/`npm test` clean in both workspaces (88 API tests total, 13 new for `eventsService` — status derivation at the day boundary, troopNames/organizer/category mapping including the unassigned case, create/update category+organizer validation, not-found on get/update/delete). Playwright against the real dev servers + seeded Neon DB as Administrator: all 4 real seeded events render (real dates, "Troop 12 — Virac" from the real organizer's led troop, 6 real registrations each); ran a full real create → edit (assigned a real organizer, "Troop 12 — Virac" appeared immediately) → delete cycle against Neon, cleaned up after; confirmed Troop Leader sees List/Calendar and all events but no New Event button and no Edit/Delete actions. Backend curl-verified directly: unauthenticated `GET /events` → 401; Troop Leader `POST /events` → 403 (real RBAC, not just hidden UI) while `GET /events` → 200; missing required fields → 422 with per-field details; unknown `categoryId` → 400; `endTime` before `startTime` → 422; `DELETE` on an unknown id → 404.
- **Done gate:** ✅ Events visible on both calendar and list (real seeded data, real dates). ✅ Full CRUD works end-to-end against Neon. ✅ "Leader/troop assignment" — build-plan's phrasing maps to the schema's actual `Event.organizerId` (a `User`, always a troop leader); "troop" is derived read-only from that organizer's own led troop(s), since the schema has no direct `Event.troopId`.
- **Deliberate deviations from the prototype/build-plan — flagged at visual verify:**
  1. Calendar is a wholly new build — `Gsp.html`'s `.cal-grid`/`.cal-day` CSS existed but was never wired to any markup (ui-registry.md §4 noted this explicitly), so there was no real layout to port. Built as a month grid of real `<button>` day cells with a below-grid "selected day" events panel.
  2. List view is a card grid, not a table — matches the prototype's own Events page (`Gsp.html` used cards there too), with pagination/empty/error/loading states added (the prototype had none).
  3. Event status (Upcoming/Completed) is derived server-side from `eventDate` vs. today, never stored — same pattern as 1.5 dashboard's "Led/No Leader".
  4. Delete's `ConfirmDialog` explicitly names the cascade ("along with its N registrations and any attendance records") since `EventRegistration`/`AttendanceRecord`/`AttendanceSummary`/`ActivityReport` all `onDelete: Cascade` on `Event` — unlike 1.6's org entities (which block deletion via a usage-count guard), this cascade is intended schema behavior, so the dialog surfaces the consequence instead of blocking it.
  5. Native `<input type="date">`/`type="time">` instead of the still-`planned` `DatePicker` — same precedent as 1.3's renewal dates.
- **RBAC correction (2026-07-23, before building):** Both `roles.ts` files granted Executive Council `events:write`, contradicting project-overview.md — event Create/Edit/Delete/"Assign troop leaders" is scoped to Administrator only; Executive Council's own section only has "Event participation" (analytics) and "Event Reports" (generation), i.e. monitoring, not managing. Same class of issue as 1.6's `organizations:write` correction. User confirmed the fix — Executive Council now holds `events:read` only, matching Troop Leader.
- **Bug caught and fixed during self-review (2026-07-23):** `EventCalendar` was unmounting on every background refetch (gated behind `viewState === 'ready'`), silently resetting its internal month-cursor/selected-day state back to the current month after every mutation. Fixed by adding a `hasLoadedOnce` flag so the calendar (and list) stay mounted through subsequent loading states once the first successful fetch completes — verified by navigating to August, creating an event, and confirming the calendar stayed on August afterward.
- **Bug fix — edited value reverting mid-edit on the second open (2026-07-26):** User reported: edit an event, save (succeeds) — then immediately reopen the edit form, type a new value, and after ~500–1000ms it reverts to the original. Root cause: `EventFormModal`'s reset `useEffect` was keyed on `[isOpen, initialValues]`, but `events-view.tsx` passes `initialValues={toFormValues(formModal.event)}` inline — a brand-new object literal on *every* render of `EventsView`. Any unrelated re-render while the modal stayed open (here, the background `fetchEvents()` refetch the previous save kicked off, which takes a few hundred ms to resolve) produced a new `initialValues` reference, re-firing the effect and overwriting whatever the user had just typed with the originally-loaded values. Fix: gated the reset on the actual closed→open transition via a `wasOpen` ref, so it no longer fires on every re-render while the modal stays open. **Cross-cutting**: the identical `useEffect`-keyed-on-a-fresh-object pattern, copy-pasted, was found in 7 other edit-capable modals and given the identical fix: `member-form-modal.tsx` (1.3), `troop-form-modal.tsx`/`category-form-modal.tsx`/`council-form-modal.tsx` (1.6), `badge-form-modal.tsx` (2.4), `user-form-modal.tsx` (3.4), `fee-type-form-modal.tsx` (3.1) — all noted in their own feature entries. (Create-only modals with no `initialValues` prop — activity reports, achievements, record-badge, announcements, expenses, payments — depend only on the stable `isOpen` boolean and were never affected.) Verified: full test suite (218 tests) + typecheck clean both workspaces; live-reproduced against the real dev server + seeded Neon DB — edited "Coastal Clean-Up Drive", saved, immediately reopened, typed a new title, confirmed it survived a 900ms wait, saved again, then restored the original title so the seed data is unchanged.
- **Remaining:** None.
- **Notes:** Mutations refetch rather than patch local state (settled decision #3). Real seed data (`seedEvents` in `seed.ts`) never sets `startTime`/`endTime`, so all 4 real events show date+location only — `formatEventTimeRange` correctly returns `null` rather than a fake range. `Grace Tapel`/`Liza Bagadiong` (real seeded troop leaders) appear in the organizer picker alongside any troop-leader accounts created via self-signup since (e.g. a real `Vernel Aquino` account from earlier testing) — the picker is genuinely `GET /organizations/troop-leaders`, not a fixed list.

### 2.2 Event Registration & Attendance — ●

- **Loop step:** Done (steps 1–6 complete). Visual sign-off (step 2) passed 2026-07-23; Contract, Wire Read, Wire Write, Test & Done all same day.
- **Screens:** `/attendance` (replaced the Phase-0 placeholder) — event picker, stat cards (Registered/Present/Absent/Rate, threshold-colored), one combined **Participants & Attendance** table (participant list + present/absent checklist in a single table rather than two near-duplicate ones), Register Participant modal (searchable picker of not-yet-registered active members). Linked from `/events/[id]`'s Registrations card via a new "Manage Attendance" button, deep-linking to `/attendance?eventId=...`.
- **Components:** `AttendanceView`, `ParticipantAttendanceTable`, `RegisterParticipantModal`, `AttendanceSummaryCards` — all compose existing kit primitives (`Card`, `CardHeader`, `Table`, `Modal`, `ConfirmDialog`, `Select`, `SearchInput`, `ToggleSwitch`, `StatCard`, `Badge`, `EmptyState`/`ErrorState`/`TableSkeleton`); no new base primitives
- **Endpoints:** **4 live** — `GET/POST /api/v1/events/:id/registrations` and `PATCH /api/v1/events/:id/registrations/:memberId` (added to the existing `events` module rather than a new one — same precedent as 1.4 extending `members` for approvals, since `EventRegistration` is a sub-resource of `Event`), plus `POST /api/v1/attendance` (new `attendance` module, owning `AttendanceRecord`). Reads open to all 3 roles; writes (register/cancel/reactivate/record) gated `requireRole('admin','troop_leader')` — Executive Council is monitoring-only per project-overview.md, and the existing `attendance:read`/`attendance:write` permission grants already matched this split with no correction needed (unlike 1.6/2.1's RBAC fixes). The register-participant picker reuses 1.3's `GET /members?status=active` rather than a new lookup endpoint.
- **Files:** `apps/web/src/app/(app)/attendance/page.tsx` · `apps/web/src/features/attendance/{types,constants}.ts` · `apps/web/src/features/attendance/components/{attendance-view,participant-attendance-table,register-participant-modal,attendance-summary-cards}.tsx` · `apps/web/src/features/attendance/services/attendance.service.ts` · `apps/web/src/app/api/events/[id]/registrations/route.ts` · `apps/web/src/app/api/events/[id]/registrations/[memberId]/route.ts` · `apps/web/src/app/api/attendance/route.ts` · `apps/web/src/features/events/components/event-detail-view.tsx` (added the "Manage Attendance" button) · `apps/api/src/modules/events/{events.schema,events.types,events.repository,events.service,events.controller,events.routes}.ts` (extended) · `apps/api/src/modules/attendance/{attendance.schema,attendance.types,attendance.repository,attendance.service,attendance.controller,attendance.routes,index}.ts` (new module) · `apps/api/src/routes/v1.ts` · `apps/api/tests/{events,attendance}.test.ts`
- **Verified:** `npm run typecheck`/`npm run lint`/`npm test` clean in both workspaces (88 API tests total, 13 new — 9 for `eventsService`'s `listParticipants`/`registerParticipant`/`updateRegistrationStatus` covering the join+summary math, cancelled-registration exclusion, unknown event/member, and reactivation; 4 for `attendanceService.record` covering unknown event, not-registered, cancelled-registration rejection, and a successful upsert). Playwright against the real dev servers + seeded Neon DB as Troop Leader: the real "Provincial Investiture Ceremony" event (6 seeded registrations, 5 present/1 absent, 83%) rendered exactly matching a direct curl of the same endpoint; registered a real member via the modal, toggled her attendance to Present (stat cards recalculated live to 86%), then cancelled her registration (count returned to 6/83%, restoring the original seeded state) — all genuinely round-tripped through Neon. Confirmed Executive Council sees the full real roster read-only (no Register button, disabled toggles, no Actions column). Backend curl-verified directly: unauthenticated → 401; Executive Council `POST /registrations` and `POST /attendance` → 403 (read still 200); missing `memberId` → 422; unknown member → 400; unknown event → 404; cancelling a non-existent registration → 404; recording attendance against a cancelled registration → 400 (`"This member is not registered for this event."`); bad attendance-status enum value → 422. Full write cycle (register → record present → cancel → blocked-attendance-on-cancelled → reactivate) walked via curl against real seed data and left in its original state afterward.
- **Done gate:** ✅ Attendance recordable per event, per real registered member. ✅ Summary + rate visible with threshold coloring (≥90% green, ≥75% gold, <75% red — adapted from ui-rules.md §7's absent-count example to a percentage, since a fixed absent-count threshold doesn't generalize across events of different sizes; flagged at visual verify, no objection). ✅ RBAC enforced at the API (curl-verified 403s), not just hidden UI.
- **Bugs caught and fixed during self-review (2026-07-23):**
  1. The register-participant picker excluded only non-cancelled registrants, so a member with a *cancelled* row for the event could be "registered" again through the modal — which would have raced the real unique `[eventId, memberId]` constraint against the table's own Re-register action. Fixed to exclude anyone with any existing row for the event, cancelled or not; reactivating stays the table's job.
  2. A freshly-registered mock participant rendered an empty troop-code pill (no real troop code available client-side before the fix). Table now falls back to the troop name when no code is present.
  3. Troop-code pills and the attendance-toggle-plus-label were wrapping across multiple lines at 768px — same class of bug 1.4 hit with its own troop badge. Fixed with `whitespace-nowrap` on both, so the table scrolls horizontally instead (same convention every prior feature's tables use).
  4. The register modal's member picker requested `pageSize: 200` from `GET /members`, but that endpoint caps `pageSize` at 100 (`members.schema.ts`) — would have 422'd on any real click of "Register Participant". Caught via a curl walkthrough before browser-testing; fixed to request 100.
- **Notes:** Mutations refetch rather than patch local state (settled decision #3) — every register/cancel/reactivate/attendance-toggle re-runs `GET /events/:id/registrations` (which returns participants + summary together in one response, not two round trips). `AttendanceSummaryDto` is computed at request time from live `AttendanceRecord`/`EventRegistration` counts — the schema's `AttendanceSummary` table stays unused, same simplification precedent as 1.5's/2.1's other server-computed fields (a stored summary would need to be kept in sync on every attendance write for no query benefit here, since the per-event roster is small). Cancelling a registration doesn't delete or clear its attendance history — reactivating shows whatever attendance was previously recorded, which is genuine history rather than a fresh blank. The registration/attendance checklist only ever writes `present`/`absent`; the schema's `AttendanceStatus` enum also has `excused`/`late` and `RegistrationStatus` also has `waitlisted`, both left unused since nothing in this feature's scope (no event capacity concept exists) calls for them.

### 2.3 Activity Report Submission (Troop Leader) — ●

- **Loop step:** Done (steps 1–6 complete). Visual sign-off (step 2) — human reviewed the full report and gave a go-ahead to proceed without a separate live walkthrough; Contract, Wire Read, Wire Write, Test & Done all same session (2026-07-23).
- **Screens:** `/activity-reports` — one page, two variants picked server-side from the session (Troop Leader: "My Activity Reports" + Submit action; Admin/Executive Council: "Activity Reports" across every troop, with Submitted By/Troop columns) · Submit form modal (event picker limited to completed events, summary/participation notes/outcomes) · read-only detail modal
- **Components:** `ActivityReportsView` (role-branch container, same convention as `DashboardView`), `ActivityReportFormModal`, `ActivityReportDetailModal`, `ActivityReportStatusBadge` — all compose existing kit primitives (`Card`, `Table`, `Modal`, `FormField`, `Select`, `Textarea`, `Pagination`, `EmptyState`/`ErrorState`/`TableSkeleton`); no new base components
- **Endpoints:** **All 3 live** — `GET/POST /api/v1/activity-reports`, `GET /api/v1/activity-reports/:id`. Reads gated `requireRole('admin','executive_council','troop_leader')` with role-based scoping in the service (Troop Leader sees only `submittedById = self`; Admin/Executive Council see every troop). Writes gated `requireRole('admin','troop_leader')`, matching the existing `activity-reports:write` grant (checked pre-build, already correct — no RBAC correction needed, same outcome as 2.2). `create` validates the event exists and is genuinely completed (`eventDate` in the past) before writing, independent of the frontend's own completed-only picker.
- **Files:** `apps/web/src/app/(app)/activity-reports/page.tsx` · `apps/web/src/features/activity-reports/{types,constants}.ts` · `apps/web/src/features/activity-reports/components/{activity-reports-view,activity-report-form-modal,activity-report-detail-modal,activity-report-status-badge}.tsx` · `apps/web/src/features/activity-reports/services/activity-reports.service.ts` · `apps/web/src/app/api/activity-reports/route.ts` · `apps/web/src/app/api/activity-reports/[id]/route.ts` · `apps/web/src/shared/components/layout/nav-items.ts` (added the nav item + `DocumentIcon`) · `apps/web/src/shared/constants/roles.ts` (added the `/activity-reports` route-permission entry) · `apps/api/src/modules/activity-reports/{activity-reports.schema,activity-reports.types,activity-reports.repository,activity-reports.service,activity-reports.controller,activity-reports.routes,index}.ts` · `apps/api/src/routes/v1.ts` · `apps/api/tests/activity-reports.test.ts`
- **Verified:** `npm run typecheck`/`npm run lint`/`npm test` clean in both workspaces (96 API tests total, 8 new for `activityReportsService` — troop-leader scoping, admin/EC unscoped read, troop-name mapping, not-found on get, unknown-event/not-yet-completed/success paths on create). Playwright against the real dev servers + seeded Neon DB: as Troop Leader (Liza Bagadiong), the real empty state rendered first (no seeded `ActivityReport` rows), then submitted a real report against the real "Coastal Clean-Up Drive" event — appeared instantly (refetch after write, settled decision #3), detail modal showed the real troop name ("Troop 12 — Virac", derived from her `ledTroops`) and the exact text submitted. As Administrator, the same real report appeared in the all-troops table with a working Submit action alongside it. Responsive re-confirmed at 900/768/480px with real data; fixed one bug during self-review before this stage — the event-title and submitted-date cells wrapped mid-word at 900px, same class of bug 1.4/2.2 hit, fixed with `whitespace-nowrap` so the table scrolls horizontally instead. Backend curl-verified directly: unauthenticated `GET` → 401; Executive Council `POST` → 403 (read still 200); missing `summary` → 422 `VALIDATION_ERROR`; unknown `eventId` → 400; a real upcoming event's id → 400 "You can only submit a report for a completed event."; unknown report id on `GET /:id` → 404.
- **Done gate:** ✅ A troop leader submits a report against a real event (curl + Playwright, both landed in Neon). ✅ Council (and Admin) see it in the all-troops list. ✅ RBAC enforced at the API (curl-verified 403), not just hidden UI.
- **Resolved during this session:**
  1. **No review/"mark reviewed" action is built.** build-plan.md's endpoints line lists exactly `GET/POST /activity-reports` + `GET /activity-reports/:id` — no PATCH — and its Access line scopes Executive Council/Admin as read-only. `ActivityReportStatus` keeps its `reviewed` value and the schema keeps `reviewedAt`, but nothing in this feature ever transitions a report to `reviewed`; every real report stays `submitted` forever unless a future feature adds that action. Same precedent as 2.2's unused `excused`/`late`/`waitlisted` enum values — schema-supported, deliberately not wired.
  2. **Admin gets both the all-troops view and a Submit action**, not read-only-only as build-plan's terser Access-line prose might suggest read literally. Resolved in Admin's favor because the existing `activity-reports:write` permission grant already includes Admin (checked against project-overview.md's "Administrator... has unrestricted access to all modules"), and 2.2 set the precedent of trusting an already-correct permission grant over the shorter feature-scoped prose. `showAllTroops` (view scope) and `canSubmit` (write capability) are two independent flags in `ActivityReportsView`, not one — Troop Leader gets own-scoped + submit, Executive Council gets all-scoped + read-only, Admin gets all-scoped + submit.
- **Notes:** No separate "detail" DTO — `ActivityReportSummary` is identical whether it comes from the list or `GET /:id`, so the detail modal reads directly off the row already in memory rather than an extra fetch (unlike 1.3/1.4's members, whose list row is a lighter `MemberSummary`). `troopName` is derived from the submitter's own `ledTroops`, same "derived, not stored" pattern as events' `troopNames`. The event picker reuses 2.1's `listAllEvents({status:'completed'})` rather than a new lookup endpoint. Mutations refetch after write (settled decision #3) — submitting re-runs `GET /activity-reports` rather than patching local state.

### 2.4 Badges & Achievements — ●

- **Loop step:** Done (steps 1–6 complete). Visual sign-off (step 2) passed 2026-07-24; Contract, Wire Read, Wire Write, Test & Done all same session.
- **Screens:** `/badges` — one page, stat cards (Badge Catalog/In Progress/Earned/Verified) + three `Tabs`: **Badge Catalog** (grid, Admin CRUD), **Member Progress** (per-member progress-bar table, "View" opens a detail modal listing that member's badge statuses with an Admin-only Verify action), **Achievement History** (table of free-form `AchievementRecord` entries). A page-level "Record Badge" action (Admin + Troop Leader) opens a member+badge+status picker independent of the active tab.
- **Components:** `BadgesView` (role-branch container + fetch orchestration, same convention as `DashboardView`/`ActivityReportsView`), `BadgeCatalogPanel`/`BadgeFormModal` (dynamic requirement-row list), `MemberProgressPanel`/`MemberProgressDetailModal`, `AchievementHistoryPanel`/`AchievementFormModal`, `RecordBadgeModal`, `MemberBadgeStatusBadge` (status → `Badge` tone, `in_progress`→blue/`earned`→gold/`verified`→green) — all compose existing kit primitives (`Card`, `Tabs`, `Table`, `Modal`, `ConfirmDialog`, `FormField`, `Select`, `StatCard`, `ProgressBar`, `EmptyState`/`ErrorState`/`CardSkeleton`/`TableSkeleton`); no new base primitives. Reuses 1.6's `GET /organizations/badge-categories` for the catalog form's category picker rather than duplicating it.
- **Endpoints:** **10 live** across three route groups — `GET/POST /api/v1/badges`, `PUT/DELETE /api/v1/badges/:id`, `GET /api/v1/badges/member-options` (catalog CRUD + a scoped member-picker lookup); `GET/POST /api/v1/member-badges`, `PATCH /api/v1/member-badges/:id/verify` (progress read + record + verify); `GET/POST /api/v1/achievements`. Reads open to all 3 roles (member-options is Admin+Troop Leader only, since only they write); catalog CRUD `requireRole('admin')`; record/achievement-create `requireRole('admin','troop_leader')`; verify `requireRole('admin')`. Troop Leader reads (`GET /member-badges`, `GET /achievements`, `GET /badges/member-options`) are scoped server-side to members of their own led troop(s) via `Member.troop.leaderId`, same "derived, not trusted" pattern as 2.1–2.3; Admin/Executive Council see every troop. `earnedCount` on each catalog badge is a live filtered relation count (`_count.memberBadges` where `status in [earned, verified]`), not a stored field.
- **Files:** `apps/web/src/app/(app)/badges/page.tsx` · `apps/web/src/features/badges/{types,constants}.ts` · `apps/web/src/features/badges/components/{badges-view,badge-catalog-panel,badge-form-modal,member-progress-panel,member-progress-detail-modal,achievement-history-panel,achievement-form-modal,record-badge-modal,member-badge-status-badge}.tsx` · `apps/web/src/features/badges/services/badges.service.ts` · `apps/web/src/app/api/badges/route.ts` · `apps/web/src/app/api/badges/[id]/route.ts` · `apps/web/src/app/api/badges/member-options/route.ts` · `apps/web/src/app/api/member-badges/route.ts` · `apps/web/src/app/api/member-badges/[id]/verify/route.ts` · `apps/web/src/app/api/achievements/route.ts` · `apps/api/src/modules/badges/{badges.schema,badges.types,badges.repository,badges.service,badges.controller,badges.routes,index}.ts` · `apps/api/tests/badges.test.ts` · `apps/api/src/routes/v1.ts` · `apps/api/src/shared/constants/roles.ts` · `apps/web/src/shared/constants/roles.ts` (both corrected, see RBAC note below)
- **Verified:** `npm run typecheck`/`npm run lint`/`npm test` clean in both workspaces (113 API tests total, 17 new for `badgesService` — catalog CRUD conflict/not-found paths, progress scoping for troop leader vs. admin/EC, progress aggregation math, record-badge unknown-member/out-of-scope-member/unknown-badge/success paths, verify not-found/not-earned/success paths, achievement out-of-scope/success paths). Playwright against the real dev servers + seeded Neon DB: as Administrator, all 6 real seeded badges rendered with real category/points/earned-counts and real stat cards (2 in progress/3 earned/2 verified, matching `seed.ts`'s award spread exactly); recorded a real achievement ("First Aid Certification" for Faith Bermundo) — appeared instantly via refetch; recorded a real "First Aider" badge as Earned for the same member — catalog's earned-count and the Earned stat both went up live; verified it from the Member Progress detail modal — flipped to "Verified by Marisol Tabuena" live, Verified stat incremented. As Executive Council, the same real data rendered fully read-only (no Add/Edit/Delete/Record/Verify anywhere). As Troop Leader (Liza Bagadiong), catalog/progress/achievements/member-picker were all correctly scoped to her own Troop 12 members only. Backend curl-verified directly: unauthenticated → 401 on all three read endpoints; Executive Council/Troop Leader `POST /badges` → 403 (catalog write is Admin-only); Executive Council `POST /member-badges` → 403; Troop Leader `PATCH /member-badges/:id/verify` → 403; Troop Leader recording a badge for a real member outside her own troop (Nadine Sorreda, Troop San Andres) → 403 with an explicit message; unknown badge id for an in-scope member → 400; verifying a real `in_progress` record → 400 "Only badges with status \"earned\" can be verified."; duplicate badge name on create → 409; delete of an unknown badge id → 404.
- **Done gate:** ✅ Badges recorded and verified against real members (curl + Playwright, both landed in Neon). ✅ Per-member progress visible with a real progress bar (earned+verified / catalog size). ✅ RBAC enforced at the API (curl-verified 401/403s), not just hidden UI, including the troop-ownership scope check on writes.
- **RBAC correction (2026-07-24, before building):** The seeded `ROLE_PERMISSIONS` (both `roles.ts` files) granted Troop Leader `badges:verify` alongside Admin, contradicting project-overview.md, which lists "Verify achievements" only under Administrator's "Badge & Achievement" section — Troop Leader's own "Badge Tracking" section says "record"/"update", never "verify". Confirmed with the user (same class of pre-build correction as 1.6/2.1) and removed `BADGES_VERIFY`/`'badges:verify'` from Troop Leader's list in both files; the seed re-derives `role_permissions` from this constant, so no separate data fix was needed. Catalog-write (`badges:write`, nominally shared with Troop Leader in the seeded grant) is enforced `requireRole('admin')` directly at the route level regardless, since catalog management ("Create badge requirements/Update badge information/Delete badges") is Admin-only per the same doc — Troop Leader's real write scope is confined to `MemberBadge`/`AchievementRecord`, not the catalog.
- **Deliberate deviations from the prototype — flagged at visual verify:**
  1. Catalog cards show neutral catalog info, not the prototype's per-viewer `.badge-card.earned` toggle — "earned" is a per-member fact, so it lives on the Member Progress tab instead of the shared catalog.
  2. The prototype's single "Award Badge" action is split into two: **Record Badge** (Troop Leader/Admin, writes `in_progress`/`earned`) and **Verify** (Admin-only, `earned`→`verified`) — matches the schema's real 3-state `MemberBadgeStatus` lifecycle instead of a binary earned/not-earned toggle.
  3. Added a third tab, **Achievement History** (`AchievementRecord`), which `Gsp.html` never had — required by build-plan.md's screen list and backed by a schema table distinct from `MemberBadge`.
  4. Stat cards relabel the prototype's 4th slot from "Members w/ Badges" to "Verified" — ties directly to `MemberBadgeStatus` instead of an arbitrary count, same tone order as the prototype (green/blue/gold/red).
  5. No pagination on the Member Progress or Achievement History tables — matches what was visually verified at the mock step (small, troop-scoped lists); revisit if total org membership grows enough for the all-troops Admin/EC view to matter, same style of documented gap as 1.4's approval queue.
- **Revision — category icons (2026-07-25, shared blast radius):** Catalog cards rendered a uniform `BadgeIcon`, so the largest element on every card carried no information. The icon now comes from the badge's **category** rather than the badge itself — same category means same glyph, which is what makes the grid scannable; per-badge picking across a 12-icon set and a growing catalog would only produce arbitrary near-duplicates. Rejected image upload outright: 3.5 already settled that this app has no avatar upload or URL anywhere, and badge art would drag in blob storage.
  - **Schema:** `BadgeCategory.icon String @default("award")` (migration `20260725051325_add_badge_category_icon`). The default backfills existing rows, so no data migration was needed; the five seeded categories were then backfilled to real glyphs (heart/compass/lifebuoy/flag/music) by targeted `UPDATE`, not a full re-seed. `seed.ts` carries the same values so a fresh seed matches.
  - **Contract:** `BadgeCategoryDto` gained `icon`; `BadgeDto` gained a denormalized `categoryIcon` (`badge.category?.icon ?? null`), same join as the existing `categoryName`. New `createBadgeCategorySchema`/`updateBadgeCategorySchema` extend `createCategorySchema` with a `z.enum` over 12 curated keys, defaulting to `award` — mirroring how scout levels extend it with `orderNumber`, so **activity categories keep the unextended schema and are unaffected**.
  - **Only a key is persisted, never a `react-icons` component name** — `icons.ts` exists so the icon set is swappable in one place, and writing `FaCampground` into Postgres would leak that decision into the database permanently. `resolveBadgeCategoryIcon()` falls back to the award glyph on an unknown key rather than throwing, so a bad row can't blank the catalog grid.
  - **New shared primitive:** `IconPicker` (`shared/components/ui/icon-picker.tsx`) — third user of the `sr-only` radio + styled label technique after `RoleSelector`/`ReportTypeSelector`. Deliberately not wrapped in `FormField`, which renders one `htmlFor` label that would dangle over a radio group; it owns its `<fieldset>`/`<legend>`.
  - **Shared components gated, not changed for everyone:** `CategoryPanel`/`CategoryFormModal` serve all three lookup tabs, so the icon swatch and picker are behind an opt-in `showIcon` prop (same shape as the existing `showOrder`). Scout Levels and Activity Categories render exactly as before — re-verified in the browser, not assumed.
  - **Verified:** `npm run typecheck`/`lint`/`test` clean in both workspaces (200 API + 5 web tests; 2 new — an out-of-set icon key is rejected, an omitted icon defaults to `award`). Playwright against the real dev servers + Neon: catalog showed 5 distinct category glyphs across 6 badges with the two Outdoor Skills badges correctly sharing the compass; changed Outdoor Skills to a star in the picker and the catalog card followed after the write, then reverted it to compass; Scout Levels' and Activity Categories' modals and tables confirmed unchanged; both changed screens checked at 480px with no overflow (picker reflows 7+5).
  - **Follow-on same day — the other two screens that show badges by name:** the catalog wasn't the only place a bare `BadgeIcon`/no-icon-at-all made 2.4 look inconsistent internally. Extended the same category-icon treatment to:
    - **Member Progress detail modal** (`member-progress-detail-modal.tsx`) — each of a member's badge rows now shows its category glyph beside the name, identical treatment to the catalog card. Required threading `badgeCategoryIcon` onto `MemberBadgeRecordDto` (API) and `MemberBadgeRecord` (web), denormalized from `badge.category?.icon` the same way `badgeCategoryName` already was.
    - **Record Badge modal** (`record-badge-modal.tsx`) — the badge field is a native `<select>` (`Combobox` is still `planned` in the registry, matching 1.3's precedent for small option counts), so a `react-icons` glyph can't render inside an `<option>`. Added a live preview swatch beside the select instead, driven by `resolveBadgeCategoryIcon(selectedBadge?.categoryIcon)` off the already-available `BadgeCatalogItem[]` — updates as the badge choice changes, defaults to the award glyph with nothing selected.
    - Deliberately **not** touched: the Member Progress summary table (lists members, not badges — no badge identity to attach a glyph to) and Achievement History (`AchievementRecord` is free-form text with no `badgeId` at all — nothing to resolve an icon from).
    - Re-verified: `typecheck`/`lint`/`test` clean (still 200 API + 5 web — no new test surface, this only threads an existing field through one more DTO); Playwright confirmed First Aider showed the Health & Safety life-buoy and Heritage Keeper the Arts & Culture music note in the detail modal, and the Record Badge preview swatch updated live from award → heart when Community Helper was selected; both checked at 480px, no overflow.
- **Bug fix — edit-form values reverting mid-edit (2026-07-26):** `badge-form-modal.tsx` had the same reset-`useEffect`-keyed-on-a-fresh-object bug found and fixed in 2.1's `EventFormModal`; same fix applied (reset only on the closed→open transition). See [2.1](#21-event-management--) for the full root-cause writeup. (`record-badge-modal.tsx`/`achievement-form-modal.tsx` are create-only with no `initialValues` — not affected.)
- **Remaining:** None.
- **Notes:** Mutations refetch everything (`fetchAll()`) rather than patch local state (settled decision #3) — every catalog/record/verify/achievement write re-runs badges + member-progress + achievements together, since a single write can move counts across all three (e.g. recording a badge changes both the catalog card's earned-count and the progress tab's totals). Caught and fixed two real bugs during self-review before Playwright: (1) the Record Badge button was squeezing the `Tabs` strip's overflow-x-auto region at 768px instead of wrapping below it — fixed by dropping a stray `flex-1 min-w-0` that was preventing the header row's `flex-wrap` from ever triggering; (2) the Member Progress detail modal captured a stale `MemberProgressSummary` snapshot on "View", so verifying a badge updated the stat cards live but not the open modal — fixed by tracking the selected member's id instead of the row object, so the modal re-reads from the live `members` prop every render. `MemberBadge` has no `notes`/`awardedBy` column (only `status`/`earnedAt`/`verifiedById`), so the Record Badge modal deliberately has no notes field, unlike the prototype's "Award Badge" modal — matches the schema truth rather than the prototype's UI.

### 2.5 Notifications & Announcements — ●

- **Loop step:** Done (steps 1–6 complete). Visual sign-off (step 2) passed 2026-07-24; Contract/Wire Read/Wire Write/Test & Done same day.
- **Screens:** Topbar notification bell + dropdown panel (all pages) · `/notifications` full personal history (reached only via the bell's "View All Notifications" link, no sidebar entry) · `/announcements` feed (new sidebar entry under Activities)
- **Components:** `NotificationBell`, `NotificationRow` (not `ActivityItem` — see deviation below), `AnnouncementCard`, `AnnouncementFormModal` — all reuse existing kit primitives (`Card`/`CardHeader`/`Modal`/`FormField`/`Input`/`Textarea`/`Badge`/`Pagination`/`EmptyState`/`ErrorState`/`CardSkeleton`/`TableSkeleton`), no base primitives duplicated
- **Endpoints:** **All 5 live** — `GET /api/v1/notifications` (own, paginated, includes `unreadCount`), `PATCH /api/v1/notifications/:id/read`, `PATCH /api/v1/notifications/read-all`, `GET /api/v1/announcements` (paginated, all 3 roles), `POST /api/v1/announcements` (`requireRole('admin','executive_council')`)
- **Files:** `apps/web/src/features/notifications/{types,services/notifications.service}.ts` · `apps/web/src/features/notifications/components/{notification-bell,notification-row,notifications-view}.tsx` · `apps/web/src/features/announcements/{types,constants,services/announcements.service}.ts` · `apps/web/src/features/announcements/components/{announcement-card,announcement-form-modal,announcements-view}.tsx` · `apps/web/src/app/(app)/{notifications,announcements}/page.tsx` · `apps/web/src/app/api/notifications/{route,read-all/route,[id]/read/route}.ts` · `apps/web/src/app/api/announcements/route.ts` · `apps/web/src/shared/components/layout/{topbar,nav-items}.tsx` · `apps/web/src/shared/components/icons.ts` (added `AnnouncementIcon`) · `apps/api/src/shared/utils/notify.ts` (new — `notifyUser`/`notifyUsers`) · `apps/api/src/modules/notifications/*` · `apps/api/src/modules/announcements/*` · `apps/api/src/modules/members/members.service.ts` (approve/reject notify) · `apps/api/src/modules/badges/badges.service.ts` (verify notify) · `apps/api/src/routes/v1.ts` · `apps/api/tests/{notifications,announcements}.test.ts` (new) · `apps/api/tests/{members,badges}.test.ts` (extended)
- **Deliberate deviations from the prototype — flagged at visual verify, no changes requested:**
  1. Built a dedicated `NotificationRow` instead of reusing `ActivityItem` (dashboard's feed row) — `ActivityItem`'s `meta` slot is a one-line caption (the dashboard only ever puts a timestamp there); forcing a full message into it truncated notification titles. Caught during self-review before the human look, fixed before presenting.
  2. Notification rows show a uniform green/blue dot for unread/read rather than the prototype's per-category icon+color (🆕/🏅/⚠️/📋) — the real `Notification` schema has no category column, only `isRead`; inventing a client-side category from title text would be a fragile heuristic with nothing backing it.
  3. The prototype's "View All Notifications" footer link was dead text (no `onclick`); it now really navigates to `/notifications`, a new full-history page not in the prototype at all.
  4. Announcements are create-only for v1 — no edit/delete, matching build-plan.md's endpoint scope exactly.
  5. `/notifications` has no sidebar nav entry (reached only via the bell) while `/announcements` does — matches how a personal notification-history page is normally surfaced vs. a shared content feed.
- **RBAC check before building (same discipline as 1.6/2.1/2.2/2.4):** the seeded `announcements:write` grant is Admin + Executive Council only, Troop Leader excluded — checked against project-overview.md's Troop Leader "Notifications" section ("Receive announcements", "View council notices", both receive-only framing) and found already consistent, no correction needed (same outcome as 2.2/2.3). `notifications:read` is granted to all three roles and is always scoped server-side to `req.user!.id` — there is no separate "announcements:read" permission because reading the feed is universal (like Dashboard/My Profile, the nav item carries no permission gate); only the `POST` is role-gated.
- **Real notification triggers (Done gate: "generated by real actions"):** exactly the three build-plan.md names — (1) **announcement posted**: `announcementsService.create` fans out one `Notification` row to every user except the poster via the new `notifyUsers` util; (2) **registration approved/rejected**: `membersService.approve`/`reject` notify `member.troop.leaderId` when the member's troop has an assigned leader (skipped otherwise — a self-signed-up leader with no troop link yet, same edge case 1.5 documented); (3) **badge verified**: `badgesService.verifyMemberBadge` notifies `verified.member.troop.leaderId` when present. All three go through one shared, best-effort `notifyUser`/`notifyUsers` (`apps/api/src/shared/utils/notify.ts`) that swallows its own failures — a failed notification write must never block the approval/verification/post it's attached to, same precedent as auth's best-effort logout revoke. Deliberately did **not** wire a fourth trigger for "event reminder" (mentioned only in the mock-step screens list, not in the Done gate's real-actions list) — there is no scheduler/cron in this codebase to fire a time-based reminder, and build-plan.md §2.5's scope note only commits to in-app notifications generated by real user actions, not clock-based ones.
- **Verified so far:** `npm run typecheck`/`npm run lint`/`npm test` clean in both workspaces (125 API tests passing, 5 new in `notifications.test.ts`, 3 new in `announcements.test.ts`, plus 2 new notify-side-effect tests each added to `members.test.ts` and `badges.test.ts` — 18/18 members, 18/18 badges). Playwright-verified against the real dev servers + seeded Neon DB: as Administrator, the real seeded "Welcome to the GSP portal" notification and the real seeded "Annual Council Camp registration is open" announcement (posted by Rosario Verceles) both rendered; marked the seeded notification read and watched the bell's unread count and highlight update live; posted a real new announcement ("Real Wire Verification — 2.5") — appeared instantly at the top via refetch, no unread badge for the poster. Signed in as Troop Leader (Liza Bagadiong) and confirmed she received a live, real "New announcement posted" notification from that same post (2 unread total, including the pre-existing welcome one) — the fan-out-excluding-poster logic verified end-to-end, not just in a unit test. Backend curl-verified directly: unauthenticated `GET /notifications` → 401; Troop Leader `POST /announcements` → 403; Executive Council `POST /announcements` → 201 (real row + fan-out); a real cross-user `PATCH /notifications/:id/read` (Troop Leader targeting an Admin-owned notification id) → 404, confirming the ownership check is real and not just a hidden UI gap; empty title/content → 422 with field details; verified a real earned badge (Faith Bermundo's "Heritage Keeper") via curl → 200, confirming the new badge-verify notify path runs cleanly live (its troop, Troop 4 — Bato, has a real assigned leader).
- **Remaining:** None. The members-approve/reject notify path is covered by 2 new unit tests (leader-present / no-leader) rather than an additional live end-to-end walkthrough — the identical shared `notifyUser` util was already proven live via the badge-verify and announcement-fan-out checks above, so a third live pass would exercise no new code.
- **Notes:** Mutations refetch after write, not optimistic (settled decision #3) — `NotificationBell` and `NotificationsView` each hold an independent fetch, so marking a notification read in one does not live-update the other's already-rendered state until its own next fetch (acceptable: they're never open at the same time in normal use). `Notification`/`AnnouncementPost` have no seed-time relation to specific "reminder" logic — the seed's one pre-existing notification and one pre-existing announcement were authored directly for demo purposes, not generated by another feature's trigger.

---

# Phase 3 — Finance, Reporting & Administration

### 3.1 Finance Management — ●

- **Loop step:** Done (steps 1–6 complete). UI + Mock (step 1) and visual sign-off (step 2) 2026-07-24; Contract + Wire Read + Wire Write (steps 3–5) same day; Test & Done (step 6) same day.
- **Screens:** `/finance` — 4 tabs (`Tabs` primitive): **Overview** (4 stat cards + `FinanceTrendChart` income-vs-expense bar + `ExpenseCategoryDonut` + Budget & Collection Summary card — folds build-plan's "financial dashboard" and "budget summary" into one tab since both read the same aggregate), **Payments** (paginated table + Record Payment modal), **Expenses** (paginated table + Record Expense modal), **Fee Types** (Admin CRUD table, usage-guarded delete)
- **Components:** `FinanceView`, `FinanceOverviewPanel`, `FinanceTrendChart`, `ExpenseCategoryDonut`, `PaymentsPanel`/`PaymentFormModal`/`PaymentStatusBadge`, `ExpensesPanel`/`ExpenseFormModal`, `FeeTypesPanel`/`FeeTypeFormModal` — all reuse existing kit primitives (`Card`/`Table`/`Modal`/`ConfirmDialog`/`FormField`/`Select`/`Pagination`/`Tabs`/`StatCard`), no new base components
- **Endpoints:** **All 9 live**, mounted at `/api/v1/finance` — `GET /summaries`, `GET /member-options` (Admin-only), `GET/POST /payments`, `GET/POST /expenses`, `GET/POST /fee-types`, `PUT/DELETE /fee-types/:id`. Reads (`finance:read`) are Admin + Executive Council; writes (`finance:write`) are Admin-only. Every stat/chart figure is computed live from `Payment`/`Expense` rows, not the schema's stored `FinancialPeriod.totalIncome/totalExpense/balance` columns (same simplification precedent as 2.2's unused `AttendanceSummary`).
- **Files:** `apps/web/src/app/(app)/finance/page.tsx` · `apps/web/src/features/finance/{types,constants}.ts` · `apps/web/src/features/finance/services/finance.service.ts` · `apps/web/src/features/finance/components/*.tsx` (9 files) · `apps/web/src/app/api/finance/{summaries,member-options,payments,expenses,fee-types,fee-types/[id]}/route.ts` · `apps/web/src/shared/utils/format-currency.ts` · `apps/api/src/modules/finance/{finance.types,finance.schema,finance.repository,finance.service,finance.controller,finance.routes,index}.ts` · `apps/api/tests/finance.test.ts` · `apps/web/src/shared/components/icons.ts` (added `IncomeIcon`/`ExpenseIcon`/`BalanceIcon`) · `apps/web/src/shared/components/ui/stat-card.tsx` (wrapping fix, see below)
- **Pre-build RBAC fix (confirmed with the user, 2026-07-24):** removed `finance:write` from Executive Council in both `roles.ts` files plus the live Neon `role_permissions` row — project-overview.md scopes Executive Council to read-only "Financial Monitoring" (view collections/expenses/balances/summaries); only Administrator gets "Record payments/Record expenses/Manage registration fees". Same class of drift found and fixed in 1.6/2.1/2.4.
- **3 real bugs caught and fixed during self-review** (before visual sign-off / before Test & Done): (1) `FinanceTrendChart` registering ChartJS's `Legend` plugin is a global registry, not per-chart — it silently turned on `ExpenseCategoryDonut`'s own built-in legend too, overlapping its custom HTML legend; fixed by explicitly setting `legend: {display:false}` in the donut's own chart options rather than relying on Legend never being registered anywhere on the page. (2) The step-1 "Empty" preview state passed `overview: null`, but `FinanceOverviewPanel` treats a `null` overview as "still loading" — showed a permanent skeleton instead of a real empty state; fixed with a genuinely-zeroed `EMPTY_FINANCE_OVERVIEW` fixture (empty arrays, zeroed stats, a real-but-zeroed period) so the panel's already-correct empty-state messaging actually renders. (3) Recording a payment refetched Payments + the Overview but not Fee Types, so the payment-count column that gates fee-type deletion (usage guard) went stale until a full page reload — fixed by also refetching Fee Types after a payment write.
- **Also fixed a pre-existing bug found in code inherited from the `event-form-modal.tsx`/`category-form-modal.tsx` pattern this feature's 3 form modals were built from**: `isSubmitting` is set `true` on submit but only ever reset to `false` on failure — a *successful* submit closes the modal without resetting it, so the same modal instance's Cancel/Save buttons render permanently disabled the next time it reopens. Fixed in finance's 3 modals (reset `isSubmitting` alongside the other per-open state in the `isOpen` effect). **Not fixed in 1.6/2.1's original modals** — out of scope for this feature, flagged here as a known follow-up if either screen is revisited.
- **Also caught a duplicate-toast bug during Test & Done**: `finance-view.tsx`'s fee-type handlers were showing a toast *and* `FeeTypesPanel` was already showing its own (matching 1.6's `CategoryPanel` precedent, where the panel owns create/update/delete toasts) — removed the duplicate from the view layer.
- **900px stat-card overflow (caught before visual sign-off):** at exactly 900px the 4-up stat-card grid made cards too narrow for long currency strings like "₱20,500" — the unbreakable string overflowed into the next card, a case dashboard/badges never hit since their stat values are short numbers/percentages. Fixed both generally (shared `StatCard` gained `min-w-0`/`break-words` as a safety net) and specifically (finance's stat grid uses `md:grid-cols-2 xl:grid-cols-4` instead of the usual `xs:grid-cols-2 lg2:grid-cols-4`, since 2 columns of long currency values need more room than 2 columns of short counts).
- **Verified:** `npm run typecheck`/`npm run lint`/`npm test` all clean in both workspaces (140 API tests passing, 15 of them `finance.test.ts` — member options, fee-type CRUD conflict/not-found/usage-guard, payment/expense list+create incl. unknown-member/unknown-fee-type, summaries stats/monthly-trend/category-breakdown incl. the no-period case). Playwright-verified against the real dev servers + seeded Neon DB as Administrator: real seeded fee types/payments/expenses render exactly; recorded a real payment (Faith Bermundo, Training Fee, ₱300) and a real expense — both landed instantly via refetch and the Overview's totals recalculated live; full Fee Type create → edit → usage-guarded-delete-blocked → real-delete cycle walked live and left the catalog in its original state. Confirmed Executive Council reads all 4 tabs with zero write controls anywhere. Backend curl-verified directly: unauthenticated → 401 on every route; Troop Leader → 403 on `GET /summaries`/`GET /payments` (no `finance:read` at all); Executive Council → 200 on reads, 403 on `POST /payments`\|`/expenses`\|`/fee-types` and `GET /member-options`; missing required fields on payment create → 422; unknown member/fee-type id → 400; duplicate fee-type name → 409; deleting an in-use fee type → 409; deleting an unknown fee type → 404.
- **Not built (deliberately out of scope):** payment/expense edit or delete — project-overview.md grants only "Record payments"/"Record expenses" for Administrator, no edit/delete verb; same append-only precedent as 2.3's activity reports. Multi-period selection (the schema's `FinancialPeriod`/`FinancialSummary` tables support it) — only one period is ever seeded (`FY 2026`), so `currentPeriod()` just takes the most recent row; revisit if a real second period is ever created.
- **Notes:** Mutations refetch after write, not optimistic (settled decision #3). Amount fields are Prisma `Decimal` — first feature to touch a Decimal-typed column; converted to `number` via `.toNumber()` at the service-layer DTO-mapping boundary, same place every other module normalizes its Prisma payload shape.
- **Bug fix — edit-form values reverting mid-edit (2026-07-26):** `fee-type-form-modal.tsx` had the same reset-`useEffect`-keyed-on-a-fresh-object bug found and fixed in 2.1's `EventFormModal`; same fix applied (reset only on the closed→open transition). See [2.1](#21-event-management--) for the full root-cause writeup. (`payment-form-modal.tsx`/`expense-form-modal.tsx` are create-only with no `initialValues` — not affected.)
- **Revision — Payments tab filtering (2026-07-26):** the Payments tab shipped with pagination only, inconsistent with every other paginated list in the app (Members, Events, Settings' Audit Log/Users all have search+filter bars). Added a filter bar matching `AuditLogPanel`'s established pattern: `SearchInput` (member name, 300ms debounced) + `Select` filters for fee type, payment method, and status — all server-side, page resets to 1 on any filter change. Isolated to `finance`'s own module (`finance.schema.ts`/`finance.repository.ts`/`finance.service.ts` on the API side, `finance.service.ts`/`finance-view.tsx`/`payments-panel.tsx` on the web side) plus a new pass-through test in `finance.test.ts` — no shared component or schema contract changed, so no other consumer needed re-verification. The fee-type filter's options come from the `feeTypes` state that was already fetched unconditionally for all roles (not the Admin-only `feeTypeOptions` used by the Record Payment modal), so Executive Council's read-only view gets a populated filter too — Playwright-verified live as both Administrator and Executive Council against the real dev servers + seeded Neon DB, including the debounced search, a status filter, the combined-filters no-match empty state, and the 480px wrap. `npm run typecheck`/`lint`/`test` clean in both workspaces (220 API tests, 1 new).

### 3.2 Reports — ●

- **Loop step:** Done (steps 1–6 complete). UI + Mock (step 1) and visual sign-off (step 2) 2026-07-24; Contract + Wire Read + Wire Write (steps 3–5) same day; Test & Done (step 6) same day.
- **Screens:** `/reports` — one screen for all 6 types: `ReportTypeSelector` (radio-card grid, pre-filtered by role/permission), date-range + optional Troop `Select` filter row, a generic preview panel (stat cards + data table — one renderer for all 6 types since the shape is already generic server-side), Export PDF/Export Excel actions, and a paginated "Report History" table reading the `Report` model.
- **Types:** Membership · Attendance · Badge · Financial · Activity · Executive — all 6 available to Admin/Executive Council; Troop Leader gets 4 (no Financial — no `finance:read`; no Executive — reserved for org-wide roles, same split as 1.5's dashboard). Resolved against actual seeded permissions rather than project-overview.md's inconsistent per-role prose (each role's own section lists a different subset) — documented in `reports.constants.ts`/`apps/api/.../reports.service.ts`, same "trust the permission grant" precedent as 2.2.
- **Components:** `ReportsView`, `ReportTypeSelector`, `ReportFiltersBar`, `ReportPreviewPanel`, `ReportHistoryPanel` — all reuse existing kit primitives (`Card`/`Table`/`Select`/`Input`/`Pagination`/`StatCard`/`EmptyState`/`ErrorState`/skeletons), no new base components. `ReportTypeSelector` is the one genuinely new interaction pattern — a radio-card grid (native `sr-only` inputs + styled labels, same technique as auth's `RoleSelector`), registered in ui-registry.md §5.
- **Endpoints:** **All 4 live**, mounted at `/api/v1/reports` — `GET /preview` (query: `reportType`/`dateFrom`/`dateTo`/`troopId?`), `GET /` (paginated history), `POST /export` (generates + persists a file, writes a `Report` row), `GET /:id/download` (streams the stored file). Reads (`reports:read`) are all 3 roles, with per-type access enforced in the service (`assertTypeAccess`) since it varies by report type rather than by route; export (`reports:export`) is Admin + Executive Council only, gated at the router same as finance's `canManage`.
- **Files:** `apps/web/src/app/(app)/reports/page.tsx` · `apps/web/src/features/reports/{types,constants}.ts` · `apps/web/src/features/reports/services/reports.service.ts` · `apps/web/src/features/reports/components/*.tsx` (5 files) · `apps/web/src/app/api/reports/{preview,export}/route.ts` · `apps/web/src/app/api/reports/route.ts` · `apps/web/src/app/api/reports/[id]/download/route.ts` (binary passthrough, not `proxyApiRequest` — that helper assumes a JSON envelope) · `apps/web/src/shared/components/icons.ts` (added `DownloadIcon`) · `apps/api/src/modules/reports/{reports.types,reports.schema,reports.constants,reports.repository,reports.service,reports.generators,reports.storage,reports.controller,reports.routes,index}.ts` · `apps/api/tests/reports.test.ts` · `.gitignore` (added `apps/api/storage/`)
- **First feature to generate and persist real files:** `pdfkit` (PDF) and `exceljs` (Excel) generate a buffer from the same generic `ReportPreviewDto`, written to `apps/api/storage/reports/<uuid>.<ext>` (gitignored, greenfield convention — no prior upload/storage code existed anywhere in the codebase) and referenced by the schema's pre-existing `Report.filePath` column. Download streams the file back with `Content-Type`/`Content-Disposition` headers rather than `res.sendFile`/`express.static`, so `requireAuth` + the per-type access check run before any bytes are served.
- **Report History is defense-in-depth, not just UI hiding:** `listHistory` filters `Report` rows to the requesting role's allowed types *server-side*, so a Troop Leader never sees a Financial/Executive export in their history — and `getDownload` independently re-checks type access even for a direct-by-id request, so a guessed/leaked report id still 403s. Verified live (see below), not just unit-tested.
- **Membership stats bucket to Active/Pending/Other, not the mock's Active/Pending/Inactive** — real seeded `MemberStatus` names are `pending`\|`active`\|`expiring`\|`expired`\|`archived`\|`rejected`, no literal "inactive" status exists, so the 4th stat card honestly lumps every non-active/non-pending status together rather than inventing a category. Badge preview only lists earned/verified records as rows (real `earnedAt` dates); "In Progress" is a separate date-independent count since `MemberBadge` has no `createdAt` to bound an in-progress record to a period. Executive's row table dropped the mock's fabricated "Trend" column — there's no historical baseline stored (`AnalyticsSnapshot` is schema-only, unused, same precedent as 2.2/3.1's other unused tables) to honestly derive a trend from.
- **900px stat-card overflow (caught before visual sign-off, same class of bug 3.1 hit):** the 4-up stat-card grid used the usual `xs:grid-cols-2 lg2:grid-cols-4` and squeezed "12" into "1"/"2" on separate lines at exactly 900px. Fixed with 3.1's exact precedent — `md:grid-cols-2 xl:grid-cols-4` — rather than re-discovering it.
- **Verified:** `npm run typecheck`/`npm run lint`/`npm test` all clean in both workspaces (156 API tests passing, 16 of them `reports.test.ts` — per-type access incl. Troop Leader blocked from financial/executive, membership status bucketing, attendance rate math + troop-scoping to events with at least one matching record, badge earned-vs-in-progress split, activity `submittedById` scoping, financial payment/expense merge+sort, history type-filtering by role, export writing a `Report` row before touching storage is skipped on a 403, download 404/403/happy-path). Playwright-verified against the real dev servers + seeded Neon DB as Administrator: all 6 types generate real previews from live data (12 real members with genuine mixed statuses incl. Expired/Rejected/Archived/Expiring, not just Active/Pending); exported the same Membership report as both PDF (`%PDF-` magic bytes confirmed) and Excel (`PK` zip signature confirmed, valid `.xlsx` content-type) — both landed as real rows in Report History with the real signed-in user as `generatedByName`. As Troop Leader (Liza Bagadiong): type selector correctly showed only 4 cards; attendance/badge/activity previews came back correctly troop-scoped (e.g. badge rows all real Troop 12 members, attendance numbers lower than Admin's org-wide view) while membership stayed unscoped (matching the members module's own existing behavior — it has no troop-leader filtering either). Backend curl-equivalent (via authenticated `fetch` in the browser) verified directly: unauthenticated → 401; Troop Leader `GET /preview?reportType=financial` → 403, `?reportType=executive` → 403, `?reportType=membership` → 200; Troop Leader `POST /export` → 403 regardless of type; a Financial report exported by Admin was **absent** from Troop Leader's `GET /reports` history and a direct `GET /reports/:id/download` by that same id → 403; `dateFrom` after `dateTo` → 422; unknown `reportType` enum value → 422; unknown report id on download → 404.
- **Not built (deliberately out of scope):** `ReportTemplate` (schema-only, unused — no feature in build-plan.md scopes custom/saved report templates); scheduled/recurring report generation (not in the Done gate, which only commits to on-demand preview+export).
- **Notes:** Mutations refetch after write, not optimistic (settled decision #3). Date-range filters use native `<input type="date">`, matching 1.3/2.1's precedent (`DatePicker` still `planned`, ui-registry.md §9). Every report type's preview is shaped generically (`{stats, columns, rows}`) so one `ReportPreviewPanel` and one PDF/Excel generator pair cover all 6 types without per-type markup or per-type export code.
- **Revision (2026-07-26, see Session Log):** Fixed a real Excel-content bug (numeric cells were written as shared strings instead of typed numbers — no sums/sort/right-align/number-format in Excel) and gave the PDF an actual styled design (branded banner, stat cards, bordered/banded table, page-numbered footer) — both in `reports.generators.ts`. Immediately after, Excel export was disabled "for now": `POST /export` only accepts `format: 'pdf'` (`exportFormatSchema`); the "Export Excel" button is removed. Reading/downloading previously-exported Excel `Report` rows still works (`reportFormatSchema`/`ReportFormat` type and `generateExcelBuffer` were left untouched) — only *new* Excel exports are blocked, reversibly.
- **Revision (2026-07-26, same session, see Session Log):** Added a 2-year (731-day, leap-year-safe) max date-range cap — no repository query behind any of the 6 report types is paginated, so an unbounded range pulled the whole table into memory before PDF/Excel generation ran. A 1-2 month cap was floated first but rejected: it would have rejected this feature's own default filter (`startOfYearIso()` → today) and the existing `reports.test.ts` `RANGE` fixture, both ~7 months. Enforced with a second zod `.refine()` (`MAX_REPORT_RANGE_DAYS = 731`) chained after the existing `dateFrom <= dateTo` refine on both `previewQuerySchema` and `exportSchema`, plus a mirrored frontend constant that constrains the date inputs' native `min`/`max` in `report-filters-bar.tsx` so the browser never offers an out-of-range combination in the first place.

### 3.3 Analytics (Executive) — ●

- **Loop step:** Done (steps 1–6 complete). UI + Mock (step 1) and visual sign-off (step 2) 2026-07-24; Contract + Wire Read (steps 3–4) same day — no Wire Write, feature is read-only; Test & Done (step 6) same day.
- **Screens:** `/analytics` — one page, 6 tabs (`Tabs` primitive): **Membership** (4 stat cards + registrations/month bar), **Attendance** (4 stat cards + attendance-rate%/month bar), **Participation** (3 stat cards + horizontal bar of registrants per event, most recent 8), **Badges** (3 stat cards + horizontal bar of % of members who earned/verified each catalog badge), **Financial** (3 stat cards + income-vs-expense/month bar), **Organization** (3 stat cards + member-count-per-troop bar + a Troop Performance table for attendance rate/badges earned, since those are different-unit metrics that can't share one axis with member counts).
- **Components:** `AnalyticsView` (single combined fetch drives all 6 tabs — read-only, no per-tab pagination/writes, so one shared `viewState`/`snapshot` pair, unlike Reports' per-type fetch), `MembershipTrendsPanel`/`MembershipTrendChart`, `AttendanceTrendsPanel`/`AttendanceTrendChart`, `ParticipationPanel`/`ParticipationChart`, `BadgeCompletionPanel`/`BadgeCompletionChart`, `FinancialTrendsPanel`/`FinancialTrendChart`, `OrganizationPerformancePanel`/`OrganizationPerformanceChart` — all reuse existing kit primitives (`Card`/`CardHeader`/`Tabs`/`StatCard`/`Table`/`EmptyState`/`ErrorState`/`ChartSkeleton`/`StatCardSkeleton`/`TableSkeleton`); no new base primitives. `ParticipationChart`/`BadgeCompletionChart` are the **first horizontal-bar** (`indexAxis:'y'`) charts in the app — event titles/badge names read better as row labels than crowded x-axis ticks.
- **Endpoints:** **1 live** — `GET /api/v1/analytics/overview`, returning all 6 sections in one payload. `analytics:read` is Admin + Executive Council (already correct in the seeded `ROLE_PERMISSIONS` — no pre-build RBAC fix needed, unlike 1.6/2.1/2.4/3.1). Every figure computed live from `Member`/`Event`/`EventRegistration`/`AttendanceRecord`/`Badge`/`MemberBadge`/`Troop`/`Payment`/`Expense` rows — the schema's `AnalyticsSnapshot` model stays unused, same "no stored snapshot" precedent as finance/reports.
- **Files:** `apps/web/src/app/(app)/analytics/page.tsx` · `apps/web/src/features/analytics/{types,constants}.ts` · `apps/web/src/features/analytics/services/analytics.service.ts` · `apps/web/src/features/analytics/components/*.tsx` (13 files) · `apps/web/src/app/api/analytics/overview/route.ts` · `apps/api/src/modules/analytics/{analytics.types,analytics.repository,analytics.service,analytics.controller,analytics.routes,index}.ts` · `apps/api/tests/analytics.test.ts` · `apps/api/src/routes/v1.ts`
- **2 real bugs caught and fixed during this session** (before visual sign-off / before Test & Done): (1) **ChartJS `Legend` global-registry leak** — all six chart components are statically imported on one page (`AnalyticsView` imports every panel unconditionally so tab-switching needs no refetch), so `FinancialTrendChart`'s `ChartJS.register(..., Legend)` leaked a stray "■ undefined" legend onto the other five charts; fixed by explicitly setting `legend:{display:false}` on all five, same bug class 3.1 hit with its donut. (2) **768px stat-card text fragmentation** — the original "Top Performing Troop" stat put a full troop name in `StatCard`'s large stat-value font; at 768px the 3-column grid was too narrow even for one word, and the shared `break-words` safety net (added for 3.1's currency strings) fragmented "Troop 12 — Virac" into unreadable chunks. Fixed by replacing it with "Top Attendance Rate" (numeric, e.g. "85%") — every other stat card in the entire app is a number/percentage, and the top troop's identity is already visible in the chart's tallest bar and the table's first row, so switching to a numeric stat avoids the whole bug class rather than patching around it.
- **Verified:** `npm run typecheck`/`npm run lint`/`npm test` all clean in both workspaces (162 API tests passing, 6 new in `analytics.test.ts` — membership stats+trend, attendance rate excluding no-attendance events, participation excluding no-registration events with rounded per-event average, badge completion rate incl. a real 0%-completion badge, financial stats+trend incl. a negative balance, per-troop performance incl. the org-wide top-attendance-rate stat). Playwright-verified against the real dev servers + seeded Neon DB: all 6 tabs render real data as Administrator (12 real members/8 active, 3 events held/86% avg rate, 20 real registrations across 2 events, 6 real badges incl. one genuine 0%-completion badge, a real negative council balance formatted correctly with a leading minus, and 3 real troops with per-troop attendance rates of 100%/100%/0%) — cross-checked against the dashboard's own numbers (12 members, 8 active) for consistency. Confirmed Executive Council reaches the same page read-only. Responsive re-confirmed at 900/768/480px with real data, including the stat-card fix. Backend curl-equivalent (via authenticated `fetch` in the browser) verified directly: unauthenticated → 401; Troop Leader → 403 (and separately bounced to `/dashboard` by the Next.js middleware before ever reaching the page); Admin and Executive Council → 200.
- **Done gate:** ✅ Analytics render from real data (curl-equivalent + Playwright, both against live Neon). ✅ Role-gated to Admin + Executive Council (403 for Troop Leader at the API, redirect at the middleware, and no nav entry at all for Troop Leader).
- **Not built (deliberately out of scope):** no date-range filter (build-plan.md's scope is a fixed "last 6 months" trend view, same as finance/dashboard's own monthly trend charts — no per-feature date picker was ever specified); no export (Reports already owns PDF/Excel export, including an "Executive" report type — Analytics is the interactive-chart counterpart, not a second export surface).
- **Notes:** No prototype baseline exists for this feature — `Gsp.html` has no analytics section at all — so the tab/stat-row/chart-card layout is composed from patterns already established by Finance/Reports/Badges/Dashboard rather than ported from anything. Every chart carries `role="img"` + a descriptive `aria-label` summarizing its data, per the established convention.

### 3.4 Settings & System Administration — ●

- **Loop step:** Done (steps 1–6 complete). UI + Mock (step 1) and visual sign-off (step 2) 2026-07-24; Contract + Wire Read + Wire Write (steps 3–5) same day; Test & Done (step 6) same day.
- **Screens:** `/settings` — one page, 4 tabs (`Tabs` primitive): **System Settings** (General/Membership/Notifications form sections, each mapped to real `system_settings` rows), **Users & Access** (Portal Users table with search/role filter/pagination, Add/Edit User modal, reset-password/activate-deactivate/delete actions), **Audit Log** (read-only paginated table over `audit_logs`), **Backups** (manual "Run Backup Now" + Backup History, both derived from the audit log — no stored backup-state row).
- **Components:** `SettingsView` (per-section fetch/state, no shared `viewState` — unlike Analytics' one combined fetch, Users/Audit Log each need their own page/search/filter-driven refetch, same as Members' directory), `SystemSettingsPanel`, `UsersPanel`/`UserFormModal`/`ResetPasswordResultModal`, `AuditLogPanel`, `BackupsPanel` — all reuse existing kit primitives (`Card`/`CardHeader`/`Tabs`/`Table`/`Badge`/`ActivityDot`/`ActivityItem`/`ToggleSwitch`/`Pagination`/`EmptyState`/`ErrorState`/`ConfirmDialog`/`FormField`/`PasswordInput`); reused `PasswordStrengthMeter` from `features/auth` in the Add-User modal (cross-feature component reuse, same precedent as dashboard's `MemberStatusBadge` import) rather than duplicating it. First screen to render `audit_logs` directly rather than as a backing table for another feature's writes.
- **Endpoints:** **11 live**, all `requireRole('admin')` only — `GET/PUT /api/v1/settings`, `GET/POST /api/v1/settings/backup`, `GET/POST /api/v1/users`, `PATCH /api/v1/users/:id` (profile + role reassignment together, matching the single Edit-User form), `PATCH /api/v1/users/:id/status`, `POST /api/v1/users/:id/reset-password`, `DELETE /api/v1/users/:id`, `GET /api/v1/audit-logs`. Matches project-overview.md exactly — System Administration and User Management are 100% Administrator-only, no Executive Council/Troop Leader responsibilities at all — so unlike 1.6/2.1/2.4/3.1 there was no pre-build RBAC correction needed; `users:manage`/`settings:manage`/`audit-logs:read` were already admin-only in both seeded `roles.ts` files.
- **Files:** `apps/web/src/app/(app)/settings/page.tsx` · `apps/web/src/features/settings/{types,constants}.ts` · `apps/web/src/features/settings/services/settings.service.ts` · `apps/web/src/features/settings/components/*.tsx` (7 files) · `apps/web/src/app/api/settings/{route,backup/route}.ts` · `apps/web/src/app/api/users/{route,[id]/route,[id]/status/route,[id]/reset-password/route}.ts` · `apps/web/src/app/api/audit-logs/route.ts` · `apps/api/src/modules/settings/{settings.types,settings.schema,settings.repository,settings.service,settings.controller,settings.routes,index}.ts` · `apps/api/tests/settings.test.ts` · `apps/api/src/routes/v1.ts` · `apps/web/src/shared/components/icons.ts` (added `PasswordIcon`/`BackupIcon`)
- **Real, honest simplifications (not deliberately-out-of-scope gaps):** Reset Password generates and returns a real bcrypt-backed temporary password shown once in a modal — there is still no email-sending service (open decision #7), so the admin relays it out of band, same non-blocking gap as 1.1's forgot-password. Backups has no real database-dump job — "Run Backup Now" writes a real `audit_logs` row (`backup.run`, `entityType: 'system'`) and the Backups tab derives "Last Backup"/history from that row rather than a fabricated status; called out on-screen via an inline `Alert` so this is never mistaken for a real backup job. Delete User is a real hard delete (every `User` FK elsewhere in the schema is `onDelete: SetNull`, so it's schema-safe) guarded by three real checks: can't delete/deactivate your own account, can't remove the last remaining Administrator (`assertNotLastAdmin`, counts the role excluding the target), can't delete a user who still leads a troop (409, "reassign those troops first").
- **1 real bug caught during Wire Read/Write verification against live Neon** (before Test & Done): `updateSystemSettings` and `runBackup` both originally wrote `entityType: 'system'` to `audit_logs`, so the Backups tab's history list (a plain `entityType` filter, no separate `action` filter) picked up System Settings saves and mislabeled them "Automated backup completed successfully". Fixed by giving `settings.update` its own `entityType: 'settings'`, reserving `system` for `backup.run` alone, and added a "Settings" entry to the Audit Log tab's record-type filter. **2 stale rows written by the pre-fix code during this session's own testing are still sitting in the live Neon `audit_logs` table** (mislabeled `entityType: 'system'`, real timestamp ~2026-07-24 20:32) — left in place at the user's explicit choice rather than a cleanup script; cosmetically these will keep showing as two extra "Automated backup completed successfully" entries in Backup History until deleted. Harmless (correct going forward, no correctness impact on the guard logic), but worth clearing out if the Backup History list is revisited.
- **Verified:** `npm run typecheck`/`npm run lint`/`npm test` all clean in both workspaces (184 API tests — 22 new in `settings.test.ts`: settings defaults/mapping/save+audit-log, backup info derivation from the audit log + no-real-job write, user list/create incl. duplicate-email conflict and real bcrypt hash verification, update incl. last-admin demotion guard and cross-user email conflict, status toggle incl. self-guard and last-admin guard, reset-password incl. hash verification, delete incl. self-guard/last-admin guard/led-troops conflict, audit log list + system-actor null mapping; 5 web tests unaffected). Playwright-verified against the real dev servers + seeded Neon DB as Administrator: System Settings read/saved the real seeded values (org name, 12-month term, 30-day window, email toggle) and reverted cleanly; Users & Access showed all 5 real seeded/prior-session users; ran a full real Add User → Reset Password (real generated+hashed temp password shown once) → Deactivate → Delete cycle on a throwaway "QA Test User" account, left the table back at its original 5; confirmed the currently-signed-in admin's own row has Deactivate/Delete genuinely disabled (not just visually — real click produces nothing, and the API independently 400s if forced); Audit Log showed all 6 real actions from the session (paginated, correct actor names, correct action labels/dot tones) alongside 1.4's pre-existing `member.approve`/`member.reject` rows; Backups showed "Never run" honestly until a real "Run Backup Now" click produced a real `Jul 24, 2026 · by Marisol Tabuena` entry. Backend curl-equivalent verified directly: unauthenticated → 401 on all three prefixes; Troop Leader → 403 on `/settings`, `/users`, `/audit-logs`; Executive Council → 403 on `/settings`, `/users`; Admin → 200 on all three; duplicate-email create → 409; reset-password on an unknown id → 404; self-delete and self-deactivate → 400 with an explicit message; `PUT /settings` with an empty org name and non-numeric fields → 422 with all four field errors.
- **Done gate:** ✅ Admin manages users (create/edit/activate/deactivate/reset-password/delete) and roles (assign on create/edit). ✅ Settings persist (real `system_settings` rows, confirmed round-tripped). ✅ Audit log shows critical actions (all of the above, plus 1.4's approve/reject, all visible on one real screen for the first time).
- **Remaining:** None. The 2 stale mislabeled audit rows (see bug note above) are cosmetic-only and left per user decision, not a Done-gate gap.
- **Bug fix — edit-form values reverting mid-edit (2026-07-26):** `user-form-modal.tsx` had the same reset-`useEffect`-keyed-on-a-fresh-object bug found and fixed in 2.1's `EventFormModal`; same fix applied (reset only on the closed→open transition). See [2.1](#21-event-management--) for the full root-cause writeup.
- **Notes:** No prototype baseline covers this combination — `Gsp.html`'s closest analog is `renderUsers` ("Users & Access" table + role-permission matrix + activity log card), which the Users & Access tab's layout and role-badge coloring follow closely; System Settings/Audit Log/Backups are composed from patterns already established by Finance/Reports/Badges/Analytics, same "no prototype, borrow sibling conventions" position 3.3 was in. First screen to build new modals that reset `isSubmitting` to `false` inside the `isOpen` reopen effect from the start (3.1's fix, not the still-unfixed original bug in 1.6/2.1's modals).
- **Revision — Backups tab hidden (2026-07-26):** user asked to hide the Backups tab "for now" rather than remove it, same reversible precedent as 3.2's Excel-export removal. Dropped the `{ id: 'backups', ... }` entry from `SETTINGS_TABS` (`constants.ts`, now 3 tabs) and its now-unused `BackupIcon` import; `SettingsTabId` still includes `'backups'`, and `settings-view.tsx`'s `BackupsPanel` render branch, `fetchBackups` effect, and the entire backend (`GET/POST /api/v1/settings/backup`, `settings.service.ts`/`settings.repository.ts`, tests) are untouched — the tab is simply unreachable from the UI, so restoring it later is just adding the tab entry and import back. `test-guide.md`'s Backups step marked skip-for-now. `typecheck`/`lint` clean on the web workspace (no test changes needed — no test asserted the tab list).
- **Revision — Email Notifications toggle hidden (2026-07-26):** same session, user asked to hide the Email Notifications toggle in System Settings "for now" too. Removed the whole "Notifications" `Card` from `system-settings-panel.tsx` (it contained only that one toggle) and its now-unused `ToggleSwitch` import, replacing it with a one-line comment marking why and how to restore it; `draft`/`values` still carry `emailNotificationsEnabled` unchanged (schema/DB/API untouched), so restoring the card is the only step needed. Worth noting for whoever re-enables it: the toggle was never actually wired to a real send path — announcements only ever write in-app `Notification` rows (`shared/utils/notify.ts`), never call `emailService` — so today it's cosmetic regardless of position. `test-guide.md`'s System Settings step updated to drop the toggle-check instruction. `typecheck`/`lint` clean on the web workspace.

### 3.5 Profile Management — ●

- **Loop step:** Done (6/6)
- **Screens:** `/profile` — gradient profile header (avatar initials/name/email/role pill) + Account Information card (editable Full Name/Email/Phone, read-only Role/Member Since) + Change Password card. Built with mock data first, reusing existing kit primitives only (`Card`, `CardHeader`, `FormField`, `Input`, `Alert`, `TableAvatar`, and `PasswordStrengthMeter` reused cross-feature from `features/auth`) — no new base components.
- **Components:** `ProfileHeader` (registry §3 "Profile header", now built) · `ProfileInfoForm` · `ChangePasswordForm` · `ProfileView`
- **Endpoints:** `GET/PUT /api/v1/profile` · `POST /api/v1/profile/change-password` — all three gated by `requireAuth` only, no `requireRole` at all (a first — every prior module had at least a role check beyond auth). Every signed-in role manages their own account here, distinct from 3.4's Administrator-only `UsersPanel` editing *other* users' accounts and roles.
- **Access:** All roles (own account only — routes take no `:id`, so there is no cross-user access surface to guard by construction)
- **Files:** `apps/web/src/features/profile/{types.ts,components/{profile-header,profile-info-form,change-password-form,profile-view}.tsx,services/profile.service.ts}` · `apps/web/src/app/api/profile/{route.ts,change-password/route.ts}` · `apps/web/src/app/(app)/profile/page.tsx` · `apps/api/src/modules/profile/{profile.types,profile.schema,profile.repository,profile.service,profile.controller,profile.routes,index}.ts` · `apps/api/tests/profile.test.ts`
- **Remaining:** None
- **Notes:** **Before building**, resolved the build-plan's "editable avatar" scope with the user: this app has no avatar upload or URL anywhere (every avatar — sidebar, tables, dashboard — is initials derived from the name), so the user chose display-only initials rather than adding upload/URL infrastructure as a first for this feature. `ProfileHeader` takes plain `fullName`/`email`/`role` fields rather than binding to either `AuthUser` or `ProfileSummary` directly — `ProfileView` feeds it the session's data until the real fetch resolves (so the header never needs its own loading state) and the freshly-saved profile after an edit. Contract + Wire Read + Wire Write built together, matching every prior Phase 3 feature. Email-conflict and current-password-incorrect errors render as an inline `Alert` on the relevant card (same precedent as 3.4's `UserFormModal`), not a toast, since both are field-relevant and shouldn't vanish before they're read; a successful save instead shows a toast. `updateProfile`/`changePassword` both write real `audit_logs` rows (`profile.update` / `user.change_password`, `entityType: 'user'`) so self-service edits show up in 3.4's Audit Log tab alongside admin-driven ones — the same table, no new wiring needed there. Added `apps/api/tests/profile.test.ts` (9 tests — DTO mapping, not-found, email-conflict incl. same-email-is-not-a-conflict, current-password verification incl. the wrong-password path leaving the stored hash untouched). All 193 API + 5 web tests pass, typecheck/lint clean in both workspaces. **Caught and fixed 2 real bugs during self-review**, before Playwright: (1) at 480px the read-only Role/Member Since row used `gap-x-4` (horizontal-only), so the two fields had no vertical gap once the grid collapsed to one column — fixed to `gap-4`, matching `SystemSettingsPanel`'s own grid pattern; (2) `ProfileHeader` originally bound to the session `AuthUser` alone, so renaming yourself left the gradient header — and the sidebar's own name chip, fed by the same stale server-rendered session — showing the old name until the next login; fixed by having `ProfileView` prefer the freshly-fetched profile once loaded, and calling `router.refresh()` after a successful profile save so the layout's server-side session read (and therefore the sidebar) picks up the change immediately, the same staleness `app-shell.tsx`'s `handleSignOut` already works around. Visually verified by the human at 1280/900/768/480px, mobile nav toggle, and all three states (ready/loading/error via a temporary mock-step preview switcher, since a single-record profile has no "empty" state to draw) — gate 2 passed, no changes requested. Playwright-verified against the real dev servers + seeded Neon DB: as Administrator, real seeded values rendered exactly (phone, the real 2026-07-22 `createdAt`); a real full-name edit round-tripped through Neon and updated both the header and the sidebar live via `router.refresh()`, then was reverted to leave the account as found; a real email-conflict against the seeded Executive Council account produced the inline "A user with this email already exists." alert; a real password change (verified via a genuine bcrypt round trip) succeeded and was immediately reverted to the original demo password so the shared demo account is unaffected; confirmed Troop Leader (Liza Bagadiong) reaches the identical page with her own real data. Backend curl-verified directly: unauthenticated `GET`/`PUT /profile` and `POST /profile/change-password` → 401 on all three; a real wrong-current-password attempt → 400 "Current password is incorrect." with the stored hash left untouched. **3.5 Profile Management is done — Phase 3 (Finance & Admin) is complete, 5/5. All 21 features across Phases 0–3 are now done.**

---

## Cross-Cutting Checklist

Verified continuously, re-checked at each phase exit ([build-plan.md](build-plan.md) §4):

| Concern                                                   | Status | Notes                                                                                                                             |
| --------------------------------------------------------- | ------ | --------------------------------------------------------------------------------------------------------------------------------- |
| RBAC enforced per route                                   | ◑      | `requireAuth`/`requireRole` live on `GET /auth/me` (1.2), all 7 members routes (1.3), the 3 approval routes (1.4, `members:approve` — same admin + executive council set as `members:archive`, curl-verified 403 for troop leader on list/approve/reject), `GET /dashboard` (1.5, `requireRole` of all 3 seeded roles since every role has a dashboard variant — curl-verified 401 unauthenticated), all 20 organizations routes (1.6 — reads open to all 3 roles, writes `requireRole('admin')` only, curl-verified 403 for Executive Council on a write), all 5 events routes (2.1 — reads open to all 3 roles, writes `requireRole('admin')` only after removing Executive Council's `events:write`, curl-verified 403 for Troop Leader on create), 2.2's 4 registrations/attendance routes (reads open to all 3 roles, writes `requireRole('admin','troop_leader')` — Executive Council curl-verified 403 on both `POST /registrations` and `POST /attendance`, read still 200), 2.3's 3 activity-reports routes (reads open to all 3 roles with service-layer scoping — Troop Leader sees only their own, Admin/Executive Council see all — writes `requireRole('admin','troop_leader')`, curl-verified 403 for Executive Council on `POST`, read still 200), and 2.4's 10 badges/member-badges/achievements routes (catalog CRUD `requireRole('admin')`; record/achievement-create `requireRole('admin','troop_leader')`; verify `requireRole('admin')`; reads service-scoped by troop for Troop Leader — curl-verified 403 for Executive Council/Troop Leader on catalog write, Executive Council on record, Troop Leader on verify, plus a 403 when a troop leader targets a real member outside their own troop), and 2.5's 5 notifications/announcements routes (notifications always scoped to `req.user!.id` — no role gate beyond `requireAuth`, curl-verified 404 when a troop leader targets an admin-owned notification id; announcements read open to all 3 roles, `POST` `requireRole('admin','executive_council')`, curl-verified 403 for troop leader, 201 for executive council), and 3.3's 1 analytics route (`requireRole('admin','executive_council')`, already correct in the seeded permission grant — curl-equivalent-verified 403 for Troop Leader, 401 unauthenticated, 200 for Admin/Executive Council, plus a Next.js middleware redirect to `/dashboard` for Troop Leader hitting `/analytics` directly), and 3.4's 11 settings/users/audit-logs routes (`requireRole('admin')` only — no `anyRole` tier at all, matching project-overview.md's System Administration/User Management being 100% Administrator-only — curl-verified 403 for both Troop Leader and Executive Council on all three prefixes, 401 unauthenticated, 200 for Admin), and 3.5's 3 profile routes (`requireAuth` only, no `requireRole` at all — a first, since every role manages their own account and there is no `:id` param for any other role to be denied access to — curl-verified 401 unauthenticated on all three, real access confirmed for both Administrator and Troop Leader). Phase 1 + all of Phase 2 + 3.3 + 3.4 + 3.5 are covered; 3.1/3.2's routes exist and are RBAC-gated (see their own feature entries) but predate this row's last update. Next.js middleware still UX-layer only (unverified token decode) |
| Validation (frontend + backend)                           | ●      | Backend: auth (5 schemas) + members (`memberType`-discriminated create/update, query, renew, approve/reject) + events (create/update with an end-after-start time `.refine`) + registrations/attendance (uuid member/event ids, `present`/`absent`/`registered`/`cancelled` enums) + activity-reports (uuid `eventId`, required `summary`, plus a service-layer "event must be completed" check independent of Zod) + badges (badge name/points/requirements, uuid member/badge ids, `in_progress`/`earned` status enum, plus service-layer troop-ownership and earned-before-verify checks independent of Zod) all Zod, live on real routes; `ZodError` → 422 with field details (curl-verified for a too-short rejection reason, `endTime` before `startTime`, a missing `memberId`, a bad attendance-status enum value, a missing activity-report `summary`, and a missing badge `badgeId`). + notifications (none — no user-writable fields) + announcements (required title/content, optional expiry) all Zod, live on real routes; `ZodError` → 422 with field details (curl-verified for a too-short rejection reason, `endTime` before `startTime`, a missing `memberId`, a bad attendance-status enum value, a missing activity-report `summary`, a missing badge `badgeId`, and empty announcement title/content) + settings/users (organization-name/numeric-membership-field/email-toggle on system settings, full-name/email/role/8-char-password on user create, email-conflict and last-admin-demotion guards independent of Zod on user update) all Zod, live on real routes; `ZodError` → 422 with field details (curl-verified for a too-short rejection reason, `endTime` before `startTime`, a missing `memberId`, a bad attendance-status enum value, a missing activity-report `summary`, a missing badge `badgeId`, empty announcement title/content, and an empty system-settings organization name with non-numeric membership fields) + profile (required full name, valid email, min-8-char new password, plus a service-layer current-password-verification check independent of Zod) all Zod, live on real routes; `ZodError` → 422 with field details, and a real duplicate-email `PUT /profile` → 409 curl-verified. Frontend: `FormField` renders field errors on login/signup/member-form/approval-review/event-form/activity-report-form/badge-form/record-badge-form/achievement-form/announcement-form/user-form/system-settings-form/profile-info-form/change-password-form, incl. every role/type-specific field |
| Centralized error handling                                | ●      | Singleton `asyncHandler` + one `errorHandler`; `ApiError`, `ZodError` and Prisma P2002/P2025 mapped. Covered by tests             |
| Password hashing + secure storage                         | ●      | `bcryptjs` in the seed and in `authService.login`/`signup` (`shared/utils/password.ts`) — real bcrypt hash/compare, no plaintext ever persisted |
| Audit logging on critical actions                         | ●      | `audit_logs` table live — 1.4's approve/reject were the first writers (`shared/utils/audit-log.ts`); 3.4 adds `settings.update`, `user.create`/`update`/`activate`/`deactivate`/`reset_password`/`delete`, and `backup.run`, and is the first screen to render the table directly as a real Audit Log tab (paginated, filterable by record type) rather than only being a backing table. Confirmed live in Neon for every one of those actions |
| Responsive at 900px / 768px / 480px                       | ◑      | Mobile-first with a custom `lg2: 900px` stop. **Human-verified 2026-07-22** on the gallery + shell; re-checked per feature screen (1.3 directory + profile, 1.4 queue + review modal, 1.5's Admin variant, 2.1's list/calendar/modal, 2.3's report table + both modals, 2.4's stat cards/tabs/catalog grid/progress table/achievement table, 2.5's announcement feed + notification dropdown, 3.5's profile header/account form/password form, all at 900/768/480px, re-confirmed with real data). Caught and fixed a real 480px bug in 3.5 — the read-only Role/Member Since row used `gap-x-4` instead of `gap-4`, leaving no vertical gap once the grid collapsed to one column. Caught and fixed a real 768px bug in 2.4 — the page-level action button was squeezing the tab strip instead of wrapping below it. 2.5's notification panel additionally checked at 360px per its own registry note (fixed 320px width) — no overflow |
| Mobile nav reachable (toggle + backdrop + scroll lock)    | ●      | All four prototype defects fixed in `AppShell`/`Sidebar`. **Human-verified 2026-07-22**                                           |
| Loading / empty / error states                            | ●      | `TableSkeleton`, `CardSkeleton`, `ChartSkeleton`, `EmptyState`, `ErrorState` built (shown in `/gallery`) **and now wired to a real feature** — 1.3's directory/profile drive them from genuine fetch state, not a demo toggle |
| Accessibility (labels, focus, keyboard modals)            | ◔      | Focus trap + restore, scroll lock, Escape, skip link, `aria-live` toasts, labelled controls. No audit run yet                     |
| WCAG AA contrast (no `#aaa` text, no white-on-gold)       | ●      | `#aaa` not ported; gold buttons use `#8a7500` (4.54:1). Both failures resolved at the token layer                                 |
| Semantic landmarks (`nav`/`main`/`header`, heading order) | ●      | Real landmarks + skip link; `h1` in topbar, `h2` per page, `h3` per card                                                          |
| Empty / loading / error states on every list              | ●      | 1.3's Membership Registry and 1.4's Pending Approvals queue both drive empty/loading/error from genuine fetch state, not a demo toggle |
| Pagination on tables that can grow                        | ●      | `Pagination` built, unit-tested, and wired to ten real server-paginated lists/grids (1.3 members, 1.4 pending queue, 2.1 events, 2.3 activity reports, 2.5 notifications, 2.5 announcements, 3.1 payments, 3.1 expenses, 3.4 portal users, 3.4 audit log). **Documented gap:** 2.4's Member Progress/Achievement History tables are unpaginated — deliberate, matches what was visually verified (small, troop-scoped lists); revisit if org-wide membership grows enough for the all-troops Admin/EC view to matter |
| Unit tests on service layer                               | ●      | 193 API + 5 web tests passing: health service, error handler, `asyncHandler`, `buildPageRange`, `authService` (14 tests), `membersService` (18 tests — list/getById/create/update/archive/restore/renew/listPending/approve/reject, incl. 2 new notify-side-effect cases), `dashboardService` (4 tests — admin aggregation, council read, troop-leader roster + the "no troop assigned" empty case), `organizationsService` (21 tests — duplicate-name conflicts, unknown-council/unknown-leader rejections, and delete-conflict guards across all 5 resource types), `eventsService` (22 tests — status derivation, troopNames/organizer/category mapping incl. unassigned, category/organizer validation on create/update, not-found on get/update/delete, plus 2.2's `listParticipants`/`registerParticipant`/`updateRegistrationStatus`), `attendanceService` (4 tests — unknown event, not-registered, cancelled-registration rejection, successful upsert), `activityReportsService` (8 tests — troop-leader scoping, admin/executive-council unscoped read, troop-name mapping, not-found on get, unknown-event/not-yet-completed/success paths on create), `badgesService` (18 tests — catalog CRUD conflict/not-found, progress-read scoping and aggregation, record-badge unknown-member/out-of-scope/unknown-badge/success, verify not-found/not-earned/success incl. 2 new notify-side-effect cases, achievement out-of-scope/success), `notificationsService` (5 tests — list scoping/pagination, mark-read ownership check, mark-all-read), `announcementsService` (3 tests — DTO mapping incl. null postedBy, create fan-out excluding the poster), `financeService` (15 tests — member-option mapping, fee-type CRUD conflict/not-found/usage-guard, payment/expense list+create incl. unknown-member/unknown-fee-type, summaries stats/monthly-trend/category-breakdown incl. the no-period case), `analyticsService` (6 tests — membership stats+trend, attendance rate excluding no-attendance events, participation excluding no-registration events with rounded per-event average, badge completion rate incl. a 0%-completion badge, financial stats+trend incl. a negative balance, per-troop performance incl. the org-wide top-attendance-rate stat), `settingsService` (22 tests — system-settings default-fallback/mapping/save+audit-log, backup-info derivation from the audit log incl. the no-backup-yet case, user list/create incl. duplicate-email conflict and a real bcrypt-hash verification, update incl. last-admin-demotion guard and cross-user email conflict, status toggle incl. self-guard/no-op/last-admin guard, reset-password incl. hash verification, delete incl. self-guard/last-admin guard/led-troops conflict, audit-log list incl. the system-actor null-name case), `profileService` (9 tests — DTO mapping, not-found on get/update/change-password, email-conflict incl. same-email-is-not-a-conflict, current-password verification via a real bcrypt round trip incl. the wrong-password path leaving the stored hash untouched) |

---

## Open Decisions

| #   | Decision                                                                                                                                       | Status                | Blocks |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------- | --------------------- | ------ |
| 1   | Icons: `react-icons` (emoji prototype replaced)                                                                                                | ✅ Settled 2026-07-22 | —      |
| 2   | Charts: `react-chartjs-2` as ChartJS wrapper                                                                                                   | ✅ Settled 2026-07-22 | —      |
| 3   | Mutations: refetch after write, not optimistic                                                                                                 | ✅ Settled 2026-07-22 | —      |
| 4   | Dashboard role from session/JWT, never client param                                                                                            | ✅ Settled 2026-07-22 | —      |
| 5   | Role model: relational `user_roles` + `role_permissions` authoritative; no role column on `users`; one role per user enforced in service layer | ✅ Settled 2026-07-22 | —      |

| 6 | Breakpoints: **mobile-first**, `min-width` only, custom `lg2: 900px`. No `max-*` variants | ✅ Settled 2026-07-22 | — |
| 7 | Email-sending service (provider, shared `EmailService` location, which feature builds it out) — added to [build-plan.md](build-plan.md) §7 2026-07-23 after noting 1.1's `forgot-password` sends no real email | ✅ Settled 2026-07-24 | — Built as `apps/api/src/shared/utils/email.ts`, SMTP via `nodemailer` (works with any provider — Gmail/SendGrid/Mailtrap/SES's SMTP interface), folded into 1.1 as its own follow-up per this decision's own note. Falls back to logging (`jsonTransport`) when `SMTP_*` env vars are unset, so dev/test never need real mail credentials. 2.5's notifications stay in-app only — unaffected, since nothing there currently needs real delivery. |
| 8 | `ADMIN_SIGNUP_KEY` hardening ahead of a real deploy — raised 2026-07-26 while explaining how the key works in production, alongside that session's Executive Council/Troop Leader self-signup approval-gate fix (see 1.1). Three gaps, none blocking: (a) **no rate limiting anywhere in the API** — `grep`-confirmed none exists — so the key is brute-forceable via repeated `POST /auth/signup` if it's short; mitigated for now only by choosing a long random value, not by any app-level throttling. (b) **no audit log entry on admin self-signup** — unlike the new pending-approval path (`user.signup_pending`/`user.activate`), a successful admin signup writes nothing to `audit_logs`, so there's no record of which admin accounts came from the shared key. (c) **it's a single non-rotating shared secret** — anyone holding it can mint unlimited Admins until the env var is changed and the API redeployed, and rotating it does not retroactively affect admins already created with the old value. | ✅ Settled 2026-07-26 | (a) `express-rate-limit`-based `shared/middleware/rate-limit.middleware.ts`, IP-keyed in-memory (correct for Render free tier's single instance), wired per-route at 50 requests/minute each on `/auth/signup`, `/auth/login`, `/auth/forgot-password`, `/auth/reset-password` (loosened twice on 2026-07-26 — first from a 10-20/15min split to 15/min, then to 50/min — so the client's own manual QA pass doesn't trip it; at 50/min this is a safety net against scripted abuse, not real brute-force protection, so tighten it back down before a public launch); `/auth/refresh` left unlimited (needs a valid refresh token already, rotates legitimately every access-token expiry). Skips in `NODE_ENV=test` so it can't rate-limit the suite's own shared `app` singleton. (b) admin self-signup now writes `writeAuditLog({ action: 'user.signup', entityType: 'user', entityId, details: { role: 'admin' } })`, mirroring the existing `user.signup_pending`/`user.create` convention. Both covered by new/updated tests in `auth.test.ts` and `rate-limit.test.ts`. (c) — the shared-secret model — kept as-is per human decision 2026-07-26: mitigated by (a)+(b), accepted as a risk for a small deployment rather than reworked into an approval queue (which has a first-Admin bootstrapping problem). Rotate `ADMIN_SIGNUP_KEY` periodically in production. |

**All 21 features across Phases 0–3 are done (●). All 8 tracked decisions are settled.**

---

## Blockers

| Date       | Feature   | Blocker                                                                                                                                                                                                                                                                                                                                                  | Owner | Resolved                                                           |
| ---------- | --------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----- | ------------------------------------------------------------------ |
| 2026-07-22 | 0.5       | No PostgreSQL instance available — schema validates and the client generates, but no migration has been applied and the seed has not run. Nothing downstream is blocked until feature 1.1 needs to read real data.                                                                                                                                       | Human | ✅ 2026-07-22 — Neon connected, `init` migration applied, seed run |
| 2026-07-22 | 0.5       | **Prisma tooling version mismatch — cosmetic, editor-only.** The Prisma VS Code extension's language server is a Prisma **7** build and flags `datasource.url` in `schema.prisma` as unsupported. The project is pinned to Prisma **6.19.3**, where that line is correct and required — `validate`, `migrate` and `generate` all pass. Not a schema bug. | Human | ✅ 2026-07-22 — resolved by upgrading the project to Prisma 7.9.0  |
| 2026-07-22 | 0.2 / 0.3 | Feature Loop gate 2 needs a human to view `/gallery` and the shell at 900 / 768 / 480px. Phase 1 should not start until this passes.                                                                                                                                                                                                                     | Human | ✅ 2026-07-22 — passed at all three breakpoints                    |
| 2026-07-26 | 1.1 | **Forgot-password emails never arrive when deployed on Render, only locally — root cause confirmed via Render logs, fixed with a new Gmail API send tier.** User confirmed Render uses `SMTP_HOST` (Gmail) and pasted the actual thrown error from Render's logs: `Error: Email send timed out after 10000ms` — our own `withTimeout` race won, meaning nodemailer's `sendMail()` never resolved *or* rejected within 10s. That rules out a fast auth/refusal error and points at the TCP connection to `smtp.gmail.com:587` hanging with no response — a silent drop somewhere in Render's egress path (or Gmail distrusting Render's IP range), not a credentials problem. User declined Mailtrap (its shared sending domain only delivers to the account owner's own inbox) — but that restriction is universal to every transactional-email HTTP API (SendGrid, Resend, Postmark, SES, Brevo all require a verified sending domain to reach arbitrary recipients), not Mailtrap-specific, and the user has no domain to verify. Resolved by adding a **Gmail API (OAuth2) send tier** to `email.ts` — sends as the user's real Gmail account over HTTPS (`https://gmail.googleapis.com/gmail/v1/users/me/messages/send`), so it's immune to whatever is silently dropping outbound port-587 traffic, and — because it's the user's own mailbox, not a bulk-sender-as-a-service — has no recipient-verification restriction, exactly like sending from Gmail's web UI. Checked first in `send()`'s priority chain (Gmail API → Mailtrap → SMTP → dev-only `jsonTransport` log fallback), so local dev (which has `SMTP_HOST` set and already works) is unaffected until the new vars are actually set. New optional env vars `GMAIL_CLIENT_ID`/`GMAIL_CLIENT_SECRET`/`GMAIL_REFRESH_TOKEN` (schema in `env.ts`, setup walkthrough in `.env.example`, declared with `sync: false` in `render.yaml`). New dependency `google-auth-library` (token refresh only; the send call itself is a raw `fetch`, no full `googleapis` SDK needed). `apps/api/src/shared/utils/email.ts`, `apps/api/src/config/env.ts`, `apps/api/.env.example`, `render.yaml`, `apps/api/package.json`. `npx tsc --noEmit`, lint, and the full API suite (226/226, `auth.test.ts`'s mocked `emailService` cases unaffected) all clean. **Not yet live-verified** — sending requires real Gmail OAuth2 credentials (Google Cloud OAuth client + a one-time-generated refresh token for the sending Gmail account) that only the user can create; this session has no way to obtain or test them. | Human | ✅ 2026-07-26 — OAuth2 credentials generated and live-verified: a local test script (fetch → refresh token exchange → `messages.send`, no secrets written to any file, deleted after use) sent a real email to `aquinovernel69@gmail.com` and it arrived. Two setup snags hit and corrected along the way, both worth remembering if this is ever redone: (1) the OAuth Playground requires a **Web application** OAuth client with `https://developers.google.com/oauthplayground` registered as an Authorized redirect URI — a **Desktop app** client (my initial, wrong guidance) has no redirect-URI field and throws `redirect_uri_mismatch`; (2) the account authorizing the app must be added under the Audience tab's **Test users** list while the app is in Testing publishing status, or authorization throws `Error 403: access_denied`. Also hit repeated transcription errors (client ID/secret retyped from a screenshot instead of copy-icon'd, silently swapping characters like `l`/`I` and `0`/`o`) — worth copy-icon'ing credentials directly rather than retyping if this ever needs redoing. User still needs to set `GMAIL_CLIENT_ID`/`GMAIL_CLIENT_SECRET`/`GMAIL_REFRESH_TOKEN`/`EMAIL_FROM` (address must be `aquinovernel69@gmail.com`, Gmail rewrites any other From address) on Render's dashboard and redeploy — not yet done as of this session. |
| 2026-07-24 | 3.4       | **Cosmetic only, non-blocking.** 2 real `audit_logs` rows in live Neon were mislabeled `entityType: 'system'` — written by this session's own pre-fix code before `settings.update` was moved to `entityType: 'settings'` (see 3.4's bug note). They showed as 2 extra "Automated backup completed successfully" entries in the Backups tab's history list. | Human | ✅ 2026-07-24 — user asked for the cleanup after all; ran a one-off script (`apps/api/scripts/fix-audit-log-entity-type.ts`, deleted after use) against live Neon relabeling both rows (`4440af89…`, `c3bb8c42…`) to `entityType: 'settings'`. Backups tab history is now accurate. |

---

## Session Log

Newest first. One entry per work session — what was done, where it stopped, what's next.

| Date       | Worked on                                 | Outcome                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Next step                                                                                                                                                                                                     |
| ---------- | ----------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-07-26 | **Bug fix: whole app rendered "zoomed out" on real mobile phones (deployed site), app-wide (cross-cutting, not a single feature).** | User-reported with 4 real iPhone screenshots (`/members`, `/attendance`, `/badges` Member Progress) against `https://gsp-mis-web.vercel.app`, all showing the same signature: content filling only ~65–70% of the screen width with a blank margin on the right, rather than the page filling the device width. Static code audit (subagent) across `/reports`, `/members`, `/attendance`, `/badges` found no width-overflow culprit in those features' own code — all tables correctly use `TableWrapper` (`overflow-x-auto`), no fixed-px chart widths (no charts on these 4 pages at all), all grids mobile-first. Live Playwright reproduction at a 375px viewport also showed `document.body`'s width exactly matching the viewport on all 4 pages — no reproducible overflow in Chromium's emulation either. Root cause only became clear from the real device screenshots plus one more signal: `document.documentElement.scrollWidth` (686) was consistently wider than `document.body.scrollWidth` (370) even though body itself measured correctly — traced to `Sidebar` (`sidebar.tsx:55-64`), the app-wide off-canvas mobile nav rendered on every authenticated page. It hides below `md` purely via `transform: translateX(-100%)` while its own layout box stays `fixed left-0 w-sidebar` (220px wide, untransformed). This is a well-documented Mobile Safari bug: Safari's "ideal viewport width" (shrink-to-fit) calculation reads fixed-position elements' *untransformed* layout position, not their painted/transformed one — so the off-canvas panel, though invisible, still gets counted as on-screen content needing room, and the whole page renders zoomed out to fit it. Not isolated to the 4 reported pages — it affects every authenticated route, since `Sidebar` lives in the shared `AppShell`; the user just happened to test those 4. Fixed by adding `overflow-x: hidden` to `html` in `globals.css` (the standard remedy for this exact WebKit quirk — it removes the horizontal-overflow signal Safari's calculation keys off, without needing to change the sidebar's own transform-based slide animation). **First attempt was wrong and self-caught**: also setting `overflow-x: hidden` on `body` (seemed symmetric/safer) silently broke the sticky topbar header app-wide — per the CSS Overflow spec, setting `overflow-x` on an element forces its own computed `overflow-y` from `visible` to `auto` if not set explicitly, and doing that on `body` (which is not normally a scroll container) turned it into an independent nested scrolling box, detaching `position: sticky` descendants from the real viewport scroll. Caught by live-testing scroll behavior with Playwright before shipping it, not by inspection. Scoped the fix to `html` only (already the actual scrolling element per `document.scrollingElement`, so its own `overflow-y` computing to `auto` there is a no-op) — re-tested scroll-with-sticky-header (now stays pinned, matches pre-fix behavior), the off-canvas nav opening/closing (unaffected), and the desktop layout at 1280px (unaffected, screenshot-verified). `apps/web/src/app/globals.css` only. `npx tsc --noEmit`, `next lint`, and the full web test suite (5/5) all clean. **Not yet confirmed on the user's real phone against the redeployed site** — the fix targets a well-documented, widely-cited WebKit behavior and every locally-verifiable signal (the `html`-vs-`body` scrollWidth mismatch, matching the sidebar's own width) points at it, but this environment has no way to load Safari on a real device, so treat as strong-confidence-not-certainty until the user reloads on their phone. | Redeploy, then have the user reload `/members`, `/attendance`, `/reports`, and `/badges` (Member Progress) on the same iPhone that showed the bug. If it's still zoomed out, the next thing to try is replacing `Sidebar`'s `-translate-x-full` with an explicit `left: -220px`/`left: 0` transition instead of `transform` (slower, layout-triggering, but immune to this specific Safari quirk since it changes the untransformed box position directly) — not done preemptively since it trades away compositor-only animation performance and the `overflow-x: hidden` fix is the standard remedy that should not require it. |
| 2026-07-26 | **Bug fix: forgot-password "Send Reset Link" spun forever after deploying to Render (1.1).** | User-reported (worked locally, hung after deploy). Root cause: `authService.forgotPassword` (`auth.service.ts`) `await`s `emailService.sendPasswordResetEmail` before responding, and the try/catch around it only handles a *rejection* — a *hang* is neither a resolve nor a reject, so it blocks the HTTP response indefinitely. `email.ts`'s nodemailer SMTP transport had no `connectionTimeout`/`greetingTimeout`/`socketTimeout` set, so if `SMTP_HOST` is configured (e.g. Gmail) and the deploy host's egress silently drops outbound SMTP packets instead of refusing them — a known behavior on Render's free plan and similar PaaS hosts, which is exactly why this codebase already has a Mailtrap HTTP-API tier as an alternative — the connection attempt hung on nodemailer's default `socketTimeout` (10 minutes), which reads to the user as "keeps spinning indefinitely" since the frontend `ForgotPasswordModal` has no client-side abort/timeout either and just awaits the fetch. Fixed at the actual root: added a 10s `EMAIL_SEND_TIMEOUT_MS` + `withTimeout()` wrapper around both the Mailtrap and SMTP send paths in `email.ts`, plus matching `connectionTimeout`/`greetingTimeout`/`socketTimeout` on the nodemailer transporter itself, so a blocked/slow network now fails fast and is actually caught by the existing try/catch — the request returns the generic "If that email is registered…" message promptly either way, matching the feature's documented best-effort-delivery intent. `apps/api/src/shared/utils/email.ts` only; no schema/contract change. `npx tsc --noEmit` clean, full API suite 226/226 passing (`auth.test.ts`'s 21 mock-based cases unaffected), lint clean. **Not yet re-verified against the live Render deploy** — the underlying cause (SMTP egress being blocked/slow on Render) is inferred from the code + known PaaS behavior, not confirmed via Render logs, since this session has no access to the live Render dashboard/env vars. | Redeploy and retest forgot-password against the live Render URL. If it's still slow (now bounded to ~10s instead of hanging forever, but still not instant), set `MAILTRAP_API_TOKEN` on the Render service instead of relying on `SMTP_HOST` — the Mailtrap HTTP-API tier isn't subject to SMTP-port egress restrictions. |
| 2026-07-26 | **Deploy scaffolding: Render web-service config + auth hardening from decision #8, then a Render build fix.** | `render.yaml` added (root-level build so the npm-workspaces lockfile resolves, `db:generate`/`db:migrate:deploy`/`build` chained in `buildCommand`, health check on `/api/v1/health`), plus a `db:migrate:deploy` script in `apps/api/package.json` for running Prisma migrations in production. Same commit folded in open decision #8's rate-limiting + audit-log fixes (see Open Decisions #8 above) and a `trust proxy` fix in `apps/api/src/app.ts` so `express-rate-limit` reads the real client IP from `X-Forwarded-For` behind Render's reverse proxy instead of rate-limiting the proxy itself. Committed as `6a6481d`. Immediately after, a live Render build failed: `npm ci` inherits `NODE_ENV=production` during Render's build step and omits devDependencies, so `tsc` had no declarations for `typescript`/`prisma`/`@types/*` (cors, express, etc.) and the build broke. Fixed by adding `--include=dev` to `render.yaml`'s `npm ci`. Committed as `70f295b`. Not yet confirmed against a live Render deploy in this session. | If deploying: watch the next Render build for a clean `tsc` pass, then confirm `GET /api/v1/health` on the deployed URL reports `database: up`. Otherwise nothing queued — all 21 features and all 8 decisions are done/settled. |
| 2026-07-26 | **Bug fix: deleting the last user on a non-first Users & Access page showed "no users" instead of returning to the previous page (3.4).** | User-reported (manual testing). Root cause: `SettingsView.handleDeleteUser` refetched at the unchanged `userPage` after a delete, so deleting the sole row on page 2+ made the server return an empty page while `userPage` stayed put — `UsersPanel` rendered that as its empty state rather than navigating back. `usePagedItems` already auto-clamps this for client-side-paginated tables, but the server-paginated Users tab had no equivalent. Fixed in `fetchUsers` (`settings-view.tsx`): after a fetch, if `userPage` exceeds the freshly computed `totalPages`, clamp `userPage` down and return early — the existing `useEffect` on `fetchUsers` picks up the change and refetches, landing on the last page that still has users. `npx tsc --noEmit` clean on `apps/web`. | Committed and pushed to `origin/master`. Nothing else queued. |
| 2026-07-26 | **Docs: synced `user-guide.md`/`test-guide.md`/`user-guide.pdf` to the self-signup approval-gate revision.** | `context/user-guide.md` §2.1 rewritten to explain Troop Leader/Executive Council signups now land inactive (Administrator signup unchanged), §5.13 notes the existing Activate button doubles as signup approval, new FAQ entry for "I signed up but can't log in," version bumped to 1.1/2026-07-26. `context/test-guide.md` §1.1's manual test steps rewritten to walk signup → blocked login → Admin activates via Users & Access → login succeeds, with downstream references (§3's leader-picker step, §14's audit log check) adjusted to match. `context/user-guide.pdf` (a static export, last generated 2026-07-25, no build-time regeneration step exists for it) was stale against the new §2.1/§5.13/FAQ text, so regenerated from the current `user-guide.md`: no pandoc/wkhtmltopdf available in this environment, so installed `marked` + `playwright-core` in the scratchpad (not a project dependency) and rendered the markdown through a small HTML template styled with the app's real token colors (`green` #1a6b3c, `gold` #c8a900, `goldInk` #8a7500) via headless Chromium's `page.pdf()`. Visually verified the underlying HTML in-browser before printing (screenshot-checked the title block and the new §2.1 callout box) rather than trusting the PDF blind, since this environment can't render PDF pages back for review. Side effect caught and self-resolved: `npx playwright install chromium` pulled a newer Chromium (1234) than the Playwright MCP server's cached one (1228) and pruned the old revision as "unused," which briefly broke the MCP browser mid-session (`Target page, context or browser has been closed`) — it silently relaunched itself against the new 1234 build on the next call with no further action needed, re-confirmed against a real page. | Committed as `302f038` and pushed to `origin/master`. |
| 2026-07-26 | **Revision (`revise-feature`): self-signup security gate for Executive Council/Troop Leader (1.1).** | User asked whether anyone could self-signup and modify data — audit found yes: self-signup for both non-admin roles was fully open, `isActive` defaulting `true` with tokens issued immediately, no verification of the affiliation the signer typed in. Fixed by landing new Executive Council/Troop Leader accounts `isActive: false` (no session) until an Administrator activates them from the existing 3.4 Users & Access panel — no new screen built, since that panel's Active/Inactive badge + Activate/Deactivate action already covers exactly this. Admin signup unchanged (its `ADMIN_SIGNUP_KEY` gate stands). `SignupResponseBody`/`SignupResponse` became a discriminated union (`active` with tokens vs. `pending` with a message); login intentionally still returns the same generic "Invalid email or password." for a pending account, preserving the existing no-enumeration convention. New `user.signup_pending` audit log entry. Full details under [1.1](#11-authentication-). `npm run typecheck`/`lint`/`test` clean both workspaces (225 API + 5 web tests). Live-verified end-to-end via Playwright against the real dev servers + seeded Neon DB: signup → pending toast, no cookies, login blocked with correct password; Admin found the account Inactive in Users & Access, activated it, saw the audit entry; login then succeeded with a real session. Test account deleted afterward. | Committed as `a796cca` and pushed to `origin/master`. |
| 2026-07-26 | **Revision (`revise-feature`): real reset-password email delivery (SMTP/Mailtrap) + branded email template (1.1).** | User reported never receiving reset-password or announcement emails. Investigation confirmed both were working as designed, not broken: `SMTP_HOST` was unset so `email.ts` was logging instead of sending (documented dev behavior since 1.1's 2026-07-24 follow-up), and announcements were never emails at all — only in-app `Notification` rows. Walked through provider setup live: Gmail app passwords blocked twice on the user's first account (2FA/Workspace-policy related, not fixable from this repo), then Mailtrap's HTTP API worked but its demo domain only delivers to the exact email the Mailtrap account was signed up with (confirmed via a real `403 MailtrapError` against a different recipient), then landed on real Gmail SMTP with a second personal account — which then hit a `self-signed certificate in certificate chain` TLS error traced to Avast's Mail Shield (not Web Shield) intercepting the SMTP connection; disabling it in Avast's Core Shields settings fixed it, confirmed local-only (no bearing on any real deploy target). Added `mailtrap` (`^4.8.0`) to `apps/api`, a new optional `MAILTRAP_API_TOKEN` env var, and a 3-tier `send()` in `email.ts` (Mailtrap API → SMTP → dev-log fallback). Then styled the reset-password email for real: new `apps/api/src/shared/utils/email-templates.ts` (table-based, fully inline-styled for Outlook compatibility — green gradient banner, gold accent divider, gold CTA button, muted footer, matching `tokens.ts`'s palette). Full details under [1.1](#11-authentication-). Never printed the pasted Mailtrap token or Gmail app password anywhere but the gitignored `.env` (confirmed via `git check-ignore`). `npm run typecheck`/`npm run lint`/`npm test` clean in both workspaces (223 API tests unaffected — `auth.test.ts` mocks `emailService`). Live-verified: real `POST /forgot-password` triggered after each provider swap, confirmed by server-console output and, once Gmail SMTP worked, by checking the real inbox for the styled email. | Committed as `5ad8f8d` and pushed to `origin/master`. |
| 2026-07-26 | **Revision (`revise-feature`): hid the Email Notifications toggle in System Settings (3.4).** | Same session as the Backups-tab hide below, user asked to hide this too. Removed the whole "Notifications" `Card` from `system-settings-panel.tsx` (it held only that one toggle) and its now-unused `ToggleSwitch` import; `emailNotificationsEnabled` stays in the type/schema/DB untouched, so restoring is just adding the card back. Noted for later: the toggle was never wired to a real send path anyway — announcements only write in-app `Notification` rows. `test-guide.md` updated to drop the toggle-check step. Full details under [3.4](#34-settings--system-administration-). `npm run typecheck`/`npm run lint` clean on the web workspace. | Committed as `5ad8f8d` and pushed to `origin/master`. |
| 2026-07-26 | **Revision (`revise-feature`): hid the Settings Backups tab (3.4).** | User asked to hide the Backups tab "for now" rather than remove it — same reversible-removal precedent as this session's Reports Excel-export decision. Dropped the `backups` entry from `SETTINGS_TABS` (`constants.ts`) and its now-unused `BackupIcon` import; left `SettingsTabId`, `BackupsPanel`, `settings-view.tsx`'s fetch/render logic, and the whole backend (`settings.service.ts`/`settings.repository.ts`, `GET/POST /api/v1/settings/backup`, `settings.test.ts`) untouched, so restoring the tab later is just adding the entry back. `test-guide.md`'s Backups manual-test step marked skip-for-now. Full details under [3.4](#34-settings--system-administration-). `npm run typecheck`/`npm run lint` clean on the web workspace; no test changes needed since no test asserted the tab list. | Not yet committed — commit and push when the user asks. Nothing else queued. |
| 2026-07-26 | **Revision (`revise-feature`): added a max date-range cap to Reports (3.2).** | No repository query behind any of the 6 report types was paginated (`listPaymentsInRange`, `listMembersInRange`, etc.), so an unbounded date range would pull the whole table into memory before PDF/Excel generation ran. Discussed the cap size with the user first — a 1-2 month cap was floated and rejected because it would have broken the feature's own default filter state (`reports-view.tsx` defaults to `startOfYearIso()` → today, currently ~7 months) and the existing `reports.test.ts` `RANGE` fixture (also ~7 months). Settled on 2 years, and specifically **731** days not 730 — verified with a quick day-math check that a literal Jan-1-to-Jan-1 two-year span crosses one leap day (e.g. `2024-01-01`→`2026-01-01` is 731 days), so 730 would have rejected an honest "2 years." Implemented as a second zod `.refine()` chained after the existing `dateFrom <= dateTo` refine on both `previewQuerySchema` and `exportSchema` (`reports.schema.ts`, exported `MAX_REPORT_RANGE_DAYS`), mirrored on the frontend as a `MAX_REPORT_RANGE_DAYS` constant in `reports/constants.ts` that constrains the date inputs' native `min`/`max` in `report-filters-bar.tsx` so the browser never offers an out-of-range combination. Added 4 new schema-level unit tests (`reports.test.ts`, via `.safeParse` — same precedent as `organizations.test.ts`'s schema tests) covering the feature's own default range, the leap-year boundary case, one day past the cap, and the export schema sharing the same cap. | Isolated to the Reports module — no shared UI kit, Prisma model, or cross-feature component touched. `npm run typecheck`/`npm run lint`/`npm test` all clean in both workspaces (223 API tests — 20 in `reports.test.ts`, up from 16 — 5 web tests). No further steps — awaiting the next user-reported item. |
| 2026-07-26 | **User-reported bug fix + revision (`revise-feature`): Excel report content and PDF styling (3.2), then Excel export option removed.** | Bug: `reports.generators.ts`'s Excel export wrote every cell (including currency/percentage/counts) as a shared string — confirmed by unzipping a generated `.xlsx` and finding `t="s"` on every `<c>`, so Excel couldn't sum/sort/right-align any numeric column and flagged them as "number stored as text." Added `parseCell()` to reclassify `"₱1,500"`/`"67%"`/`"42"` back into real typed numbers with `numFmt`, applied to both Summary and Detail sheets; also added header fill/bold, thin borders, frozen header row, and an autofilter. PDF had no styling at all (plain black `Helvetica` text, no table lines) — redesigned with the brand green banner, gold-accented stat cards, a bordered/banded Detail table with per-column alignment, empty-state row, and a footer with page numbers across multi-page tables. Verified by generating real sample files via `tsx` and inspecting the raw `.xlsx` XML (`t="s"` gone from numeric cells, correct `numFmt`/fill/border in `styles.xml`) and the PDF's decompressed content stream (correct RGB fill/text ops) — no rasterizer available in this environment to screenshot the PDF directly. Immediately after, user asked to remove the Excel export option "for now": restricted `POST /export`'s `format` to a new `exportFormatSchema` (`z.enum(['pdf'])`, separate from the existing `reportFormatSchema` which stays `'pdf'\|'excel'` for reading/downloading already-exported Excel `Report` rows), removed the "Export Excel" button, and added a frontend `ExportFormat = 'pdf'` type threaded through `reports-view.tsx`/`report-preview-panel.tsx`/`reports.service.ts` — deliberately not touching the DB's `report_format` enum or `reports.generators.ts`'s `generateExcelBuffer` so re-enabling later is just widening two type aliases back. | Isolated to the Reports module (`apps/api/src/modules/reports/*`, `apps/web/src/features/reports/*`) — no shared UI kit or cross-feature component touched, no `ui-registry.md` update needed. `npm run typecheck`/`npm run lint`/`npm test` all clean in both workspaces (219 API tests, 5 web tests). No further steps — awaiting the next user-reported item. |
| 2026-07-26 | **Revision (`revise-feature`): added search + filter bar to the Finance Payments tab (3.1).** | User asked whether Payments should have filtering; investigation showed it shipped with pagination only, unlike every other paginated list in the app. Added a server-side filter bar (debounced member-name search + fee type/method/status `Select`s) matching `AuditLogPanel`'s established pattern (3.4). Isolated to finance's own module on both sides; fee-type filter options come from the already-unconditional `feeTypes` fetch so Executive Council's read-only view gets a populated filter too, not just Admin. Full details under [3.1](#31-finance-management-). `typecheck`/`lint`/`test` clean both workspaces (220 API tests, 1 new). Playwright-verified live as both Administrator and Executive Council against the real dev servers + seeded Neon DB. | Nothing queued from this revision. |
| 2026-07-26 | **User-reported bug fix: Announcement notifications carried only the post's title, never its content (2.5).** | User asked why `/notifications` couldn't show a notification's full details. Investigation found `announcementsService.create` (`announcements.service.ts`) called `notifyUsers(recipientIds, 'New announcement posted', created.title)` — the notification's message field held the announcement's own **title** a second time, never its **content**, and `/notifications` has no click-through to the source post (confirmed the `Notification` model has no `sourceType`/`sourceId` at all), so the actual announcement text was unreachable from the notification. Fixed by swapping to `notifyUsers(recipientIds, created.title, created.content)` — the notification's headline is now the real title and its body the real content. Updated `announcements.test.ts`'s call-shape assertion to match. Separately answered a follow-up design question (no code change): recommended against adding a per-notification detail modal, since the row already renders full untruncated text — a modal would only be justified once notifications carry a real source link to navigate to, which is a bigger, undiscussed feature. No feature's Loop step changes — bug fix to an already-done (●) feature. `tsc --noEmit` clean, all 218 API tests pass. Live-verified: posted a real announcement as Admin, confirmed as Troop Leader Liza Bagadiong that her `/notifications` row showed the real title + real content (older pre-fix rows, e.g. "EC Test Announcement", correctly still show the old title-only shape — this only changes new writes, not historical data). | Committed as `f6f4f6b` and pushed to `origin/master`. Nothing else queued. |
| 2026-07-26 | **User-reported bug fix: Record Badge / payment member pickers silently excluded `expiring` members (2.4, 3.1).** | User asked why a member's "Expiring Soon" status wasn't offered in the Badges Record Badge member picker. Investigation found `listMemberOptions` in both `badges.repository.ts` and `finance.repository.ts` filtered strictly to `status: { name: 'active' }` — an explicit shared convention (finance's own comment cited badges' as precedent) that silently dropped any member whose renewal is due within 30 days, even though `expiring` is still a current, participating member by every other measure in the app (`member-profile-view.tsx`'s `canRenew` already treats `active`/`expiring`/`expired` as equally reachable). Confirmed this contradicted the project's own `test-guide.md:479`, which already documented Liza's Record Badge picker as expected to list "Althea, Cristina, Elena" — Cristina Ople is exactly the seeded `expiring` member. Asked the user for a recommendation call rather than assuming; recommended and applied including `expiring` alongside `active` in both repositories (`status: { name: { in: ['active', 'expiring'] } }`), leaving `pending`/`expired`/`archived`/`rejected` excluded. No feature's Loop step changes — bug fix to two already-done (●) features. `tsc --noEmit` clean, all 218 API tests pass (existing suites use mocked repositories, so none asserted the old single-status filter). Live-verified via Playwright as Administrator: Record Badge's member picker now lists "Cristina Ople — Troop 12 — Virac" alongside the rest. | Committed as `f6f4f6b` and pushed to `origin/master`. Nothing else queued. |
| 2026-07-26 | **User-reported bug fix: Badges Edit modal's Save button not disabled with zero changes (2.4) — cross-cutting fix across 8 modals once audited.** | User reported that opening Edit on a badge in `/badges` left "Save Changes" clickable immediately, before any field was touched. Root cause: `BadgeFormModal` tracked `values` (reset on the closed→open transition via the existing `wasOpen` ref) but had no baseline/dirty state at all — the submit `Button` was only ever disabled by `isSubmitting`. Fixed by adding a `snapshot` state set at the same reset point as `values`, computing `isDirty = JSON.stringify(values) !== JSON.stringify(snapshot)`, and changing the button to `disabled={!isDirty || isSubmitting}` — the same pattern already used correctly in `system-settings-panel.tsx`/`profile-info-form.tsx`. User then flagged that other edit modals had the same problem; a read-only audit (`Explore` subagent) checked all 20 modal/form-modal files and found 7 more edit-capable modals with the identical copy-pasted gap — `troop-form-modal.tsx`/`council-form-modal.tsx`/`category-form-modal.tsx` (1.6), `fee-type-form-modal.tsx` (3.1), `user-form-modal.tsx` (3.4), `member-form-modal.tsx` (1.3), `event-form-modal.tsx` (2.1) — and 6 create-only modals (payment/expense/announcement/record-badge/achievement/activity-report) plus `renew-membership-modal` where a dirty-check doesn't apply (no `mode`/`initialValues` baseline to diff against), left untouched by design. Applied the identical fix to all 7; `member-form-modal.tsx`'s reset also folds in its Troop Leader `restrictToTroopId` override, so the snapshot was set from that same computed `resetValues`, not the raw `initialValues`. No feature's Loop step changes — bug fixes to already-done (●) features. `npm run typecheck` clean on the web workspace. Live-verified via Playwright against the real dev server: Badge Catalog's "Camp Cook" Edit and Troops' "Troop 4 — Bato" Edit both now render Save disabled on open and enable it the instant a field changes; both cancelled without saving. | Committed as `f6f4f6b` and pushed to `origin/master`. Nothing else queued. |
| 2026-07-26 | **Two user-reported bug fixes: Members archive/restore losing the pre-archive status (1.3), and Events edit-form values reverting mid-edit (2.1) — the latter a cross-cutting fix across 8 modals.** | **(1) Archive/restore:** user reported archiving a member with a non-`active` status, then restoring, always landed on `active`. Root cause: `membersService.restore` unconditionally set `active` — nothing ever recorded the prior status. Fixed by adding `Member.preArchiveStatusId` (migration `20260726013133_add_member_pre_archive_status`, applied live to Neon), stashed on archive and consumed+cleared on restore (falls back to `active` only if nothing was stashed); archive is now a no-op if already archived. Full details under [1.3](#13-membership-management-). **(2) Events edit-form revert:** user reported editing an event twice in a row — the second edit's typed value reverted ~500-1000ms after typing. Root cause: `EventFormModal`'s reset `useEffect` was keyed on `[isOpen, initialValues]`, but the parent recreates `initialValues` as a fresh object every render, so any unrelated re-render while the modal stayed open (here, the previous save's background refetch resolving) re-fired the reset and clobbered the user's typed input. Fixed by gating the reset on the closed→open transition via a `wasOpen` ref. While tracing it, found the identical copy-pasted pattern in 7 other edit-capable modals (members, troops, categories, councils, badges, users, fee types) and applied the identical fix to all of them — full list and root-cause writeup under [2.1](#21-event-management-); one-line cross-references added to [1.3](#13-membership-management-)/[1.6](#16-organization-management-)/[2.4](#24-badges--achievements-)/[3.1](#31-finance-management-)/[3.4](#34-settings--system-administration-). Both fixes verified: full test suite (218 tests) + typecheck clean both workspaces; the events fix additionally live-reproduced and confirmed against the real dev server + seeded Neon DB (edit → save → immediately reopen → type → 900ms wait → value held), with the test event's title restored afterward. No feature's Loop step changes — both are bug fixes to already-done (●) features. | Nothing queued. Build plan remains 21/21 done, no open blockers. Resume when new work is scoped. |
| 2026-07-26 | **Bug fix: My Troop Roster unbounded height (1.5) — plus a `test-guide.md` documentation expansion.** | User manually testing the app flagged the Troop Leader dashboard's My Troop Roster card growing taller than its "Membership by Scout Level" sibling for a large troop. Fixed by capping it `max-h-[360px] overflow-y-auto` — the same pattern already used for the notification panel. Also expanded `context/test-guide.md`'s manual-test steps for the two most recent revisions (1.6 one-troop-per-leader, 1.3 Membership Registry troop-scoping): added the leader-picker exclusion check for an already-assigned leader (Grace Tapel) and the conflict-error path, split the registry's create/read/scope-guard steps out per troop leader (Liza on Troop 12, Grace on Troop 4, Admin on Troop 7) instead of one generic pass, and added the unassigned-leader edge case (a freshly self-signed-up Troop Leader sees a "no troop assigned yet" placeholder and can't submit a registration until linked). No feature's Loop step changes — both are fixes/documentation on already-done (●) features. `npm run typecheck` clean. | Nothing queued. Build plan remains 21/21 done, no open blockers. Resume when new work is scoped. |
| 2026-07-25 | **Cross-cutting UI revision: remove the duplicated page headers and the topbar's dead search bar — 0.3 App Shell + 15 feature pages.** | User flagged that every page showed 2–3 stacked headings repeating the same title (topbar `<h1>` → each page's `<PageHeader>` `<h2>` → often the main card's own `<CardHeader>` `<h3>`), and asked to remove the topbar's never-wired search input too. Asked one clarifying question first since this reverses a pattern spanning every page and previously documented in ui-rules.md §9 — user chose to keep the topbar's `<h1>` as each page's one header. Removed `<PageHeader>` from all 15 real feature `page.tsx` files (left alone on `/gallery`, QA tooling, and the already-dead `PlaceholderPage`); made `CardHeader`'s `title` optional and dropped it on the 3 pages where it was a literal duplicate of the topbar text (Membership Registry, Activity Reports' admin/EC view, Announcements) — a self-review catch found and fixed 2 of those that an initial subagent audit had missed, and separately caught that `/notifications`' CardHeader title was *not* a duplicate (that route isn't in `nav-items.ts`, so its topbar shows a generic fallback) and correctly left it alone. Removed the topbar's disabled `SearchInput`. Full rationale, exact file list, and the deliberately-scoped known gap (heading levels not renumbered after `PageHeader`'s removal) recorded under [0.3](#03-app-shell-). 215 tests pass unchanged (presentational only), typecheck/lint clean. Live-verified across Admin and Troop Leader on Members/Organizations/Dashboard/Activity Reports/Announcements/a detail page, plus responsive at 480/900/1280px. | Nothing queued from this revision. The known gap (CardHeader levels not bumped from h3→h2 now that PageHeader's h2 is gone) is noted in ui-rules.md §9 for a future accessibility pass. |
| 2026-07-25 | **Same-day follow-up (`revise-feature`): swap the Troop column for a header badge (Troop Leader) and add a Troop filter (Admin/EC) — 1.3.** | Direct continuation of the troop-scoping revision earlier the same day. Since Troop Leader's directory now only ever shows one troop, the per-row Troop column was redundant for that role — removed it and put the troop name in a `Badge` next to the "Membership Registry" title instead. Admin/EC keep the column and gained a "Filter by Troop" dropdown (same pattern as the existing status/type filters), wired through an additive optional `troopId` on the web service's `ListMembersParams` — verified the backend needed zero changes, since `listMembersQuerySchema`/`buildWhere`'s `troopId` support and its `AND`-based scope-composition were already built in the prior revision. Confirmed isolated (only `page.tsx` imports `MemberDirectory`; `attendance-view.tsx`'s unrelated `listMembers()` call is unaffected by the new optional param). Full details appended to [1.3](#13-membership-management-)'s existing revision entry. 215 tests still pass unchanged, typecheck/lint clean. Live-verified: Troop Leader's page now shows a "CAT-VIR-012 — Troop 12 — Virac" badge and no Troop column at 480/900/1280px; Admin's Troop filter live-filtered real seeded data to exactly one troop's members. | Nothing queued from this follow-up. |
| 2026-07-25 | **Revision (`revise-feature`): scope Troop Leader to their own troop — 1.3, list/read/create/update/renew.** | User asked whether a Troop Leader should be able to edit another troop's member on `/members`. Investigation found no — the answer was already "yes" org-wide by accident: identical role middleware on every route, zero ownership check in the service/repository, and no troop filter on the list query at all. User chose full scoping (list + edit + create, extended during implementation to also cover the equally-unscoped `getById`/`renew`, since leaving those open would have defeated the fix). Followed the `scopeToOwnTroop`/`requireMemberInScope` convention already established in the Badges module (2.4) rather than inventing a new pattern. Web side locks and pre-fills the Troop field in the register/edit form for a Troop Leader so the UI doesn't offer a choice the API would just reject. Full details under [1.3](#13-membership-management-). 12 new tests, 215 total pass. Live-verified: Liza Bagadiong's directory now shows only her own 6-member troop, her own edits still round-trip, and direct `fetch()` attempts against another troop's member return real `403`s on read, update, and renew; Admin unaffected (still sees all 13 members). | Nothing queued from this revision. Related, still-open gap noticed but not touched: `1.4`'s approval queue and `2.x` attendance/activity-reports modules were not audited for the same troop-scoping question in this pass — worth a follow-up sweep if Troop Leader access to those is ever questioned. |
| 2026-07-25 | **Revision (`revise-feature`): enforce one troop per leader — 1.6, blast radius reviewed across dashboard/badges/reports/settings/members.** | User asked whether a Troop Leader could lead multiple troops in the Troops tab, and if so which troop a multi-troop leader's dashboard would show. Investigation found no constraint at any layer, and a real inconsistency: Dashboard's `findFirst`-based lookup would silently pick one arbitrary troop while Badges/Reports already aggregated correctly across all of a leader's troops. User chose to enforce one-troop-per-leader. Found the live Neon DB already had this exact conflict (Grace Tapel on 2 troops, one of them 0-member leftover test data); user confirmed clearing the stale one before migrating. Added `Troop.leaderId @unique` (migration `20260725093000_troop_leader_id_unique`, applied to Neon), a service-layer conflict guard on create/update (409, current-troop-excluded on edit), and a UI filter on the leader picker. Full details under [1.6](#16-organization-management-). 203 API + 5 web tests pass; live-verified via Playwright + direct fetch (UI excludes assigned leaders, forced API reassignment returns real 409, self-resave returns 200). | Nothing queued from this revision. Separately still open from the same investigation but **not yet acted on**: the earlier uncommitted manual-testing bug-fix batch (see the entry directly below) is still sitting uncommitted, and 1.3's Members directory has no troop-leader scoping at all (a pre-existing, unrelated gap noticed during the blast-radius grep, not touched here). |
| 2026-07-25 | **Uncommitted manual-testing bug-fix batch (checkpointed only, not committed).** Found via `context/test-guide.md` (new, replaces the deleted `context/test-data.md`). | Working tree has 5 fixes across already-done (●) features, verified green (`typecheck` clean, 200 API + 5 web tests pass) but **left uncommitted at the user's request** so they can review the diff first: (1) **Stuck-submit-spinner bug** — all 10 form modals (member, troop, council, category, badge, achievement, event, announcement, activity-report, renew-membership) now reset `isSubmitting` to `false` on close/reopen; previously submitting then reopening left the button stuck on "Saving…". (2) **Missing pagination added** to 8 tables via a new shared hook `apps/web/src/shared/hooks/use-paged-items.ts` (`usePagedItems`, own passing unit test): Troops, Councils, Categories, Fee Types, Achievement History, Member Progress, Report Preview, Organization Performance (analytics) — note several of these panels' own entries elsewhere in this file already list `Pagination` among their reused primitives even though it was not actually wired into the table markup until now; those descriptions were aspirational, not accurate, until this fix. (3) **`JWT_EXPIRES_IN` 15m → 3h** in both `apps/api/.env.example` and `env.ts`'s default — 15-minute sessions were expiring mid manual-test-session. (4) Copy fix in `MemberFormModal`: pending-review note now credits "an Administrator or Executive Council member" instead of only Executive Council, matching who can actually approve. (5) `context/test-data.md` deleted in favor of the more complete `context/test-guide.md`. No feature's Loop step changes — these are bug fixes to done features, not new scope. | **Not committed on purpose** — user chose to checkpoint only and review the diff themselves before deciding whether to commit. Touched files: `apps/api/.env.example`, `apps/api/src/config/env.ts`, `apps/web/src/shared/hooks/use-paged-items.ts` (new), plus the 10 form-modal + 8 panel components named above, plus untracked `.claude/settings.json` (accumulated permission allowlist, not feature work) and `context/test-guide.md`. Resume by reviewing `git status`/`git diff`, then commit when ready. |
| 2026-07-25 | **Revision (`revise-feature`): badge-category icons — 1.6 + 2.4, then extended to Member Progress + Record Badge.** | User asked whether badge icons should get a selector; recommended putting the icon on `BadgeCategory` rather than `Badge` (same category = same glyph is what makes a grid scannable; per-badge picking across a curated set just yields arbitrary near-duplicates) and they approved. Blast radius classified **shared** before any edit: `BadgeCategory` (organizations + badges modules), and web's `CategoryPanel`/`CategoryFormModal`/`CategoryItem`, which serve all three lookup tabs. Added `badge_categories.icon` (`@default("award")`, migration `20260725051325_add_badge_category_icon`, applied to Neon), backfilled the 5 seeded categories by targeted `UPDATE` rather than a full re-seed, and threaded the key through schema→repo→service→DTO on both sides. New `IconPicker` shared primitive (12 Feather keys; `sr-only` radios, `RoleSelector` technique). Icon support is behind an opt-in `showIcon` prop so the two sibling tabs are untouched. **Both sibling consumers re-verified in the browser, not assumed** — Scout Levels' and Activity Categories' tables and modals render exactly as before. Full round trip proven live: picker → PUT → Neon → catalog card. In the same reply I flagged, without being asked, that the catalog was now visually inconsistent with 2.4's other two badge-by-name screens (Member Progress detail modal, Record Badge modal) — user confirmed and asked for it same session. Threaded `badgeCategoryIcon` onto `MemberBadgeRecordDto`/`MemberBadgeRecord` for the detail modal's per-row swatch; Record Badge's badge field is a native `<select>` (icons can't render inside `<option>`), so it got a live preview swatch beside the select instead. Deliberately left the Member Progress summary table and Achievement History alone — neither has a per-badge identity to hang a glyph on. `typecheck`/`lint`/`test` clean both workspaces throughout, 200 API + 5 web tests. | Nothing queued. Build plan remains 21/21 done, no open blockers. Per-badge icon override is a one-column follow-on if it's ever wanted — the `badge.icon ?? category.icon ?? default` fallback chain is already the shape of `resolveBadgeCategoryIcon`. |
| 2026-07-24 | **Non-blocker cleanup: fixed the 2 mislabeled `audit_logs` rows from 3.4.** | User asked to clear the cosmetic blocker left open at the end of the last session. Wrote a one-off script (`apps/api/scripts/fix-audit-log-entity-type.ts`) using the real `prisma` singleton to find rows with `action: 'settings.update'` AND `entityType: 'system'`, confirmed exactly the 2 expected rows (`4440af89…`, `c3bb8c42…`), relabeled both to `entityType: 'settings'` via `updateMany`, then deleted the script. Backups tab's history list in `/settings` no longer shows the 2 extra phantom "Automated backup completed successfully" entries. Blockers table entry marked resolved. No code changes — this was a data-only fix; the write path (`settings.service.ts`) has been correct since 3.4. | Nothing queued. Build plan remains 21/21 done, all decisions settled, no open blockers. Resume when new work is scoped. |
| 2026-07-24 | **Post-completion follow-up: resolved open decision #7 (email-sending service) as a 1.1 follow-up.** | With all 21 features done, the user asked to implement open decision #7. Ran it through the Feature Loop as a slice of 1.1 rather than a new numbered feature (it completes 1.1's own documented "Known gap"), since it adds one real screen: **Step 1 (mock)** — built `/reset-password` (`ResetPasswordCard`) with 3 states (set-new-password form / invalid-or-expired link / success), reusing `Alert`/`Button`/`FormField`/`PasswordInput`/`PasswordStrengthMeter` — no new base components. Added `/reset-password` to `middleware.ts`'s public-path allowlist (same treatment as `/login`) after discovering the page 307-redirected to `/login` without it. **Step 2** — visually verified by the human at 1280/900/768/480px across all 3 states (self-checked first via Playwright screenshots) — gate 2 passed, no changes requested. **Step 3 (Contract)** — `resetPasswordSchema`/`ResetPasswordInput` (Zod) and matching frontend types. **Steps 4–5 (Wire Read + Write)** built together: added a `PasswordResetToken` model + migration (`token_hash` unique, `expires_at`, `used_at` for single-use); built the shared `EmailService` (`apps/api/src/shared/utils/email.ts`) via `nodemailer` — SMTP transport works with any provider, falls back to `jsonTransport` (logs instead of sending) when `SMTP_HOST` is unset so dev/test need no real credentials; `forgotPassword` now generates a random 32-byte token, hashes it (same sha256-not-bcrypt reasoning as refresh tokens), invalidates any prior outstanding tokens for that account, and emails a `${WEB_APP_URL}/reset-password?token=...` link (1-hour expiry) — email delivery is wrapped in try/catch and only logged on failure, since surfacing it to the caller would itself leak whether the address exists, breaking the same no-enumeration guarantee the generic response already protects; new `resetPassword` service validates the token, updates the password, marks the token used, and **revokes every refresh token on the account** (stronger than 3.5's self-service `changePassword`, since a reset implies the account was inaccessible or compromised). New public route `POST /auth/reset-password` (6th public auth route, joining the other 5 — a token, not a session, is the credential here). Frontend: `resetPassword()` added to `auth.service.ts`, new BFF proxy route, `ResetPasswordCard` wired for real — invalid/expired/reused tokens surface the API's own message as an inline `Alert` rather than swapping views, since that message is already identical copy to the no-token `invalid` state. **Step 6** — added 6 new tests to `auth.test.ts` (email sent only for active/known accounts, no email + same generic message for unknown/deactivated accounts, generic message survives a delivery failure, valid reset revokes all sessions, invalid/expired/reused token → 400); all 198 API + 5 web tests pass, typecheck/lint clean both workspaces. Playwright + curl end-to-end against the real dev servers + seeded Neon DB: submitted the real forgot-password modal for a seeded Troop Leader, read the real link out of the API's dev-mode console log, opened it in the browser, set a new password, saw the success screen, signed in with it, confirmed the old password now fails and the same token can't be replayed (400), confirmed an unrecognized token renders the inline "invalid or expired" `Alert`, confirmed no email is attempted for an unknown address (log line count unchanged) — then reverted the test account back to the shared seed password (`GspDemo!2026`) via the same real flow, leaving no lingering state. `SMTP_HOST/PORT/USER/PASS`, `EMAIL_FROM`, `WEB_APP_URL` added to `env.ts` and `.env.example` (all but `EMAIL_FROM`/`WEB_APP_URL` optional). **Open decision #7 is now settled — all 7 decisions in the tracker are resolved, none remain open.** | No further features are scoped in build-plan.md; the build plan (21/21) plus this follow-up are both complete. Nothing is currently queued — resume only when the user scopes new work (e.g. a genuinely new feature, or revisiting 2.5 if real email notifications are ever wanted, which can now reuse this same `EmailService`). |
| 2026-07-24 | **3.5 Profile Management — full Loop, feature complete (●). Phase 3 is complete, 5/5. All 21 features across Phases 0–3 are now done.** | Built `/profile` with mock data first — gradient header (avatar initials/name/email/role pill) + Account Information card (editable Full Name/Email/Phone, read-only Role/Member Since) + Change Password card, reusing existing kit primitives only, including `PasswordStrengthMeter` reused cross-feature from `features/auth` (no new base components). **Before building**, resolved a real scope ambiguity with the user: build-plan.md lists "avatar" as editable, but no avatar upload or URL exists anywhere in the app (every avatar is initials derived from the name) — user chose to keep it display-only rather than add upload/URL infrastructure as a first for this feature. Visually verified by the human at 1280/900/768/480px, mobile nav, and all three states (ready/loading/error via a temporary mock-step switcher — no "empty" state, since a single-record profile has nothing to be empty) — gate 2 passed, no changes requested. **Contract + Wire Read + Wire Write** built together: new `profile` backend module — 3 endpoints, every one gated by `requireAuth` alone with no `requireRole` at all (a first — every prior module had at least one role check beyond auth), since every signed-in role manages only their own account here and the routes take no `:id`, so there is no cross-user access surface to guard by construction. `updateProfile` independently re-checks the new email isn't already claimed by a different user (409) even though the frontend has no way to submit someone else's current email by accident; `changePassword` re-verifies the current password server-side via a real bcrypt compare before hashing the new one. Both write real `audit_logs` rows (`profile.update`/`user.change_password`, `entityType: 'user'`) so self-service edits surface in 3.4's Audit Log tab alongside admin-driven ones, with no new wiring needed there. Email-conflict and wrong-current-password errors render as an inline `Alert` on the relevant card rather than a toast — same precedent as 3.4's `UserFormModal`, since both are field-relevant and shouldn't vanish before they're read. Added `apps/api/tests/profile.test.ts` (9 tests). All 193 API + 5 web tests pass, typecheck/lint clean in both workspaces. **Caught and fixed 2 real bugs during self-review**, before and during Playwright: (1) at 480px the read-only Role/Member Since row used `gap-x-4` instead of `gap-4`, so the two fields had no vertical gap once the grid collapsed to one column — fixed to match `SystemSettingsPanel`'s own grid pattern; (2) `ProfileHeader` originally bound to the session `AuthUser` alone, so renaming yourself left the gradient header — and the sidebar's own name chip, fed by the same server-rendered session — showing the old name until the next login; fixed by having `ProfileView` prefer the freshly-fetched profile once loaded and calling `router.refresh()` after a successful save, the same staleness `app-shell.tsx`'s `handleSignOut` already works around. Playwright-verified against the real dev servers + seeded Neon DB: as Administrator, real seeded values rendered exactly; a real full-name edit round-tripped through Neon and updated both the header and the sidebar live via `router.refresh()`, then was reverted; a real email-conflict against the seeded Executive Council account produced the inline conflict alert; a real password change (genuine bcrypt round trip) succeeded and was immediately reverted to the original demo password, leaving the shared demo account unaffected; confirmed Troop Leader (Liza Bagadiong) reaches the identical page with her own real data. Backend curl-verified directly: unauthenticated → 401 on all three routes; a real wrong-current-password attempt → 400 with the stored hash left untouched. **3.5 Profile Management is done — Phase 3 (Finance & Admin) is complete, 5/5. Every feature in the build plan (Phases 0–3, 21/21) is now done.** | **The build plan is complete — all 21 features across Phases 0–3 are done.** No further features are scoped in build-plan.md. Remaining non-blocking item: open decision #7 (real email delivery for 1.1's forgot-password and any future notification email) — revisit only if a real requirement emerges; nothing currently depends on it. |
| 2026-07-24 | **3.4 Settings & System Administration — full Loop, feature complete (●). Phase 3 is 4/5.** | Built `/settings` with mock data first — 4 tabs (System Settings/Users & Access/Audit Log/Backups), reusing existing kit primitives only, no new base components (added `PasswordIcon`/`BackupIcon` to the icon map only). No prototype baseline covers this exact combination — `Gsp.html`'s `renderUsers` (table + role-permission matrix + activity log card) was the closest analog and shaped Users & Access's layout/role-badge coloring; the other 3 tabs compose patterns already established by Finance/Reports/Badges/Analytics. Reused `PasswordStrengthMeter` from `features/auth` in the Add-User modal rather than duplicating it — cross-feature component reuse, same precedent as dashboard's `MemberStatusBadge` import from `features/members`. Visually verified by the human at 1280/900/768/480px, all four states (ready/loading/empty/error via a temporary mock-step preview switcher) — gate 2 passed, no changes requested. **Before building, confirmed no RBAC correction was needed** (unlike 1.6/2.1/2.4/3.1): `users:manage`/`settings:manage`/`audit-logs:read` were already Administrator-only in both seeded `roles.ts` files, matching project-overview.md's System Administration/User Management sections having zero Executive Council or Troop Leader responsibilities. **Contract + Wire Read + Wire Write** built together: new `settings` backend module spanning 3 route groups (system settings + backups, users, audit log) — 11 endpoints total, every one `requireRole('admin')` only, no `anyRole` read tier at all (a first — every prior Phase 1–3 module had at least one non-admin read role). `GET`/`PUT /settings` speak the same typed `SystemSettingsFormValues` shape the frontend form uses directly — the API owns the mapping to/from the string-keyed `system_settings` rows entirely, so the client never sees raw setting keys (simpler than the frontend's original mock-stage `SystemSettingItem[]` design, which was dropped once the real contract was written). Reset Password generates a real bcrypt-backed temporary password, shown once in a modal — no real email delivery yet (open decision #7, same non-blocking gap as 1.1's forgot-password). Delete User is a real hard delete (every other `User` foreign key in the schema is `onDelete: SetNull`, confirmed schema-safe) guarded by three checks: self-delete/self-deactivate blocked, last-remaining-Administrator blocked (`assertNotLastAdmin`), and a user who still leads a troop blocked with an explicit 409 telling the admin to reassign troops first. Backups has no real database-dump job — "Run Backup Now" writes a real `audit_logs` row and the tab derives "Last Backup"/history from that row rather than a stored status, called out on-screen via an inline `Alert`. Added `apps/api/tests/settings.test.ts` (22 tests). All 184 API + 5 web tests pass, typecheck/lint clean in both workspaces. **Caught and fixed 1 real bug during Playwright verification against live Neon** (before Test & Done): `updateSystemSettings` and `runBackup` both originally wrote `entityType: 'system'` to `audit_logs`, so the Backups tab's history list (a plain entityType filter) picked up System Settings saves and mislabeled them as completed backups — fixed by giving `settings.update` its own `entityType: 'settings'`, reserving `system` for `backup.run` alone, and added a "Settings" option to the Audit Log tab's record-type filter. 2 stale rows written by the pre-fix code during this session's own testing remain in live Neon (mislabeled, harmless) — human explicitly declined a one-off cleanup script, so they're documented as an open cosmetic item in Blockers rather than silently fixed. Playwright-verified against the real dev servers + seeded Neon DB as Administrator: System Settings round-tripped the real seeded values and a real edit+revert; Users & Access showed all 5 real users and ran a full real Add User → Reset Password → Deactivate → Delete cycle on a throwaway account, leaving the table back at its original state; confirmed the signed-in admin's own row has Deactivate/Delete genuinely disabled, not just visually; Audit Log showed all 6 real actions from this session plus 1.4's pre-existing rows, paginated; Backups correctly showed "Never run" until a real click produced a real timestamped entry. Backend curl-verified directly: unauthenticated → 401; Troop Leader and Executive Council → 403 on all three route prefixes; Admin → 200; duplicate-email create → 409; reset-password on an unknown id → 404; self-delete/self-deactivate → 400; empty-org-name-plus-non-numeric-fields save → 422 with all four field errors. **3.4 Settings & System Administration is done — Phase 3 is 4/5.** | **3.5 Profile Management** — start at Loop step 1: profile header + editable info (name/email/phone/avatar), password change, with mock data first. All roles, own profile only — distinct from 3.4's admin-driven user management, which edits *other* users' accounts and roles. First feature every role reaches, not just Admin/EC. |
| 2026-07-24 | **3.3 Analytics (Executive) — full Loop, feature complete (●). Phase 3 is 3/5.** | Built `/analytics` with mock data first — one page, 6 tabs on the `Tabs` primitive (Membership/Attendance/Participation/Badges/Financial/Organization), each a stat-card row + one ChartJS chart (Organization also gets a supporting table), reusing existing kit primitives only. No prototype baseline exists for this feature — `Gsp.html` has no analytics section at all — so the layout is composed from patterns already established by Finance/Reports/Badges/Dashboard. Visually verified by the human at 1280/900/768/480px as Administrator via Playwright against the real dev server — gate 2 passed after 2 self-caught fixes (see below), no changes requested. **Contract + Wire Read** built together (no Wire Write — the feature is read-only, matching build-plan.md's scope): new `analytics` backend module — a single `GET /analytics/overview` endpoint returning all 6 sections in one payload (simpler than Reports' per-type fetch, since there's no pagination or writes to justify separate round trips), `requireRole('admin','executive_council')` matching the seeded `analytics:read` grant (already correct — no pre-build RBAC fix needed, unlike 1.6/2.1/2.4/3.1). Every figure computed live from `Member`/`Event`/`EventRegistration`/`AttendanceRecord`/`Badge`/`MemberBadge`/`Troop`/`Payment`/`Expense` rows, same "no stored snapshot" precedent as finance/reports — the schema's `AnalyticsSnapshot` model stays unused. Monthly trend charts (Membership/Attendance/Financial) reuse the same "last 6 calendar months, oldest first" bucketing technique dashboard/finance already established, duplicated locally per that same per-module convention rather than shared. `ParticipationChart`/`BadgeCompletionChart` are the first horizontal-bar (`indexAxis:'y'`) charts in the app. Added `apps/api/tests/analytics.test.ts` (6 tests). All 162 API + web tests pass, typecheck/lint clean in both workspaces. **Caught and fixed 2 real bugs during this session**: (1) all six chart components are statically imported on one page, so `FinancialTrendChart`'s ChartJS `Legend` registration (a shared registry, not per-chart) leaked a stray "■ undefined" legend onto the other five charts — fixed by explicitly setting `legend:{display:false}` on all five, same bug class 3.1 hit with its donut; (2) the original "Top Performing Troop" stat card put a full troop name in `StatCard`'s large stat-value font, and at 768px the 3-column grid was too narrow even for one word — the shared `break-words` safety net (added for 3.1's currency strings) fragmented "Troop 12 — Virac" into unreadable chunks; fixed by replacing it with "Top Attendance Rate" (numeric) since every other stat card app-wide is a number/percentage and the troop's identity is already visible in the chart and table below. Playwright-verified against the real dev servers + seeded Neon DB: all 6 tabs render real data as Administrator (12 members/8 active, 3 events held/86% avg rate, 20 registrations across 2 events, 6 badges incl. a genuine 0%-completion one, a real negative council balance formatted correctly, 3 troops with attendance rates of 100%/100%/0%) — cross-checked against the dashboard's own numbers for consistency; confirmed Executive Council reaches the same page read-only. Backend curl-equivalent (authenticated `fetch` in the browser) verified directly: unauthenticated → 401; Troop Leader → 403 (and separately bounced to `/dashboard` by the Next.js middleware, plus no nav entry at all for that role); Admin and Executive Council → 200. **3.3 Analytics (Executive) is done — Phase 3 is 3/5.** | **3.4 Settings & System Administration** — start at Loop step 1: settings sections with toggles, user management (Admin CRUD + activate/deactivate + reset password + role assignment), audit log view, system config, backup controls, with mock data first. First feature to read/write `SystemSetting`/`User`/`UserRole` broadly and to surface the `audit_logs` table (already written by 1.4's approve/reject) as a real screen rather than just a backing table. |
| 2026-07-24 | **3.2 Reports — full Loop, feature complete (●). Phase 3 is 2/5.** | Built `/reports` with mock data first — one screen covering all 6 report types (Membership/Attendance/Badge/Financial/Activity/Executive) via a radio-card `ReportTypeSelector` (same `sr-only`-input technique as auth's `RoleSelector`, not the prototype's `onclick`-`div` cards), a date-range + optional Troop filter row, a generic stat-cards-plus-table preview panel, and a paginated Report History table — reusing existing kit primitives only. Visually verified by the human at 1280/900/768/480px, all four states (ready/loading/empty/error via a temporary mock-step preview switcher), and all three roles — gate 2 passed after one self-caught fix (see below), no changes requested. **Resolved a real ambiguity before building**: project-overview.md's per-role "Reports" sections each list a *different* subset of type names (Admin: Membership/Attendance/Activity/Badge/Financial/Organization; EC: Executive/Membership/Financial/Event/Analytics; Troop Leader: Troop/Attendance/Badge/Activity) that don't reconcile into one canonical list — resolved by trusting the already-correct seeded permissions instead (same "trust the grant over terser prose" precedent as 2.2/3.1): a type shows if the role holds that domain's `:read` permission, Financial requires `finance:read` (Troop Leader doesn't have it), and Executive is reserved for Admin/Executive Council by role check (no single domain permission covers a cross-module summary — same split as 1.5's dashboard). **Contract + Wire Read + Wire Write** built together: new `reports` backend module — 4 endpoints (`GET /preview`, `GET /`, `POST /export`, `GET /:id/download`), reads open to all 3 roles with per-type access enforced in the service (`assertTypeAccess`, since it varies by type not by route), export `requireRole('admin','executive_council')` matching finance's `canManage` precedent. Each of the 6 preview builders queries the existing domain tables directly (not the other modules' services) but replicates their exact scoping rules: Membership stays unscoped (matches `members` module's own unscoped reads), Attendance/Badge/Activity scope a Troop Leader via `Troop.leaderId` → `troopId in (...)` (badges' `scopeToOwnTroop` pattern) or `submittedById` (activity-reports' own rule), Financial/Executive are Admin/EC-only. First feature to generate and persist real files — installed `pdfkit` + `exceljs`, both render off one generic `{stats, columns, rows}` shape so no per-type export code was needed; files write to `apps/api/storage/reports/<uuid>.<ext>` (new convention, gitignored — no upload/storage code existed anywhere in the codebase before this) and are referenced by the schema's pre-existing but previously-unused `Report.filePath`. Download streams the file with real `Content-Type`/`Content-Disposition` headers behind `requireAuth` + the same per-type check, rather than `res.sendFile`/`express.static`. **Report History is defense-in-depth, not just a hidden UI card**: `listHistory` filters `Report` rows to the caller's allowed types server-side, and `getDownload` independently re-checks type access even for a direct-by-id request — verified live, not just unit-tested (see below). Frontend: `features/reports/services/reports.service.ts` + 4 new BFF routes, including a binary-passthrough `[id]/download/route.ts` that can't reuse `proxyApiRequest` (that helper assumes a JSON envelope) — it forwards the same cookie-to-Bearer translation but streams raw bytes through instead. `downloadReport()` triggers the browser's native download via a hidden anchor click rather than manual blob handling. Deleted `reports.mock.ts` and the mock-step preview switcher once wired. Added `apps/api/tests/reports.test.ts` (16 tests — per-type access incl. Troop Leader blocked from financial/executive, membership status bucketing, attendance rate math + troop-scoping to only events with a matching record, badge earned-vs-in-progress split, activity `submittedById` scoping, financial payment/expense merge+sort, history type-filtering by role, export skips storage entirely on a 403, download 404/403/happy-path). All 156 API + 5 web tests pass, typecheck/lint clean in both workspaces. **Caught and fixed 1 real bug before visual sign-off**: the same 900px stat-card-overflow class 3.1 hit (`xs:grid-cols-2 lg2:grid-cols-4` squeezing "12" into "1"/"2") — fixed with 3.1's exact `md:grid-cols-2 xl:grid-cols-4` precedent instead of re-diagnosing it. Also corrected 3 mock-vs-real mismatches during Wire Read since the real seeded data didn't match the mock's assumptions: Membership's 4th stat is "Other" not "Inactive" (real `MemberStatus` names are pending/active/expiring/expired/archived/rejected — no literal "inactive" exists); Badge preview's rows are earned/verified only, since `MemberBadge.earnedAt` is null for in-progress records and can't be date-ranged (in-progress becomes a separate date-independent count); Executive's row table dropped the mock's fabricated "Trend" column since there's no historical baseline to honestly derive one from (`AnalyticsSnapshot` stays schema-only and unused, same precedent as 2.2/3.1's other unused tables). Playwright-verified against the real dev servers + seeded Neon DB as Administrator: all 6 types generated real previews (12 real members with genuinely mixed statuses incl. Expired/Rejected/Archived/Expiring); exported the same Membership report as both PDF (`%PDF-` magic bytes confirmed via an authenticated in-browser `fetch`) and Excel (`PK` zip signature + correct `.xlsx` content-type confirmed the same way) — both landed as real rows in Report History with the real signed-in user as `generatedByName`. As Troop Leader (Liza Bagadiong): type selector correctly showed only 4 cards (no Financial/Executive); Attendance/Badge/Activity previews came back correctly troop-scoped (badge rows all real Troop 12 members, attendance counts lower than Admin's org-wide numbers) while Membership stayed unscoped, matching; Export buttons were replaced by a real "Ask an administrator or council member to export this report" note. Backend verified directly (authenticated `fetch` from the browser, curl-equivalent): unauthenticated → 401; Troop Leader `GET /preview?reportType=financial`\|`executive` → 403 (`membership` still 200); Troop Leader `POST /export` → 403 regardless of type; a real Financial report exported by Admin was **absent** from Troop Leader's `GET /reports` history, and a direct `GET /reports/:id/download` by that same real id → 403; `dateFrom` after `dateTo` → 422; unknown `reportType` enum → 422; unknown report id on download → 404. **3.2 Reports is done — Phase 3 is 2/5.** | **3.3 Analytics (Executive)** — start at Loop step 1: analytics views (membership/attendance trends, participation, badge completion, financial trends, org performance) with mock `react-chartjs-2` charts first, role-gated to Admin + Executive Council. First feature to read/write against `AnalyticsSnapshot`, or — following 2.2's/3.2's precedent of computing live from source tables rather than a stored aggregate table — a candidate to again compute trends live from `Member`/`Event`/`MemberBadge`/`Payment`/`Expense` rather than wiring snapshot storage, worth a deliberate decision before building. |
| 2026-07-24 | **3.1 Finance Management — full Loop, feature complete (●). Phase 3 is 1/5.** | Built `/finance` with mock data first — 4 tabs (Overview/Payments/Expenses/Fee Types) on the `Tabs` primitive, reusing existing kit primitives only (`StatCard`, `Table`, `Modal`, `FormField`, `Pagination`, plus two new chart components). **Before building**, caught and fixed the same class of RBAC bug found in 1.6/2.1/2.4: seeded `finance:write` was granted to Executive Council in both `roles.ts` files, contradicting project-overview.md (Executive Council's Financial section is read-only "Financial Monitoring"; only Administrator "Records payments/expenses") — user confirmed removing it, plus cleaned up the matching stale row in the live Neon `role_permissions` table via a one-off script (cosmetic only, real enforcement is `requireRole()`). Visually verified by the human at 1280/900/768/480px, all four states (ready/loading/empty/error via a temporary mock-step preview switcher), and both Administrator (full read/write) and Executive Council (fully read-only) — gate 2 passed after 2 rounds of self-caught fixes (see below), no changes requested. **Contract + Wire Read + Wire Write** built together: full `finance` backend module (repository/service/controller/routes/schema/types) — 9 endpoints, reads (`finance:read`) open to Admin + Executive Council, writes (`finance:write`) Admin-only; Troop Leader has no `finance:read` grant at all, matching project-overview.md's complete absence of a Financial section for that role. Every Overview stat/chart figure is computed live from `Payment`/`Expense` rows rather than the schema's stored `FinancialPeriod` totals, same simplification precedent as 2.2's unused `AttendanceSummary`. First feature to touch a Prisma `Decimal` column (`amount` on `Payment`/`Expense`/`FeeType`) — normalized to `number` via `.toNumber()` at the service-layer DTO mapping, same boundary every other module normalizes its Prisma payload at. Payments/Expenses are append-only (no edit/delete — matches the granted verbs, same precedent as 2.3's activity reports); Fee Types is full Admin CRUD with a usage-guarded delete (`fee-types-panel.tsx`, same pattern as 1.6's `CategoryPanel`, kept as its own bespoke panel since fee types carry an `amount` field categories don't). Added `apps/api/tests/finance.test.ts` (15 tests). All 140 API + 5 web tests pass, typecheck/lint clean in both workspaces. **Caught and fixed 3 real bugs during self-review** (mock stage and Wire Read/Write): (1) the trend chart registering ChartJS's `Legend` plugin — a global registry, not per-chart — silently turned on the neighboring donut's own built-in legend too, colliding with its custom HTML legend; fixed by explicitly disabling it in the donut's own options. (2) the step-1 "Empty" preview passed `overview: null`, which the overview panel treats as "still loading" — showed a permanent skeleton instead of a real empty state; fixed with a genuinely-zeroed fixture. (3) at exactly 900px the 4-up stat-card grid was too narrow for long currency strings like "₱20,500", overflowing into the next card — a case dashboard/badges never hit since their stat values are short; fixed both the shared `StatCard` (added `min-w-0`/`break-words` as a safety net) and finance's own grid breakpoints (`md:grid-cols-2 xl:grid-cols-4` instead of the usual `xs:grid-cols-2 lg2:grid-cols-4`). During Test & Done, also found and fixed (4) a stale-data bug — recording a payment didn't refetch Fee Types, so the payment-count usage-guard for fee-type deletion went stale until a full reload; and (5) a duplicate-toast bug — both the view and `FeeTypesPanel` were showing a toast for the same create/update action, fixed by removing the view-layer one (the panel already owns it, matching 1.6's precedent). Separately, while testing Fee Types' edit flow, **found and fixed a pre-existing bug inherited from the `event-form-modal.tsx`/`category-form-modal.tsx` pattern this feature's 3 modals were built from**: `isSubmitting` is never reset to `false` on a *successful* submit, so a modal's Cancel/Save buttons render permanently disabled the next time that same modal instance reopens — fixed in finance's 3 modals only; 1.6/2.1's original modals still have this bug, flagged as a follow-up if either screen is revisited. Playwright-verified against the real dev servers + seeded Neon DB as Administrator: real seeded fee types/payments/expenses render exactly; recorded a real payment and a real expense, both landing instantly via refetch with the Overview's totals recalculating live; ran a full real Fee Type create → edit → usage-guarded-delete-blocked → real-delete cycle, left the catalog in its original state. Confirmed Executive Council reads all 4 tabs with zero write controls anywhere. Backend curl-verified directly: unauthenticated → 401 everywhere; Troop Leader → 403 on reads (no `finance:read`); Executive Council → 200 on reads, 403 on every write route and on `GET /member-options`; missing required fields → 422; unknown member/fee-type id → 400; duplicate fee-type name → 409; deleting an in-use fee type → 409; deleting an unknown fee type → 404. **3.1 Finance Management is done — Phase 3 is 1/5.** | **3.2 Reports** — start at Loop step 1: report type selector (Membership/Attendance/Badge/Financial/Activity/Executive), filters, preview, export actions, with mock data first. Distinct from 2.3's activity report submission — this generates/exports from system data rather than accepting human submission. Exports to PDF and Excel per build-plan.md's Done gate. |
| 2026-07-24 | **2.5 Notifications & Announcements — full Loop, feature complete (●). Phase 2 is complete, 5/5.** | Built the topbar notification bell/dropdown (all pages) and two new pages — `/announcements` (new sidebar entry) and `/notifications` (reached only via the bell's "View All" link) — with mock data first, reusing existing kit primitives only. Caught and fixed a real mismatch during self-review before the human look: initially reused `ActivityItem` for notification rows, but its `meta` slot is a one-line caption (the dashboard's feed only ever puts a timestamp there) and truncated full notification titles — replaced with a dedicated `NotificationRow`. Visually verified by the human at 1280/900/768/480/360px (the last per the registry's own "notification panel must not overflow at 360px" note), all four states (ready/loading/empty/error via a temporary mock-step switcher), and both Administrator (post access) and Troop Leader (read-only) — gate 2 passed, no changes requested. **Before building**, checked the seeded `announcements:write` grant (Admin + Executive Council only) against project-overview.md's Troop Leader "Notifications" section (receive-only framing) and found it already correct — no RBAC correction needed, same outcome as 2.2/2.3. **Contract + Wire Read + Wire Write** built together: new shared best-effort util `apps/api/src/shared/utils/notify.ts` (`notifyUser`/`notifyUsers`, swallows its own failures — a failed notification must never block the action it's attached to, same precedent as auth's best-effort logout revoke); new `notifications` module (own-rows-only, no role gate beyond `requireAuth`, `PATCH /:id/read` 404s on a real ownership mismatch) and `announcements` module (reads open to all 3 roles, `POST` `requireRole('admin','executive_council')`) — 5 endpoints total. Wired the three real triggers build-plan.md's Done gate actually names: announcement-post fans out to every other user; `membersService.approve`/`reject` and `badgesService.verifyMemberBadge` each notify the affected member's `troop.leaderId` when one is assigned (skipped otherwise, same "self-signed-up leader with no troop yet" edge case 1.5 documented) — deliberately did not invent a fourth "event reminder" trigger, since there is no scheduler in this codebase and the Done gate only commits to actions, not clock time. Added `apps/api/tests/{notifications,announcements}.test.ts` (5 + 3 tests) and 2 new notify-side-effect tests each to `members.test.ts`/`badges.test.ts` (leader-present / no-leader). All 125 API + 5 web tests pass, typecheck/lint clean in both workspaces. Playwright-verified against the real dev servers + seeded Neon DB: the real seeded "Welcome to the GSP portal" notification and "Annual Council Camp registration is open" announcement (posted by Rosario Verceles) both rendered for Administrator; marked the seeded notification read live; posted a real new announcement and watched Troop Leader Liza Bagadiong receive a live, real fan-out notification for it (poster correctly excluded from her own fan-out). Backend curl-verified directly: unauthenticated `GET /notifications` → 401; Troop Leader `POST /announcements` → 403; Executive Council `POST /announcements` → 201; a real cross-user `PATCH /notifications/:id/read` → 404 (ownership check is real, not just a hidden UI gap); empty title/content → 422; verifying a real earned badge (Faith Bermundo's "Heritage Keeper", Troop 4 — Bato, a troop with a real assigned leader) → 200, confirming the new badge-verify notify path runs cleanly live. **2.5 Notifications & Announcements is done — Phase 2 (Activities & Engagement) is complete, 5/5.** | **Phase 3 — 3.1 Finance Management** — start at Loop step 1: financial dashboard (stat cards + charts), payment form, expense log table, fee-type config, budget/collection summary, with mock data first. First feature to read/write `Payment`/`Expense`/`FeeType`/`FinancialPeriod`/`FinancialSummary`. |
| 2026-07-24 | **2.4 Badges & Achievements — full Loop, feature complete (●). Phase 2 is 4/5.** | Built `/badges` with mock data first — stat cards, a `Tabs`-based Badge Catalog/Member Progress/Achievement History screen, and a page-level Record Badge action, reusing existing kit primitives only (no new base components). **Before building**, caught and resolved a real RBAC ambiguity (flagged to the user, same discipline as 1.6/2.1): the seeded permissions granted Troop Leader `badges:verify` alongside Admin, but project-overview.md lists "Verify achievements" only under Administrator — user confirmed Admin-only verification, so `badges:verify` was removed from Troop Leader in both `roles.ts` files before any UI was drawn. Visually verified by the human at 900/768/480px, all three roles, all states (loading/empty/error via a temporary mock-step preview switcher) — gate 2 passed, no changes requested. **Contract + Wire Read + Wire Write** built together: new `badges` backend module spanning three route groups (badge catalog CRUD, member-badges progress/record/verify, achievements) — 10 endpoints total, reads scoped server-side for Troop Leader via `Member.troop.leaderId` (same "derived, not trusted" pattern as 2.1–2.3), catalog writes `requireRole('admin')`, record/achievement-create `requireRole('admin','troop_leader')`, verify `requireRole('admin')`. `recordMemberBadge`/`createAchievement` both independently re-check that the target member belongs to the requesting troop leader's own troop server-side, even though the frontend's member picker is already scoped — caught by the "never trust the client" discipline, not by a UI gap. Catalog badge deletion cascades `BadgeRequirement`/`MemberBadge` by schema design rather than being usage-guarded, same precedent as 2.1's event-delete. Added `apps/api/tests/badges.test.ts` (17 tests). All 113 API + 5 web tests pass, typecheck/lint clean in both workspaces. **Caught and fixed 2 real bugs during self-review**, before Playwright: (1) the page-level Record Badge button was squeezing the `Tabs` strip's own horizontal-scroll region at 768px instead of the header row wrapping below it — root cause was a stray `flex-1 min-w-0` on `Tabs` that absorbed the space pressure a plain `flex-wrap` needed to trigger; (2) the Member Progress detail modal held a stale snapshot of the member row captured at "View" time, so verifying a badge updated the stat cards live but left the open modal showing "Earned" with the Verify button still there — fixed by tracking the selected member's id and re-deriving the row from the live list every render. Playwright-verified against the real dev servers + seeded Neon DB: as Administrator, all 6 real seeded badges rendered with real stats (2 in progress/3 earned/2 verified, matching `seed.ts` exactly); recorded a real achievement and a real "Earned" badge for a real member (Faith Bermundo) — both landed instantly via refetch; verified the badge from the detail modal — flipped live to "Verified by Marisol Tabuena". Executive Council saw the identical real data fully read-only; Troop Leader (Liza Bagadiong) saw catalog/progress/achievements/the member picker all correctly scoped to her own Troop 12. Backend curl-verified directly: unauthenticated → 401 on all three read endpoints; Executive Council/Troop Leader `POST /badges` → 403; Executive Council `POST /member-badges` → 403; Troop Leader `PATCH /member-badges/:id/verify` → 403; Troop Leader recording a badge for a real member outside her own troop → 403 with an explicit message; unknown badge id → 400; verifying a real `in_progress` record → 400; duplicate badge name → 409; delete of an unknown badge → 404. **2.4 Badges & Achievements is done — Phase 2 is 4/5.** | **2.5 Notifications & Announcements** — start at Loop step 1: notification panel/dropdown, announcement feed, event reminders, unread state, with mock data first. In-app only per build-plan.md's scope note — no email delivery (same open decision as 1.1, §7). First feature to read/write `Notification`/`AnnouncementPost` against real actions from every prior feature (registration, badge earned/verified, approval, announcement post). |
| 2026-07-23 | **2.3 Activity Report Submission (Troop Leader) — full Loop, feature complete (●). Phase 2 is 3/5.** | Built `/activity-reports` with mock data first — a single page with two variants picked from the session role (Troop Leader: "My Activity Reports" + Submit action; Admin/Executive Council: all-troops table with Submitted By/Troop columns), a submit form modal (event picker, summary/participation notes/outcomes), and a read-only detail modal — reusing existing kit primitives only, no new base components. New nav entry ("Activity Reports", `DocumentIcon`) added to the Activities section — this route didn't exist as a Phase 0 placeholder, unlike every prior Phase 1/2 feature. Visually verified by the human (full report reviewed: states, both role variants, responsive 900/768/480px, mobile nav) — the human gave an explicit go-ahead to proceed rather than a separate live walkthrough; gate 2 treated as passed. **Resolved two open questions before continuing**, both documented in the feature's entry above: (1) no review/"mark reviewed" endpoint exists — build-plan.md's exact 3-endpoint list and read-only Access line for Council settled it, `reviewed` stays a schema-supported but unreachable status, same precedent as 2.2's unused enum values; (2) Admin gets both the all-troops read scope *and* a Submit action, resolved in Admin's favor because the existing `activity-reports:write` grant already includes Admin and 2.2 set the precedent of trusting an already-correct permission grant over build-plan's terser per-feature prose. **Contract + Wire Read + Wire Write** built together: new `activity-reports` backend module (repository/service/controller/routes/schema/types) — reads scoped server-side by role (Troop Leader sees only their own submissions via `submittedById`, Admin/Executive Council see every troop), writes gated to Admin + Troop Leader matching the existing permission grant (no RBAC correction needed, same outcome as 2.2's pre-build check). `create` independently validates the event exists and is genuinely completed (`eventDate` in the past) server-side, not just trusting the frontend's completed-only picker. No separate "detail" type — `ActivityReportSummary` is identical from the list and from `GET /:id`, so the detail modal reads the row already in memory instead of an extra fetch (unlike members, whose list row is a lighter summary). `troopName` derives from the submitter's own `ledTroops`, same pattern as events' `troopNames`; the event picker reuses 2.1's `listAllEvents({status:'completed'})` instead of a new lookup. Added `apps/api/tests/activity-reports.test.ts` (8 tests — troop-leader scoping, admin/EC unscoped read, troop-name mapping, not-found, unknown-event/not-yet-completed/success on create). All 96 API + 5 web tests pass, typecheck/lint clean in both workspaces. Playwright-verified against the real dev servers + seeded Neon DB: as Troop Leader (Liza Bagadiong), the real empty state rendered first (`seed.ts` seeds no `ActivityReport` rows), then submitted a real report against the real "Coastal Clean-Up Drive" event — appeared instantly via refetch, detail modal showed the real derived troop name and exact submitted text; as Administrator, the same real report appeared in the all-troops table alongside a working Submit action. Caught and fixed one bug during self-review before this stage — the event-title/submitted-date cells wrapped mid-word at 900px, same class of bug 1.4/2.2 hit, fixed with `whitespace-nowrap`. Backend curl-verified directly: unauthenticated `GET` → 401; Executive Council `POST` → 403 (read still 200); missing `summary` → 422; unknown `eventId` → 400; a real upcoming event's id → 400 "You can only submit a report for a completed event."; unknown id on `GET /:id` → 404. **2.3 Activity Report Submission is done — Phase 2 is 3/5.** | **2.4 Badges & Achievements** — start at Loop step 1: badge catalog grid (registry §3 `.badge-card`/`.earned` state), progress tracker (progress bars), achievement history, troop-leader verify action, with mock data first. First feature to read/write `Badge`/`BadgeRequirement`/`MemberBadge`/`AchievementRecord` against real members (1.3) and badge categories (1.6). |
| 2026-07-23 | **2.2 Event Registration & Attendance — full Loop, feature complete (●). Phase 2 is 2/5.** | Built `/attendance` (replacing the Phase-0 placeholder) with mock data first — event picker, threshold-colored stat cards, one combined participant-list-and-attendance-checklist table (`ToggleSwitch` per row) instead of two near-duplicate tables, and a searchable Register Participant modal. Linked from `/events/[id]`'s Registrations card via a new "Manage Attendance" button. Visually verified by the human at 1280/900/768/480px, all states (loading/empty/error, plus a genuinely empty event), and both the Troop Leader (full read+write) and Executive Council (read-only) views — gate 2 passed, no changes requested. Checked the existing `attendance:read`/`attendance:write` permission grants against project-overview.md before building (same discipline as 1.6/2.1's pre-build RBAC audits) and found them already correctly scoped — Troop Leader has both, Executive Council only `read`, Admin has everything — so unlike 1.6/2.1 **no correction was needed this time**. **Contract + Wire Read + Wire Write** built together: extended the existing `events` module (rather than a new one) with registrations — `GET/POST /events/:id/registrations`, `PATCH /events/:id/registrations/:memberId` — same precedent as 1.4 extending `members` for approvals, since `EventRegistration` is a sub-resource of `Event`; and built a new `attendance` module owning `AttendanceRecord` (`POST /attendance`). Registering a previously-cancelled member reactivates the same row rather than inserting a duplicate (the real `[eventId,memberId]` unique constraint would reject a second insert) — same restore-not-recreate idiom as members archive/restore. Recording attendance is rejected with 400 for any member without an active registration. `GET /events/:id/registrations` returns participants **and** the summary in one response (not two round trips) — the summary is computed from live `AttendanceRecord`/`EventRegistration` counts, the schema's `AttendanceSummary` table stays unused, same simplification precedent as 1.5's/2.1's other server-computed fields. Frontend: new `features/attendance/services/attendance.service.ts` + 3 new BFF routes (`/api/events/[id]/registrations`, `/api/events/[id]/registrations/[memberId]`, `/api/attendance`), rewrote `AttendanceView` to fetch for real and deleted both the step-1 mock (`attendance.mock.ts`) and its temporary "Preview state" switcher. Added `apps/api/tests/attendance.test.ts` (4 tests) and 9 new tests to `apps/api/tests/events.test.ts` (registrations join/summary math, cancelled-registration exclusion, unknown event/member, reactivation) — 88 API + 5 web tests pass, typecheck/lint clean in both workspaces. **Caught and fixed 4 real issues during self-review**, three before Playwright even ran: (1) the register-participant picker let you "re-register" a member who already had a *cancelled* row for the event, which would have raced the table's own Re-register action against the real unique constraint — fixed to exclude any existing row regardless of status; (2) a freshly-registered participant rendered an empty troop-code pill — table now falls back to the troop name; (3) troop-code pills and the attendance-toggle label wrapped across multiple lines at 768px, same class of bug 1.4 hit — fixed with `whitespace-nowrap` so the table scrolls horizontally instead; (4) the register modal's member picker requested `pageSize: 200` from `GET /members`, but that endpoint caps at 100 — caught via a curl walkthrough *before* browser testing, which would otherwise have 422'd on the very first click of "Register Participant" in the browser. Playwright-verified against the real dev servers + seeded Neon DB as Troop Leader: the real "Provincial Investiture Ceremony" (6 seeded registrations, 5 present/1 absent, 83%) rendered exactly matching a direct curl of the same endpoint; registered a real member via the modal, toggled her attendance live (stat cards recalculated to 86%), then cancelled her registration (count returned to 6/83%, restoring the original seeded state); confirmed Executive Council sees the full roster read-only. Backend curl-verified directly: unauthenticated → 401; Executive Council → 403 on both `POST /registrations` and `POST /attendance` (read still 200); missing `memberId` → 422; unknown member → 400; unknown event → 404; cancelling a non-existent registration → 404; recording attendance against a cancelled registration → 400; bad attendance-status enum → 422; full register→attend→cancel→blocked-attendance→reactivate cycle walked via curl and left in its original state. **2.2 Event Registration & Attendance is done — Phase 2 is 2/5.** | **2.3 Activity Report Submission (Troop Leader)** — start at Loop step 1: submit-report form (event, summary, participation notes, outcomes), my-submitted-reports table, report detail view, Council review status badge, with mock data first. First feature to read/write `ActivityReport` against the real events 2.1/2.2 now expose; distinct from Phase 3.2 Reports, which generates/exports from system data rather than accepting human submission. |
| 2026-07-23 | **2.1 Event Management — full Loop, feature complete (●). Phase 2 underway (1/5).** | Built `/events` with mock data first — stat cards, a `Tabs`-based List/Calendar view toggle, search/status/category filters, paginated event cards, create/edit modal, delete confirm, and `/events/[id]` detail page. Built the first real `EventCalendar` (registry §4's Calendar entry was CSS-only in the prototype, never wired to markup) — a month grid of real `<button>` day cells with a below-grid selected-day events panel. Visually verified by the human at 1280/900/768/480px, all states (loading/empty/error/no-organizer), and both list/calendar views — gate 2 passed, no changes requested. **Before building**, caught and fixed the same class of RBAC bug 1.6 found: Executive Council had `events:write` in both `roles.ts` files, contradicting project-overview.md (event Create/Edit/Delete is Administrator-only); user confirmed removing it, so Executive Council now holds `events:read` only, matching Troop Leader. **Contract + Wire Read + Wire Write** built together: full `events` backend module (repository/service/controller/routes/schema/types) — 5 endpoints, reads open to all 3 roles, writes `requireRole('admin')` only; status (Upcoming/Completed) derived server-side from `eventDate` vs. today rather than stored; "Troop(s)" badges derived from the organizer's own led troop(s) since the schema has no `Event.troopId`. Reused 1.6's `GET /organizations/activity-categories` and `GET /organizations/troop-leaders` for the modal's pickers instead of duplicating those lookups. Frontend `events.service.ts` replaced its in-memory mock array with real fetches through new `/api/events` and `/api/events/[id]` BFF routes; deleted `events.mock.ts` once wired (same convention as every prior feature). Caught and fixed a real bug during self-review: `EventCalendar` was unmounting on every background refetch (gated behind `viewState === 'ready'`), silently resetting its month-cursor/selected-day state to the current month after every mutation — fixed with a `hasLoadedOnce` flag so the calendar survives a refetch, verified by navigating to August, creating an event, and confirming the calendar stayed on August. Added `apps/api/tests/events.test.ts` (13 tests — status derivation at the day boundary, troopNames/organizer/category mapping incl. the unassigned case, create/update category+organizer validation, not-found on get/update/delete). All 88 API + 5 web tests pass, typecheck/lint clean in both workspaces. Playwright-verified against the real dev servers + seeded Neon DB as Administrator: all 4 real seeded events render with real dates/troop/registration counts; ran a full real create→edit (assigned a real organizer)→delete cycle against Neon, cleaned up after; confirmed Troop Leader sees both views and all events but no New Event button and no Edit/Delete actions. Backend curl-verified directly: unauthenticated `GET /events` → 401, Troop Leader `POST /events` → 403 (read still 200), missing required fields → 422, unknown `categoryId` → 400, `endTime` before `startTime` → 422, delete of an unknown id → 404. **2.1 Event Management is done — Phase 2 is 1/5.** | **2.2 Event Registration & Attendance** — start at Loop step 1: registration form, participant list, attendance checklist UI (present/absent `ToggleSwitch`), attendance summary with the threshold-colored rate, with mock data first. First feature to read/write `EventRegistration`/`AttendanceRecord`/`AttendanceSummary` against the real events 2.1 now exposes. |
| 2026-07-23 | **1.6 Organization Management — full Loop, feature complete (●). Phase 1 done (6/6).** | Built `/organizations` with mock data first — 5 tabs (Councils/Troops/Scout Levels/Badge Categories/Activity Categories) on a new `Tabs`/`TabPanel` primitive (registry §9's `planned` slot, now built), reusing `Card`/`Table`/`Modal`/`ConfirmDialog` conventions from 1.3/1.4 since the prototype never had this screen at all. One generic `CategoryPanel`/`CategoryFormModal` covers Scout Levels/Badge Categories/Activity Categories instead of three near-duplicates. Visually verified by the human at 900/768/480px, all states, and both the Admin (full CRUD) and read-only (Executive Council) views — gate 2 passed, no changes requested. **Before building**, surfaced and resolved a real RBAC discrepancy: the seeded `ROLE_PERMISSIONS` (both workspaces' `roles.ts`) granted `organizations:write` to Executive Council, contradicting project-overview.md's role matrix (Organization Management is Administrator-only; Executive Council only monitors "Organizational KPIs"/"Organization performance"). User confirmed the fix — removed the grant from both constants files, and separately cleaned up the matching stale row in the live Neon `role_permissions` table via a one-off script (cosmetic only, since this codebase's real enforcement is `requireRole()`'s hardcoded per-route role list, not that table). **Contract + Wire Read + Wire Write** built together: full `organizations` backend module (repository/service/controller/routes/schema/types) — 20 endpoints total, reads open to all 3 roles, writes `requireRole('admin')` only; delete on every resource is guarded by real usage counts (409 `CONFLICT`) because the schema's FKs are all `onDelete: SetNull` and would otherwise silently orphan real members/badges/events. Widened the existing `GET /troops`/`GET /scout-levels` (1.3's registration-picker endpoints) with additive fields rather than duplicating them. Frontend: `features/organizations/services/organizations.service.ts` + a `useResourceList` hook (one fetch-on-mount-plus-refetch pattern reused 5 times) replacing the mock step's local in-memory CRUD; deleted `organizations.mock.ts` once real data was wired (same convention as 1.5's dashboard). Added `apps/api/tests/organizations.test.ts` (21 tests — conflict/not-found/bad-input paths across all 5 resources). All 62 API + 5 web tests pass, typecheck/lint clean in both workspaces. Playwright-verified against the real dev servers + seeded Neon DB as Administrator: all 5 tabs show real seeded data exactly matching `seed.ts`; ran a full real create→edit→delete cycle for a council and a troop (with a real troop-leader assignment) and an activity category, all cleaned up afterward; confirmed Executive Council can browse all 5 tabs but has no write controls. Backend curl-verified directly: unauthenticated write → 401, Executive Council write → 403 (read still 200), duplicate name → 409, in-use delete → 409, empty name → 422. **1.6 Organization Management is done — Phase 1 (Core Operations) is complete, 6/6.** | **Phase 2 — 2.1 Event Management** — start at Loop step 1: event list/cards + calendar view + create/edit modal + event detail page, with mock data first. First feature to need `ActivityCategory` (1.6 built it) and to consume the real troop/council picker data 1.3/1.6 already expose. |
| 2026-07-23 | **1.5 Dashboard (role-aware) — full Loop, feature complete (●)** | Built `/dashboard` with mock data first — three role variants (Admin: stat cards + `react-chartjs-2` Membership Growth bar + Members by Status donut + Recent Activity + Troops Overview; Executive Council: stat cards + Troops Overview + Membership by Scout Level progress bars; Troop Leader: stat cards + My Troop roster + Membership by Scout Level progress bars) — reusing `MemberStatusBadge`/`TableAvatar` from 1.3 and building four new shared primitives (`StatCard`/`StatCardSkeleton`, `ProgressBar`, `ActivityItem`/`ActivityDot`), registered in ui-registry.md. Visually verified by the human at 1280/900/768/480px for all three roles plus loading/empty/error states via a temporary mock-step "Preview state" switcher (gate 2 passed, no changes requested). **Contract**: defined `apps/api/src/modules/dashboard/dashboard.types.ts` and refactored the frontend `DashboardStat` type to drop `icon`/`tone` — those are presentation-only (an `IconType` is a React component, can't cross the wire) and now live in `features/dashboard/constants.ts`'s per-role `*_STAT_PRESENTATION` maps instead; `ActivityEntry.time` became `occurredAt` (ISO), formatted client-side by a new `shared/utils/format-relative-time.ts`. **Wire Read**: built the full backend module (repository/service/controller/routes) — admin aggregates org-wide (councils/troops/members/pending counts, 6-month growth bucketing, status breakdown, and a first-of-its-kind merge of `members.createdAt` registrations with `audit_logs` approve/reject rows into one sorted activity feed), council reads the same council-wide data (documented scope simplification — `User` has no `councilId` and there's one council today), troop leader resolves `Troop.leaderId === session.user.id` and returns a real "No Troop Assigned" empty payload for a self-signed-up leader 1.6 hasn't linked yet. Mounted `GET /api/v1/dashboard` behind `requireAuth` + all three roles. Caught and fixed a mock/reality mismatch here: the mock had invented all-active troops, but the real seed's CAT-SAN-007 has `leaderEmail: null` — renamed the troop-overview badge from Active/Inactive to **Led/No Leader**, derived from the real `leaderId`. Frontend: new `features/dashboard/services/dashboard.service.ts` + `/api/dashboard` BFF route (via the existing `api-proxy.ts` helper), rewrote `DashboardView` to fetch for real on mount and deleted the mock-step preview switcher; deleted the now-unused `dashboard.mock.ts`. **Wire Write**: N/A — build-plan.md §1.5 scopes a single `GET` endpoint, nothing to create/update/delete. **Test & Done**: added `apps/api/tests/dashboard.test.ts` (4 tests — admin aggregation, council read, troop-leader roster, and the empty "no troop" case). All 45 API + 5 web tests pass, typecheck/lint clean in both workspaces (root-level `npm run typecheck` caught one test-file type error — a `vi.spyOn` conditional `mockImplementation` didn't structurally match Prisma's branded `PrismaPromise<number>` — fixed by chaining `mockResolvedValueOnce` twice instead). Playwright-verified against the real dev servers + seeded Neon DB for all three real accounts: Admin/Council saw live org totals (12 members, grown from the 10-member seed by earlier sessions' test data) and the real San Andres "No Leader" badge; Troop Leader (Liza Bagadiong) saw her own real 6-member roster including herself as an Adult Leader row. A sign-out/navigation race incidentally exercised the real error path too — `ErrorState` with a working Retry rendered on a genuine 401. Backend curl-verified: unauthenticated `GET /dashboard` → 401. **1.5 Dashboard (role-aware) is done — Phase 1 is 5/6 complete.** | **1.6 Organization Management** — start at Loop step 1: councils table, troops table, scout-level/badge-category/activity-category config forms, with mock data first. Admin-only (`organizations:write` — note 1.3 already pulled forward read-only `GET /troops`\|`/scout-levels`, so 1.6 adds the CRUD + council management + category config on top of that, not from scratch). This is also where a real `councilId`-on-`User` concern from 1.5's Executive Council scope-simplification note could be resolved, if troop-leader/council assignment gets a relational home there. |
| 2026-07-23 | **Commit/push 1.4 + Playwright e2e cleanup** | 1.4's completed work (members module changes, `audit-log.ts`, the approvals feature, approve/reject/pending BFF routes) had been built but never committed — staged only those files (deliberately excluding an unrelated stray diff, see below), committed as `baae888`, and pushed to `origin/master`. Separately found an **undocumented, never-committed `@playwright/test` e2e setup** sitting in the working tree (`apps/web/playwright.config.ts`, `apps/web/e2e/login.spec.ts`, plus matching `package.json`/`package-lock.json`/`.gitignore` edits) — not scoped in build-plan.md/progress.md anywhere, and duplicates the Playwright-MCP-driven visual verification the Feature Loop already uses. Ran it: 2/4 passed (empty-field validation), 2/4 failed (demo-login redirect assertion timed out) — confirmed via direct curl that the login endpoint itself works fine, so this was test flakiness, not an app regression, but it was never fixed or finished. **Decision: discarded** — reverted `package.json`/`package-lock.json`/`.gitignore` and deleted the untracked e2e files; repo is back to a clean tree with only real feature work in history. Built a replacement for that use case that doesn't touch the repo: a scratchpad-only scripted verification helper (`pw/runner.mjs` + `pw/scenarios/login.mjs`, using bare `playwright-core` against the Chromium already cached by the MCP server install) that runs a full multi-step browser flow in one process and prints only compact PASS/FAIL lines — built to cut the token cost of live verification, since every MCP `browser_click`/`browser_snapshot` call otherwise returns a full accessibility-tree dump. Verified working (3/3 passed reproducing 1.1's login checks). Lives entirely in the session scratchpad, not the repo, so it doesn't reopen the "should we commit an e2e suite" question — reintroduce this pattern per-session as needed rather than assuming it persists. | **1.5 Dashboard (role-aware)** — start at Loop step 1: stat cards row, membership-growth bar chart, distribution donut, activity feed, KPI/progress widgets, with mock data per role first. Charts port from the prototype's CSS placeholders to `react-chartjs-2`. Remember the settled rule: the role is read from the session/JWT server-side only, never a client parameter. |
| 2026-07-23 | **1.4 Membership Approval (Executive Council) — full Loop, feature complete (●)** | Built the Pending Approvals queue (`/approvals` — paginated table of `pending` members, Review action) and a review modal (read-only profile detail + Approve/Reject with an always-visible reason field) with mock data first, reusing the 1.3 members feature's `Member` type as the contract fixture; visually verified by the human at all three breakpoints plus loading/empty/error states (gate 2 passed same session, no changes requested). Then Contract + Wire Read + Wire Write together: extended the existing `members` module rather than creating a new one (these routes operate on the same `Member` entity 1.3 owns) — `members.schema.ts` gained `pendingMembersQuerySchema`/`rejectMemberSchema`, `members.repository.ts` gained `approve`/`reject` (stamping `reviewedById`/`reviewedAt`), `members.service.ts` gained `listPending`/`approve`/`reject` (the latter two guard against acting on a non-`pending` member), `members.routes.ts` mounted `GET /pending` (before `/:id` — route-shadowing hazard) and `PATCH /:id/approve\|reject` behind a `canApprove` guard (`admin`+`executive_council`, same set as `members:archive`). Built the first-ever `audit_logs` writer (`shared/utils/audit-log.ts`) — approve/reject both write a row now. Added matching BFF routes and `listPendingMembers`/`approveMember`/`rejectMember` to the web `members.service.ts`; rewrote `ApprovalQueue` to fetch for real and refetch after every mutation (settled decision #3), fetching the full record on Review the same way 1.3's directory does on Edit (the row itself is only `MemberSummary`). Deleted the step-1 mock (`membership-approvals.mock.ts`) and the now-unneeded `features/approvals/types.ts` once real types took over. Added 6 new tests to `apps/api/tests/members.test.ts` (`listPending`/`approve`/`reject`, happy + not-pending + unknown-id paths, `writeAuditLog` mocked). All 37 API + 5 web tests pass, typecheck/lint clean in both workspaces. Playwright-verified against the real dev servers + seeded Neon DB: registered a new scout as Troop Leader, watched her appear live in the Executive Council's queue, reviewed her real fetched detail, and approved — toast, queue emptied, 1.3's directory shows her Active. Separately curl-rejected a seeded pending member and confirmed the red "Rejected" badge in the directory and the real reason in 1.3's pre-existing rejection banner on her profile. Backend curl-verified directly: unauthenticated → 401, Troop Leader → 403 on list/approve/reject, a 2-char rejection reason → 422, approving an already-active member → 400, and the two `audit_logs` rows confirmed written in Neon with the real reviewer id. **1.4 Membership Approval is done.** | **1.5 Dashboard (role-aware)** — start at Loop step 1: stat cards row, membership-growth bar chart, distribution donut, activity feed, KPI/progress widgets, with mock data per role first. Charts port from the prototype's CSS placeholders to `react-chartjs-2`. Remember the settled rule: the role is read from the session/JWT server-side only, never a client parameter. |
| 2026-07-23 | **1.3 Membership Management — full Loop, feature complete (●)** | Built the Membership Registry directory (`/members` — debounced search, status/type filters, paginated table, register/edit/archive/restore) and member profile page (`/members/[id]` — personal info, emergency contact, membership history, edit/renew/archive/restore) with mock data first (`MemberFormModal` handling both Scout and Adult Leader registration via one `memberType`-conditional form), visually verified by the human at all three breakpoints plus loading/empty/error states (gate 2 passed same session). Then Contract: `apps/api/src/modules/members/{members.schema,members.types}.ts` (Zod, `memberType`-discriminated create/update). Then Wire Read + Wire Write together (the mock's local-mutation pattern had to be fully replaced, not incrementally patched, to honor settled decision #3 — refetch after write): built the full backend module (repository/service/controller/routes, Prisma joins to status/troop/scoutLevel/profile/memberships) plus a minimal `organizations` module (`GET /troops`, `GET /scout-levels` — pulled forward from 1.6 only for the registration form's pickers, no CRUD). Added the first generic BFF-proxy helper (`shared/utils/api-proxy.ts`) so every future feature's Route Handlers can forward the httpOnly cookie as `Authorization: Bearer` without re-deriving auth's login/signup-specific cookie logic; wired 7 new `/api/members/*` + 2 `/api/organizations/*` BFF routes on top of it. Rewrote `MemberDirectory`/`MemberProfileView` to fetch for real (server-side search/filter/pagination) and refetch after every mutation instead of the mock step's optimistic local state; fixed a real bug caught during the rewrite — editing from the directory now fetches the full record first, since the row only carries `MemberSummary` and would have silently blanked birthdate/gender/emergency-contact/notes on save otherwise. Added `apps/api/tests/members.test.ts` (9 tests: list/getById/create/update/archive/restore/renew, happy + not-found + bad-troop paths). All 31 API + 5 web tests pass, typecheck/lint clean in both workspaces. Playwright-verified against the real dev servers + seeded Neon DB as Administrator: registered a real scout end-to-end (appeared `Pending Review`, count 10→11), archived/restored/renewed a real member (membership history row genuinely written), edit round-trips the full record, empty/error/loading states all real (not simulated). Backend curl-verified directly: unauthenticated request → 401, Troop Leader archive attempt → 403 (real RBAC, not just a hidden button), missing required fields on create → 422 with per-field details. **1.3 Membership Management is done.** | **1.4 Membership Approval (Executive Council)** — start at Loop step 1: pending-registrations queue table (members with `status: pending`, populated for real by 1.3's registrations) with mock data first, review modal, approve/reject actions with a reason field. |
| 2026-07-23 | **1.2 App Shell + Role-Based Navigation — full Loop, feature complete (●)** | Built role-filtered nav (`getNavSectionsForRole` in `nav-items.ts`, filtering by a hand-synced permission mirror in new `shared/constants/roles.ts`), a real user footer block in `Sidebar` (avatar initials via reused `TableAvatar`, name, role label), and an enabled Sign out button — first with mock session users for all 3 roles (visually verified at 900/768/480px, gate 2 passed same day). Then Contract + Wire Read: added the first-ever real `requireAuth` route, `GET /api/v1/auth/me` (repository `findUserById`, service `getCurrentUser`), and a server-only `session.service.ts` (`getSession()`) that the `(app)` layout calls directly — replacing the mock user with the real signed-in one; deleted the now-fully-superseded `session.mock.ts`. Then Wire Write: `AppShell`'s Sign out now calls the real `logout()` service and redirects, and `middleware.ts` gained a route-level RBAC guard (decodes the access token's `role` claim without verifying the signature — edge runtime, UX-layer only — and bounces unauthorized roles to `/dashboard`). Diagnosed a user report ("Fill Demo always logs in as Administrator") as browser-saved-password autofill overwriting the login form, not an app bug — confirmed by exercising `fillDemo()` directly for each role and watching it fill the correct account every time. Added 3 new API tests for `getCurrentUser` (happy path, deleted user, deactivated account) — 22 total passing. Playwright-verified against the real dev servers + seeded Neon DB using **all three real accounts** (not mocks): correct name/role/nav per account, Sign out clears cookies and redirects, and the route guard blocks Troop Leader from `/finance`/`/approvals`/`/analytics` and Executive Council from `/settings` while Administrator reaches everything. **1.2 is done.** | **1.3 Membership Management** — start at Loop step 1: member directory table (search/filter/status) with mock data, register/edit modal for both scout and adult-leader registration types. Built before the dashboard (1.5) since the dashboard aggregates this data. |
| 2026-07-23 | **Open decision #7 — email-sending service** | User asked whether forgot-password actually sends email (it doesn't — generic response only, no provider). Checked build-plan.md and project-overview.md: no feature anywhere scopes real email delivery; 2.5 Notifications & Announcements is in-app only. Added decision #7 to build-plan.md §7 (provider TBD, needs a shared `EmailService` under `apps/api/src/shared/`, not duplicated per feature) with pointers from 1.1's forgot-password and 2.5's scope note. Non-blocking — doesn't gate 1.2. | **1.2 App Shell + Role-Based Navigation** — same next step as below; this was a documentation-only addendum, no code changed. |
| 2026-07-23 | **1.1 Authentication — Wire Write + Test & Done (Loop steps 5–6) — feature complete (●)** | Wired the three remaining endpoints for real: `signup` (backend `createUserWithRole` — one atomic nested Prisma write for `users` + its `user_roles` row; duplicate email → 409; admin role gated by a new `ADMIN_SIGNUP_KEY` env secret → 403 on mismatch; issues tokens immediately so signup auto-logs in), `logout` (best-effort revoke by hashed token, but always returns success so the client can always clear cookies), `forgot-password` (one fixed message regardless of whether the email exists — no enumeration, no DB lookup). Added matching BFF routes (`app/api/auth/{signup,logout,forgot-password}`) and frontend service functions. Rebuilt `SignupForm` with controlled state + required-field validation for every role-specific field (previously uncontrolled placeholder inputs left from the mock step) and wired it to the real service with toast/redirect on success and a top `Alert` on failure. Wired `ForgotPasswordModal` off its `setTimeout` onto the real call. Documented a deliberate scope boundary: signup's role-affiliation fields (troop number, home council, council name/region/code, employee ID) are validated but not persisted anywhere relational yet — that linking is an admin action in 1.6 (Organization Management), not something self-signup can assert. Left the Sidebar's disabled "Sign out" button untouched — enabling it is 1.2's job, but the `logout()` service function is ready for it. Added `apps/api/tests/auth.test.ts` (11 tests: login/signup/logout/forgotPassword happy + failure paths, repository mocked via `vi.spyOn`, real bcrypt/JWT). All 24 tests pass, typecheck/lint clean in both workspaces. Verified live: curl confirmed signup creates a real Neon row, duplicate-email and wrong-admin-key rejections, logout revocation + refresh-reuse rejection; Playwright confirmed the full browser flow for signup (real toast, auto-login redirect, httpOnly cookies) and forgot-password (real toast), plus the duplicate-email `Alert` rendering. RBAC middleware stays unapplied by design — none of auth's own routes can require a session. **1.1 Authentication is done.** | **1.2 App Shell + Role-Based Navigation** — start at Loop step 1: wire the existing shell's sidebar/topbar to a real session (role-filtered nav items, real user identity in the footer block, enable the currently-disabled "Sign out" button using the `logout()` service function already built). |
| 2026-07-23 | **1.1 Authentication — Wire Read (Loop step 4)** | Login is real end-to-end: backend `auth.repository/service/middleware/controller/routes.ts` mounted at `/api/v1/auth` (login + refresh, with rotation), frontend `features/auth/services/auth.service.ts` + two BFF Route Handlers (`app/api/auth/login`, `.../refresh`) that translate API tokens into httpOnly cookies, and `middleware.ts` gating the whole app on cookie presence. `LoginForm` now calls the real service; removed the now-superseded `DEMO_USERS` mock fixture. Verified live with Playwright against the real dev servers and seeded Neon DB: correct admin login + redirect, correct real "Invalid email or password" on bad creds, cookies confirmed httpOnly (`document.cookie` empty), both middleware redirect directions, and refresh-token rotation + reuse-rejection via curl. `typecheck`/`lint`/`test` clean in both workspaces. | **Loop step 5 (Wire Write)** for 1.1 — wire `signup` (create user + role, transactional), `logout` (revoke refresh token + clear cookies), `forgot-password` (generic non-enumerating response). Then step 6: apply `requireAuth`/`requireRole` somewhere real, unit-test `authService`, walk every failure path. |
| 2026-07-23 | **1.1 Authentication — Contract (Loop step 3)** | Defined the auth contract: backend `apps/api/src/modules/auth/{auth.schema,auth.types}.ts` (Zod validation + `AuthUser`/`AuthTokens`/request-response DTOs for all 5 endpoints, signup discriminated by role), mirrored in `apps/web/src/features/auth/types.ts`, and a `DEMO_USERS` fixture added to `auth.mock.ts` conforming to the new `AuthUser` type. Deliberately stopped short of routes/controller/service/repository — those carry real logic and belong to Loop step 4. Both workspaces typecheck and lint clean. | **Loop step 4 (Wire Read)** for 1.1 — build `auth.routes/controller/service/repository.ts`, mount under `/api/v1/auth`, then replace the frontend's simulated `setTimeout` login/signup with a real fetch through a feature service, JWT + httpOnly cookies via the BFF. |
| 2026-07-23 | **1.1 Authentication — visual sign-off**  | Human checked `/login` end-to-end: UI, responsiveness (900/768/480px), and validation states all confirmed good. **Feature Loop gate 2 passed** — 1.1 moves ◔ → ◑. | **Loop step 3 (Contract)** for 1.1 — define the `POST /api/v1/auth/login\|signup\|refresh\|logout\|forgot-password` request/response shapes and Zod schemas before wiring reads. |
| 2026-07-23 | **1.1 Authentication — review round 2 + tooling** | Fixed the same "no field-level errors" bug on `LoginForm` that round 1 fixed on `SignupForm`: empty email/password fell straight through to the generic "Invalid email or password" alert instead of showing required-field errors first; `handleSubmit` now checks both are non-empty before comparing to the demo account. Installed the **Playwright MCP server** (`.mcp.json` + pre-approval in `.claude/settings.local.json`) so future sessions can drive a real browser instead of curl-only checks — **not active until the next Claude Code restart**, so it has not yet been used to verify anything in this session. | **Restart Claude Code / reload the VS Code window** to activate Playwright MCP, then re-run the full visual sign-off on `/login` (header white text, real logo, 900/768/480px, both forms' empty/invalid/mismatch states) with actual screenshots — this is still Loop step 2, still ungated. |
| 2026-07-22 | **1.1 Authentication — Loop step 1**      | Built the full `/login` screen with mock data: `AuthCard` (tabbed Log In / Sign Up), `RoleSelector` (3-way, real radio-group semantics), `LoginForm` with demo-fill against the actual seeded accounts, `SignupForm` with role-conditional fields and a `PasswordStrengthMeter`, and a `ForgotPasswordModal` replacing the prototype's `prompt()`/`alert()`. Reused every existing primitive (`Alert`, `Button`, `FormField`, `Input`, `PasswordInput`, `Select`, `Modal`, `Toast`) — built no new base components, only feature-local ones now registered in ui-registry.md. `typecheck` and `lint` clean. No visual/screenshot tool is available in this environment, so the dev server was only confirmed to boot and serve `/login` with the expected markup (curl) — **not** a substitute for the human visual-verify gate. | **Human visual sign-off** of `/login` at 900/768/480px against the five flagged deviations, then Loop step 3 (Contract) for 1.1. |
| 2026-07-22 | **Prisma 7 upgrade**                      | Resolved the editor/project version split by upgrading the project rather than downgrading the extension. `prisma` + `@prisma/client` → **7.9.0** (exact-pinned), added `@prisma/adapter-pg` + `pg`. Moved the connection URL out of `schema.prisma` into a new `prisma.config.ts`, wired the node-postgres driver adapter into `src/config/prisma.ts`, pointed `seed.ts` at that singleton, and dropped the removed `package.json#prisma` key. `migrate diff` confirms **no drift** — the v7 schema produces exactly the database the v6 migration built, so the existing migration stands. Also set `sslmode=verify-full` on the Neon URL to preserve strict certificate verification ahead of the pg v9 default change. All green: typecheck, lint, 13 tests, both builds, seed, health `database: up`. | **Feature 1.1 Authentication.**                                                                                                                                                                               |
| 2026-07-22 | **0.5 database live + Prisma diagnostic** | Connected Neon PostgreSQL, created and applied migration `20260722142556_init` (37 tables), and ran the seed — verified idempotent on a second run, one-role-per-user invariant intact, `GET /api/v1/health` now 200 `database: up`. Debugged the reported `datasource.url` error: **not a schema bug** — the Prisma VS Code extension's language server is a v7 build while the project is pinned to 6.19.3, where `url` is required. Added a schema comment so nobody "fixes" it by deleting the line.                                                                                                                                                                                                                                                                                                   | **Feature 1.1 Authentication.** Optional cleanup: pin the Prisma extension to v6 or plan a deliberate v7 upgrade.                                                                                             |
| 2026-07-22 | **Phase 0 exit — visual verify**          | Human tested `/gallery` and the app shell: correct and responsive at 900 / 768 / 480px. **0.2 and 0.3 passed Feature Loop gate 2** and moved ◔ → ●. Phase 0 is complete (5/5). The five deliberate deviations from the prototype are approved and are now the house standard; the breakpoint choice is recorded as decision #6.                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Feature 1.1 Authentication** — start at Loop step 1 (auth screen with mock data). The migration + seed is still open, but it only blocks 1.1's step 4, not its start.                                       |
| 2026-07-22 | **Phase 0 — 0.1 through 0.5**             | Scaffolded the monorepo (npm workspaces: `apps/web` Next 15 + Tailwind 3.4, `apps/api` Express + Prisma), ported the design system with both WCAG fixes applied at the token layer, built 20 UI components incl. all six §9 components the prototype lacked, built the app shell with all four mobile-nav defects fixed, built the backend spine (`asyncHandler`, `errorHandler`, typed env validation, `GET /api/v1/health`), and wrote the full 33-model Prisma schema plus an idempotent seed. Verified: typecheck, lint, 13 tests, production builds, and both servers booted and probed. Chose mobile-first breakpoints with a custom `lg2: 900px` (recorded in ui-rules §5).                                                                                                                         | **Human visual verification of `/gallery` and the shell at 900/768/480px** (Loop gate 2 for 0.2 and 0.3), then run the migration + seed against a PostgreSQL instance. Phase 1.1 (Authentication) after that. |
| 2026-07-22 | UI docs + skills pass                     | Reframed the prototype as a **baseline, not a ceiling** across all docs and skills. Audited Gsp.html: found 0 loading states, 0 empty states, 0 pagination (9 tables), ~1 semantic landmark, `outline:none` ×3, and 2 WCAG contrast failures (`#aaa` 2.32:1, white-on-gold 2.30:1). Added ui-rules §9 Accessibility + §10 Sanctioned Enhancements, ui-registry §9 (15 required components the prototype lacks), expanded Phase 0.2 scope to build them.                                                                                                                                                                                                                                                                                                                                                    | Begin Phase 0.1.                                                                                                                                                                                              |
| 2026-07-22 | Role model decision                       | Settled #5: relational `user_roles` authoritative, flat `User.role` removed from the Prisma example, added the missing `role_permissions` link table and full Prisma models to database-design.md. All decisions now closed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | —                                                                                                                                                                                                             |
| 2026-07-22 | Build-plan review                         | Audited plan vs. all source docs. Fixed 6 defects: icon/chart contradiction settled, dashboard `?role=` security flaw removed, 4 missing features added (renewal, adult-leader registration, approval workflow, activity-report submission), test runner added to 0.1, env/secrets added to 0.4, Phase 1 reordered so Membership precedes Dashboard. Count 19 → 21.                                                                                                                                                                                                                                                                                                                                                                                                                                        | Resolve open decision #5 (role model).                                                                                                                                                                        |
| 2026-07-22 | Planning docs                             | Created ui-rules, ui-registry, build-plan, progress tracker. Clarified auth/BFF wording in project-overview.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | —                                                                                                                                                                                                             |
